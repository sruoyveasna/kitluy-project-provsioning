# KitLuy Independent Review Record

## 0. Review identity

| Field           | Value                                                                                                          |
| --------------- | -------------------------------------------------------------------------------------------------------------- |
| Task ID         | `WS-11-T003 Step 4`                                                                                            |
| Task title      | `Step-4 completion: deployed lapse worker, durable-job governance, post-approval verdicts, job-governor repair` |
| Reviewer        | `R2 — jobs, permissions and post-approval (independent; did not write any work under review)`                  |
| Review role     | `independent review agent`                                                                                     |
| Reviewed commit | `1dcc38f85605ca0810e658da59ddce604f7c9e4d` (HEAD at review time; `git status` clean except the R1 record)      |
| Base commit     | `2a54e1c` (eleven commits reviewed: `2a54e1c..1dcc38f`)                                                        |
| Branch/worktree | `main` @ `C:\Users\Hello-Evo-PC\Desktop\HET-KITLUY-PROJECT`                                                    |
| Review date     | `2026-08-01`                                                                                                   |
| Decision        | `APPROVED-WITH-CONDITIONS`                                                                                     |

## 1. Independence check

- Reviewer was not the primary writer: `PASS` (the eleven commits are a single-author chain by Soenghak Choeurn; this reviewer wrote none of them).
- Reviewer did not modify the task branch: `PASS` (no commits, no push, no tracked-file edits; only this review record was created; `git status --porcelain` at the end of the session shows only the pre-existing untracked R1 record and this file).
- Reviewed commit matches evidence commit: `PASS` (every command below ran against the checkout at `1dcc38f`; live cloud DB carries migrations through `20260731220000`/0160, hub DB journal through `0030` with checksum `9c7146ba…` matching R1's verification).

## 2. Materials reviewed

- task file: no `00_AI_HANDOFF/tasks/WS-11-T003*.md` exists; the operative specification is the Section-15 R2 inspection list (eleven items) plus the Step-4 handoff chain.
- handoff: `00_AI_HANDOFF/shared/2026-07-31__SHARED__WS-11-T003-STEP4__LIFECYCLE-CENSUS-AND-INDEPENDENT-REVIEW__AI-HANDOFF.md` (six blocking findings the eleven commits close: A1, B2, B3, B4, C4, C8; plus KLRISK-DEVICE-011).
- evidence: commits `9a9ca04, 24cb688, 5fd9522, 9ed400a, bbbbf39, c51cf19, 667f6d4, e86b8bd, b548e50, 7d89394, 1dcc38f`; live databases (cloud `postgres` @54322 through 0160, seeded; hub `kitluy_hub_local` through 0030, seeded — neither reset, neither mutated outside the assigned test runs).
- source authorities: AGENTS.md locked rules; KLD-2026-07-29-DEVICE-CREDENTIAL-REVOCATION-001 §2.4 (deadline fixed, never extended); KLD-2026-07-31-SECURITY-TEST-CLOCK-001 (sanctioned test authority pattern).
- diff range: `2a54e1c..1dcc38f`.

## 3. Scope and file ownership

| Check                                  | Result  | Notes                                                                                                                                                    |
| -------------------------------------- | ------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Changed files stay within allowlist    | `PASS`  | The eleven commits touch device-registry-service src/test, device-identity test support, cloud migrations 0159/0160 + assertions.sql, hub migrations 0029/0030, hub-agent, vitest configs, root package.json — the Step-4 remediation surface only. |
| No overlapping active task ownership   | `PASS`  | Single-author linear chain on `main`; no interleaved authors in range; R1's review covers a disjoint inspection list (hub runtime/snapshot) with no file conflicts. |
| Non-goals/prohibited changes preserved | `PASS`  | No APPLIED migration rewritten (cloud 0159/0160 functions exist live with post-repair shape; hub journal checksums for 0029/0030 match current files per R1 §6, spot-confirmed `9c7146ba…`). No push (push URL remains disabled). No database reset. |
| Dependencies/base commit valid         | `PASS`  | Base `2a54e1c` is the documented NOT-PROMOTED state; the eleven commits build on it linearly.                                                             |

## 4. Acceptance-criteria review (Section 15, R2 items 1–11)

| AC ID | Primary evidence | Reviewer verification | Result | Notes |
| ----- | ---------------- | --------------------- | ------ | ----- |
| R2-1 Real worker entrypoint | commits 24cb688, e86b8bd | `main.ts:32` resolves runtime before socket; `main.ts:48-54` constructs loop from `revocation.jobGateway`/`revocation.service`; `main.ts:112-115` starts AFTER `server.listen`; `main.ts:131-137` stops worker before `revocation.shutdown()`; `lapse-worker-runtime.ts:86-91` one identity per process; `:116-137` tick never throws; `:131-134` logs `error.name` only. Suite **14/14 PASS** incl. source assertions on main.ts wiring | `PASS` | |
| R2-2 Durable claim and lease | migration 0135 | `0135:555` FOR UPDATE SKIP LOCKED; `:564` attempt_count once per claim; `:549` expired-lease reclaim keeps history; `:577-593` `assert_active_lease` raises `KLUY-JOB-STALE-LEASE`; `:647-650` ALREADY_COMPLETED replay idempotent; `:701-709` defer preserves attempt; `:286-288` append-only trigger on attempts; all doors SECURITY DEFINER + fixed search_path. lapse-worker suite **6/6**; credential-lifecycle-jobs **12/12** (concurrent claim → exactly one winner, loser `STALE-LEASE`; reclaim → dead worker `STALE-LEASE`, attempt history kept) | `PASS` | retryable/permanent/ambiguous classification verified in `lapse-worker.ts:147-162` + `retry-policy.ts:58-110` (unmapped code → manual_review, fail closed) |
| R2-3 Lapse execution end-to-end | commits 24cb688, e86b8bd | runtime suite stages 1-9: shipped route 201 → job enqueued (environment-matched) → pre-deadline tick DEFERS (`retry_scheduled`, no attempt consumed) → deadline moved EARLIER (only allowed direction) → worker claims → LAPSED verdict actor NULL, `late=true` → escalation `escalated_at` set, reason contains `LAPSED` + `credentials stay revoked` → credential stays `revoked`. Stage 5 proves +10 years REFUSED inline; trigger `KLUY-EMERGENCY-DEADLINE-FIXED` at `0152:255-257`. **14/14 PASS** | `PASS` | |
| R2-4 Job-governor membership removal | migration 0160 | Live psql: `pg_has_role('postgres','kitluy_job_governor','MEMBER')` = **f**; `pg_auth_members` shows **zero members at all** (login-capable or not); all 8 functions owned by `kitluy_job_governor`, `prosecdef=t`, `proconfig search_path=pg_catalog, kitluy_ops`; explicit proacl with NO PUBLIC anywhere; scaffolds + `escalate_dead_letter_job_v1` + `release_manual_review_job_v1` → `kitluy_test_harness` ONLY; `kitluy_worker_service` appears in NONE of the 10 ACLs | `PASS` | KLRISK-DEVICE-011 closed live; census `job_governor_recorded_exception:"0"` (was 1) |
| R2-5 Inspection-reader narrowness | migration 0160 §1–§2 | Four readers: one `uuid` in, one scalar out (`attempt_count`/`deferral_count`/`status`/`last_failure_code`); `language sql`, `stable` (live `provolatile='s'`), no dynamic SQL, fixed search_path; worker `has_table_privilege` on `kitluy_ops.durable_jobs` SELECT/INSERT/UPDATE/DELETE = **f/f/f/f** (and on attempts f/f); `authenticated` on durable_jobs = f/f/f/f; worker's EXECUTE surface in kitluy_ops is exactly the governed door (claim/start/complete/defer/fail/record_attempt/enqueue/backoff/summary/authoritative_now) | `PASS` | See RV-004 on the (currently unreachable) `authenticated` EXECUTE grant |
| R2-6 APPROVE branch | production-lifecycle stage 23 | Self-approval refused containing `SELF` while the responder holds BOTH grants (the rule itself, not a missing permission); distinct second human → `POST_APPROVED`, verdict `APPROVED` naming `approverB.userId`; conflicting REFUSE after APPROVE → `ALREADY_DECIDED`; late lapse → `ALREADY_DECIDED`; credential stays revoked. Full suite **30/30 PASS**, stderr `executed=30/30 stages=[1,…,30]` one correlation id | `PASS` | |
| R2-7 REFUSE branch | emergency-refuse suite | **14/14 PASS**: stage 3 self-REFUSE refused (`SELF`, stays PENDING); stages 4-5 distinct human, verdict `REFUSED` naming refuser not responder; stage 6 evidence CONSUMED exactly once, `consumed_for_authorization` = verdict id; stage 7 deadline ISO-identical (NOT extended); stage 8 credential stays revoked; stages 4+13 `MANUAL_SECURITY_REVIEW` + non-empty escalation, `late=false`; stage 10 replay `ALREADY_DECIDED`, one verdict; stage 11 conflicting APPROVE `ALREADY_DECIDED`; stage 12 lapse after REFUSE `ALREADY_DECIDED`; stage 14 zero runtime roles with UPDATE on credentials | `PASS` | no synthetic approver anywhere (verdict decider = the distinct human) |
| R2-8 Lapse/REFUSE race | emergency-concurrency suite + package suite | Assigned file **3/3 PASS**: A — expiry-while-parked → SQLSTATE 42501 `REAUTHENTICATION-REFUSED`, credential NOT revoked, evidence still ACTIVE unconsumed with ORIGINAL expiry, zero authorization residue; B — scope mutated mid-park → `EMERGENCY_REFUSED`/`KLUY-CRED-REVOCATION-SCOPE-EMPTY`, ≤1 revocation row; C — identical replay `ALREADY_AUTHORIZED` same id, conflicting replay `EMERGENCY_REFUSED`/`KLUY-EMERGENCY-CONFLICTING-REPLAY`, exactly ONE authorization/scope, both losers' evidence unconsumed. Literal lapse-vs-human race verified in `governed-emergency-concurrency` **14/14 PASS**: scenario 8 (post-approval vs sweeper → exactly one verdict, LAPSED wins, loser audited as loser) and scenario 10 (two sweepers → exactly one lapse) | `PASS` | See RV-006 on file mapping |
| R2-9 Evidence consumption | 0152 + REFUSE stage 6 | `0152:291-303` `emergency_post_approval_reauth_spend_v1` passes `p_verdict_id` as the `consumed_for_authorization` argument into `kitluy_auth.consume_reauthentication_evidence_v1` — the REFUSE spend is bound to the VERDICT; verdict row carries the authorization id (stage 5), so evidence→verdict→authorization is exactly one link each; stage 6 asserts `consumed_for_authorization = verdict_id` and total consumed = 1 | `PASS` | single-use enforced by the governed consumer (0158 authoritative clock; expired-ACTIVE spend PROVEN refused by the census) |
| R2-10 SET ROLE escalation census | assertions.sql inspection | Sections 40a (`6545-6644`) / 40b (`6649-6843`) read: NOLOGIN governors, RLS ENABLED+FORCED, definer+fixed search_path+no PUBLIC, worker holds no table authority and cannot release/cancel/escalate its own jobs, append-only + no-delete triggers; 40b borrows `kitluy_test_harness` for exactly one block and returns it in BOTH the success path (`6831`) and the exception path (`6836-6840`). All three SET ROLE probe loops (`8772-8808`, `~9850-9917`, `10494-10525`) treat `permission denied to set role%` as the fail-CLOSED answer and record any OTHER error as a finding, with the 0160 rationale in comment | `PASS` (inspection) | `pnpm db:test` **NOT RUN** — forbidden (commits rows into the shared evidence DB). Claim `197 PASS` does not reconcile with static notice count at HEAD: assertions.sql 92 + rls-tests.sql 104 = **196**, and the range added/removed ZERO PASS notices (rls-tests.sql untouched). See RV-003 |
| R2-11 Final privilege census | residue-spendability-census | **14/14 PASS** standalone AND inside the full 219/219 run. `[census spendable]`: `spendable_evidence "0"`, `leaked_memberships "0"`, `job_governor_recorded_exception "0"`, `test_clock_policy_rows "0"`, `overdue_unresolved_authorizations "0"`, `legacy_reachable "0"`, `unsafe_builder_runtime_access "0"`, `unsigned_active_hub_snapshots "0"`, `cross_scope_active_hub_snapshots "0"`, `sync_worker_active_snapshot_mutation "0"`, `abandoned_durable_jobs "0"` — and `effective_temporary_grants "1"` (see RV-001/RV-002) | `PASS` | The claimed "all zeros" does NOT reproduce for one bounded bucket |

## 5. Technical review checklist

| Area                                          | Result  | Notes |
| --------------------------------------------- | ------- | ----- |
| Correctness and edge cases                    | `PASS`  | NOT_DUE defers without consuming attempts; ALREADY_DECIDED is success not failure; deadline direction trigger proven in both directions. |
| Contract/schema compatibility                 | `PASS`  | 0160 additive; 0135/0152 unchanged; job payload contract = exactly one authorization id enforced by `readLapseAuthorizationId`. |
| Migration safety and RLS                      | `PASS`  | 0160 in-migration guard proves membership removed, worker cannot inspect, scaffold boundary; job tables RLS ENABLED+FORCED (40a read). No production application (LOCAL only per KL-INF-P1-037 header). |
| Tenant/Store/Location isolation               | `N/A`   | Not this change's surface; environment isolation of jobs proven (`durable_job_status_summary_v1('pilot',…)` = 0 in 40b; worker claims only its own environment, runtime suite stage 2). |
| Permissions, re-auth, approval, audit         | `PASS`  | Four-eyes enforced by rule not by missing grants; verdicts append-only (`trg_device_emergency_post_approval_verdicts_append_only` 0152:184); distinct human named on every verdict; a lapse names NOBODY. |
| Append-only finance/payment/inventory/custody | `N/A`   | Not in scope; job attempts and verdicts are append-only by trigger and jobs are never deleted (40a). |
| Idempotency, retries, replay, ordering        | `PASS`  | Dedupe on authorization id (unique constraint); scheduling idempotent (EXISTING); completion replay ALREADY_COMPLETED; verdict replay ALREADY_DECIDED; deadline never moves later. |
| Store Hub/offline/reconnect behavior          | `N/A`   | R1 scope; hub-side items verified there. |
| T1/T2/T3/T4 boundaries                        | `N/A`   | Unchanged by these commits. |
| Error, stale, partial and degraded states     | `PASS`  | Tick never throws; stale lease refused on complete AND fail; expired lease reclaimable without erasing history; ambiguous commit → manual_review (fail closed). |
| Security, privacy, secrets and logging        | `PASS`  | Worker startup logs identity/port/environment only — no DSN, roles, or credentials (`main.ts:116-125`); tick failure logs `error.name` only; payload strictness means the worker can be told nothing to decide. |
| Khmer/English, KHR/USD, timezone              | `N/A`   | No user-facing surface; deadline comparisons use database time (`clock_timestamp()`), not worker-supplied instants. |
| Accessibility and UX states                   | `N/A`   | No user-facing surface. |
| Observability and operations                  | `PASS`  | Start/stop/tick logs with workerInstanceId; durable_job_status_summary_v1 exists for operators; manual-review/dead-letter path is two governed acts, harness-only in tests. |
| Test quality and negative coverage            | `PASS`  | Negative paths executable: self-approval with both grants held, stale lease on two doors, mutation-refusal probes as table owner, expired-ACTIVE evidence spend through the governed door, conflicting replay, lapse-after-verdict, verdict-after-lapse. |
| Documentation and rebuildability              | `PASS`  | File headers state authority and what is NOT claimed; one stale header found (RV-005). |

## 6. Reviewer validation

Environment: Node v22.23.0, pnpm 9.15.9 (`/c/Users/Hello-Evo-PC/toolchains/node-22.23.0`), psql 17, `PGPASSWORD=postgres`; cloud DB `postgres` @127.0.0.1:54322 (through 0160, seeded), hub DB `kitluy_hub_local` (through 0030, seeded). Neither database was reset. All psql below was read-only catalog/fact queries.

| Command/check | Environment | Result | Evidence | Notes |
| ------------- | ----------- | ------ | -------- | ----- |
| `services/kitluy-device-registry-service: pnpm vitest run test/lapse-worker-runtime.integration.test.ts` | live cloud DB | `PASS` | **14 passed / 0 failed / 0 skipped**, 1 file | R2-1, R2-3 |
| `services/kitluy-device-registry-service: pnpm vitest run test/lapse-worker.integration.test.ts` | live cloud DB | `PASS` | **6 passed / 0 failed / 0 skipped**, 1 file | R2-2 |
| `packages/device-identity: pnpm vitest run test/credential-lifecycle-jobs.integration.test.ts` | live cloud DB | `PASS` | **12 passed / 0 failed / 0 skipped**, 1 file | R2-2 concurrency aspects (SKIP LOCKED, STALE-LEASE ×2) |
| `services/kitluy-device-registry-service: pnpm vitest run test/production-lifecycle.integration.test.ts` | live cloud+hub DB | `PASS` | **30 passed / 0 failed / 0 skipped**; stderr `executed=30/30 stages=[1,…,30]` | R2-6; matches claim exactly |
| `services/kitluy-device-registry-service: pnpm vitest run test/emergency-refuse.integration.test.ts` | live cloud DB | `PASS` | **14 passed / 0 failed / 0 skipped**, 1 file | R2-7, R2-9 |
| `services/kitluy-device-registry-service: pnpm vitest run test/emergency-concurrency.integration.test.ts` | live cloud DB | `PASS` | **3 passed / 0 failed / 0 skipped**, 1 file; real PIDs, `barrier:"Lock"` ×3 | R2-8 |
| `packages/device-identity: pnpm vitest run test/governed-emergency-concurrency.integration.test.ts` | live cloud DB | `PASS` | **14 passed / 0 failed / 0 skipped**, 1 file | R2-8 literal lapse races (scenarios 8, 10) |
| `services/kitluy-device-registry-service: pnpm vitest run test/residue-spendability-census.integration.test.ts` | live cloud+hub DB | `PASS` | **14 passed / 0 failed / 0 skipped**; spendable all `"0"` EXCEPT `effective_temporary_grants:"1"` | R2-11; RV-001/RV-002 |
| `services/kitluy-device-registry-service: pnpm vitest run` (FULL) | live cloud+hub DB | `PASS` | **219 passed / 0 failed / 0 skipped, 16 files**, 16.98s; census green in-run | Matches claimed 219/219 |
| psql cloud: `pg_has_role('postgres','kitluy_job_governor','MEMBER')`; `pg_auth_members` for governor | live cloud DB | `PASS` | `f`; zero members at all | R2-4 |
| psql cloud: 8 function owners/prosecdef/proconfig; proacl of all 10 functions | live cloud DB | `PASS` | all governor-owned, secdef, fixed search_path; no PUBLIC; harness-only scaffolds+escalate+release; worker in no ACL | R2-4, R2-5 |
| psql cloud: `has_table_privilege` worker/authenticated on `kitluy_ops.durable_jobs(+attempts)`; worker kitluy_ops EXECUTE surface; schema USAGE | live cloud DB | `PASS` | worker S/I/U/D = f/f/f/f; authenticated f/f/f/f; worker EXECUTE = governed door only; `authenticated` schema USAGE = f | R2-5 |
| psql cloud: surviving-grant detail (subject, profile, window) | live cloud DB | `PASS` (fact-finding) | grant `5641cdfb…` subject `d7944ee6…`, ACTIVE profile, development, `2026-07-31 23:15:52Z → 2026-08-01 00:16:52Z`, reason `WS-11-T003 concurrency-d7df9017`; 0 live evidence | RV-001/RV-002 |
| psql cloud+hub: migration state | live DBs | `PASS` | cloud max version `20260731220000` (59 rows); hub journal top = `0030_governed_snapshot_staging.sql` checksum `9c7146ba0229` | DBs intact, not reset |
| assertions.sql: sections 40a/40b, three SET ROLE probe loops, PASS-notice count | repo inspection | `PASS` (inspection) | fail-closed handling confirmed ×3; 92 + 104 = 196 static PASS notices | R2-10; RV-003 |
| `pnpm db:test` (claim: 197 PASS) | — | `NOT RUN` | — | FORBIDDEN by assignment (commits rows into the shared evidence DB). Static count at HEAD supports 196, not 197 — see RV-003 |
| `pnpm verify` (claim: 12/13, pre-existing CRLF only) | — | `NOT RUN` | — | Not in R2's assigned list; the failing gate (repository-wide prettier CRLF) is pre-existing and unchanged by these commits |
| `pnpm test:rls` / `pnpm hub:db:test` | — | `NOT RUN` | — | Not in R2's assigned list; shared-DB mutation class |

**Totals across executed vitest commands:** 8 suites run individually = **107 passed / 0 failed / 0 skipped** (14+6+12+30+14+3+14+14); plus the full registry run **219 passed / 0 failed / 0 skipped (16 files)**. Every suite ran green on first attempt in this session.

## 7. Findings

| Finding ID | Severity | Path/location | Description | Required remediation | Blocking? |
| ---------- | -------- | ------------- | ----------- | -------------------- | --------- |
| `RV-001` | `LOW` | `services/kitluy-device-registry-service/test/residue-spendability-census.integration.test.ts:155-164` | Claim-accuracy: the reviewed claim says "census 14/14 all zeros". Reproduced twice (standalone 07:00, inside the full 219/219 run 07:03): `effective_temporary_grants` reports `"1"`. The test PASSES because the assertion is a bound (`toBeLessThanOrEqual(8)`) by documented design, while the test title still says "has ZERO effective temporary permission assignments". Identified the "1": grant `5641cdfb-dea1-4575-8564-8c07e799765b` for `fleet.device_credential.emergency_revoke` in `development`, reason `WS-11-T003 concurrency-d7df9017`, window `2026-07-31 23:15:52Z → 2026-08-01 00:16:52Z` (self-expired during this review). Not a security defect — environment-exact, time-boxed, 0 live evidence for the subject — but "all zeros" and the title overstate what the bound asserts. Same class as R1's RV-001, independently reproduced with the origin identified. | Correct the claim wording to "14/14, all bounded buckets within bounds"; retitle the test to match the bound. | no |
| `RV-002` | `LOW` | `services/kitluy-device-registry-service/test/emergency-concurrency.integration.test.ts:97-99`; `test/support/emergency-success-fixture.ts:176-204` | Teardown invisibility: the surviving grant's subject (`d7944ee6…`) still has an ACTIVE `admin_user_profiles` row AND the effective grant, i.e. `disposeEmergencyActor` never completed for run `d7df9017`, and the `afterAll … .catch(() => undefined)` swallowed the failure silently. The census's `≤8` bound then makes the residue invisible by design. Blast radius is limited (development-only, the grant's own `expires_at` bounds spendability regardless of the swallowed delete; profile without grant/evidence authorizes nothing after expiry), but "teardown that lies" is exactly the defect class the previous session claimed fixed. | Make dispose failures loud (log + fail the suite) rather than swallowed; optionally have the census assert zero grants older than the maximum fixture window. | no |
| `RV-003` | `LOW` | claim "db:test 197" vs `supabase/tests/assertions.sql` (92 `raise notice 'PASS`), `supabase/tests/rls-tests.sql` (104) | Claim-accuracy: `pnpm db:test` was NOT RUN (forbidden — it commits rows into the shared evidence DB). Statically, HEAD supports **196** PASS notices (92 + 104); the reviewed range added and removed ZERO PASS notices and never touched rls-tests.sql; every prior handoff recorded 196. The "197" claim does not reconcile with the file contents at `1dcc38f`. Either the claim is off by one or the count method included a non-notice line. | Operator runs `pnpm db:test` once and records the true number, or the claim is corrected to 196. | no |
| `RV-004` | `NOTE` | `supabase/migrations/20260731220000_0160_repair_job_governor_membership_leak.sql:310-317` | The four `inspect_*` readers are granted to `postgres, authenticated`. Verified live that `authenticated` has NO USAGE on schema `kitluy_ops` (`has_schema_privilege` = f), so the grant is currently unreachable — but if schema usage is ever granted to `authenticated` broadly, every logged-in user could read job status/attempt metadata for any known job id. A dedicated assertion role would make the intent structural rather than incidental. | Consider granting the readers to a narrow assertion role instead of `authenticated`. | no |
| `RV-005` | `NOTE` | `services/kitluy-device-registry-service/test/residue-spendability-census.integration.test.ts:8-31` | The census header describes the missing-`vitest.config.ts` / parallel-ordering hazard as current ("there is no `vitest.config.ts` in this service"), but commit `1dcc38f` added `services/kitluy-device-registry-service/vitest.config.ts` (`fileParallelism: false`) — the "separate, verifiable work" the header said was needed. The header is now stale about its own resolution. | Update the header to record that serialization landed in `1dcc38f`. | no |
| `RV-006` | `NOTE` | assignment item 8 vs `services/kitluy-device-registry-service/test/emergency-concurrency.integration.test.ts` | File mapping: the service file named by the assignment holds scenarios A/B/C (expiry-while-parked, scope mutation, replay) — it proves one terminal business effect, no double-consumption, and no loser residue, but contains no lapse-vs-verdict race. The literal race lives in `packages/device-identity/test/governed-emergency-concurrency.integration.test.ts` scenarios 8 (post-approval vs sweeper → exactly one verdict, LAPSED) and 10 (two sweepers → exactly one lapse), which this review ran: 14/14 PASS. No duplicate escalation anywhere (verdicts unique per authorization, lifecycle stage 26; `escalate_governed_emergency_v1` uses `on conflict do nothing`, 0152:336-342). | None for the code; future review assignments should name both files for this item. | no |

## 8. Decision rationale

All eleven assigned controls were verified in code AND reproduced against the live databases; nothing was taken from documentation or primary-agent claims alone.

The job-governor membership leak (KLRISK-DEVICE-011) is genuinely closed: zero members of any kind remain, the eight replacement functions are governor-owned SECURITY DEFINER with fixed search paths, the grant boundary (harness-only scaffolds, worker nowhere, no PUBLIC) is enforced by an in-migration guard and confirmed live, assertions.sql's section 40b now borrows the sanctioned harness with hand-back in both paths, and the census asserts the exception at zero so it cannot return unnoticed. The durable runtime is sound: SKIP LOCKED claiming, one-attempt-per-claim, stale-lease refusal on every lease-bound door, append-only attempt evidence, idempotent completion replay, deferral that never consumes the retry budget, and fail-closed classification of ambiguous outcomes. The lapse path now has a real production runtime — constructed from the resolved composition, started after the socket opens, stopped before the pool closes, one identity per process, a tick that never throws and logs redacted names only — and the end-to-end chain (route → enqueue → claim → governed lapse → LAPSED with actor NULL → escalation → credential stays revoked) reproduces green. Both post-approval branches are proven with distinct humans, single-use verdict-bound evidence, a deadline that provably cannot move later, terminal verdicts, idempotent replay, and escalation to MANUAL_SECURITY_REVIEW without restoration.

No blocking findings exist in R2's scope. Three LOW findings are claim/teardown hygiene: the census claim "all zeros" does not reproduce for one deliberately bounded bucket (RV-001), a swallowed fixture teardown left one self-expiring development grant + ACTIVE profile visible to that bound (RV-002), and the "db:test 197" claim does not reconcile with the static 196 PASS notices at HEAD and could not be executed under the assignment's prohibition (RV-003). None weakens a control; all three are corrections to statements about the work rather than to the work itself, and the task's own claim-honesty discipline makes them conditions of promotion rather than merge blockers. Verdict: **APPROVED-WITH-CONDITIONS** — the conditions being the three corrections in RV-001/RV-002/RV-003.

## 9. Merge conditions

- [x] reviewed commit is unchanged (`1dcc38f`; tree clean apart from the two untracked review records);
- [x] all blocking findings resolved (none found by this review);
- [x] required validation/evidence complete (all assigned commands run; forbidden/unassigned checks marked NOT RUN with reasons);
- [x] conflict records resolved (none opened by this review);
- [ ] claim corrections RV-001 (census wording), RV-002 (teardown visibility), RV-003 (db:test count) applied or explicitly accepted by the owner;
- [ ] branch updated to approved integration point (owner's merge decision);
- [ ] integration checks pass (full-repo `pnpm verify` not re-run by R2; claimed 12/13 with the pre-existing CRLF condition);
- [ ] authorized human approval obtained for sensitive implications (promotion of WS-11-T003 Step 4 remains the owner's decision; this review covers R2 scope only).

## 10. Reviewer truth statement

The reviewer did not treat documentation, a build, or a primary-agent summary as proof of deployment, migration application, pilot operation, or production correctness. Every PASS above is backed by a command executed in this review session against the live databases or the checked-out tree at `1dcc38f`; every check not executed is marked NOT RUN with its reason. Where a claim could not be reproduced exactly (census zeros, db:test 197), this record says so and shows what reproduced instead.
