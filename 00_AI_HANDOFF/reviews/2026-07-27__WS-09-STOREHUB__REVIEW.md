# KitLuy Independent Review Record

## 0. Review identity

| Field            | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Task ID          | WS-09-T001..T007 (Cycle 8 / 8B — Store Hub local runtime and persistence)                                                                                                                                                                                                                                                                                                                                                                                                                            |
| Task title       | Reconcile the Hub-local schema, build Hub migration tooling and DDL, implement the Hub command layer, journals, safety modes and recovery, and execute the offline/authorization/recovery test matrix                                                                                                                                                                                                                                                                                                |
| Reviewer         | Independent review subagent — did NOT author any reviewed commit                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Review role      | independent review agent (WS-09-T007)                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Reviewed commits | `bcc7423` (schema reconciliation), `0210fb4` (Hub tooling), `7000958` (Hub schema), `87c9fd7` (amendment), `c2dc40a` (command layer), `e70539c` (T005/T006 tests)                                                                                                                                                                                                                                                                                                                                    |
| Base commit      | `3cda2c4` (main, close of the KL-DEC-001 alignment cycle)                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| HEAD at review   | `e70539cd23997cf54e510a0b919fb64e9aafaeeb`, clean tree before and after this review                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Branch/worktree  | `main`, `C:\dev\HET-KITLUY-PROJECT` (Windows Git Bash); Hub DB `kitluy_hub_local` on `supabase_db_kitluy-local` (PostgreSQL 15.8, recorded gap G7)                                                                                                                                                                                                                                                                                                                                                   |
| Review date      | 2026-07-28 (review record dated 2026-07-27 per the assigned filename)                                                                                                                                                                                                                                                                                                                                                                                                                                |
| Decision         | **APPROVED-WITH-CONDITIONS** — every mandated gate re-executed by the reviewer PASSED (`verify` 11/11, `docs:verify` 8/8, `db:test` **121**, `test:rls` **95**, `hub:db:test` **29**, hub-agent suite **147 passed / 2 skipped**, backup/restore **2 passed** under `KITLUY_HUB_DESTRUCTIVE_TESTS=1`). All 44 reviewer-designed probes held. **No blocking finding.** Eight findings (RV-001..RV-008) are advisory; RV-001 gates the STATUS PROMOTION paperwork, not the merge and not the software. |

Content binding (re-review required if any of these change):

- `hub/tests/assertions.sql` sha256 `dbc4f691b40c5f332b04692ada097a6c42479649b2dcf9c6e90f03dd1fb49e1c`
- `hub/seed/dev-fixtures.sql` sha256 `7aa8a92d45acc388ac43a36da7a6e50371590babe593d0357090ffa5348e50f0`
- `scripts/hub/hub-db.mjs` sha256 `bd9cdd6aa9610403540fa96a9ad68b0ce528aaf8a48984d2eb702d11046258a3`
- `scripts/hub/hub-validate.mjs` sha256 `0b01c88ec66b7c0e173549aa39208cfa27f2780cee92b126610f9e2230963957`
- `services/kitluy-hub-agent/src/hub/authorization.ts` sha256 `3f9074ab68797e00f872efd6e1917a3a322b7764056a3db84db844aa163d0ac3`
- `services/kitluy-hub-agent/src/hub/command-pipeline.ts` sha256 `00532f0dae48bb9b04f9c72f9f3cb9c738731e457d417ca09c96cc8de8a1b99e`
- `services/kitluy-hub-agent/src/hub/idempotency.ts` sha256 `d77745136c0b6845a43972f1064486b6f15688f5ea0051223a4917be4c9bc523`
- `services/kitluy-hub-agent/src/hub/outbox.ts` sha256 `c04892736d40aaac6bb64f8f4e7b848d808a548622dcc501e13d6d2ce117a678`
- `services/kitluy-hub-agent/src/hub/repositories/sync.ts` sha256 `9c8de98f27ff553c08ebda281f30e8d6e6a96e90c0e16f1745958783504d5e46`
- `services/kitluy-hub-agent/src/hub/safety-mode.ts` sha256 `1221ab9d54f17e7b6c12d04dc8b900b8446828203313a5715e40100b6f3081d8`
- `services/kitluy-hub-agent/src/hub/open-items.ts` sha256 `1552da3342007fdc798984f60687d1a9ec9f8f8bbdfee6cbc306f266568abfb0`
- `services/kitluy-hub-agent/src/hub/booking-status.ts` sha256 `77222148251240245905fa8dfaa757592e77cf3f19c0904f9b7debadfb9443ae`
- `docs/data/kitluy-storehub-local-schema-reconciliation-v1.0.0.md` sha256 `0dbad9068479c0407ad538278a8576fc48933d0202219fdd949b9209cfabef7e`
- `docs/data/kitluy-storehub-local-schema-amendment-001-v1.0.0.md` sha256 `ae3db9e9eb39ddce0647e40def7d3a6a08a44b1aab4158b124b955c45fbad35b`

Applied Hub migration checksums, read by the reviewer from `edge_ops.migration_journal` after a reset from zero (15 files):

| File                               | sha256                                                             |
| ---------------------------------- | ------------------------------------------------------------------ |
| `0000_extensions_and_roles.sql`    | `718f2ba2aaee7c87c200214f9fe6039febda82f18bb64a1a9521d553556f9d0b` |
| `0001_types_and_helpers.sql`       | `35a942daa1bbbc6842c3c69a6207f83e8c75a089fb92ff10126b7f9c31e53692` |
| `0002_identity.sql`                | `985b135e60da135a66287ff89213f913497945918c3926045b6ac194a9b573ac` |
| `0003_configuration.sql`           | `57b6fd35a36a196a73573aae412cf35e03e26ae380cbef48a2ee694ff25fdc66` |
| `0004_core.sql`                    | `caf79302f36f4caf520617cd6a8560feefe01d7c9c448f1f30dc4d19b79c8cb1` |
| `0005_laundry.sql`                 | `fc77e50f325cae548b1eaeda6e2452113ec5362d2a655153a8149a832a055bbf` |
| `0006_payments_and_cash.sql`       | `b99b23ed4cc82e49fe2ad4476f476372b1bcbf6b7236770aae31f745785a2a17` |
| `0007_documents_and_print.sql`     | `13b4161860464c5dc8a60258b176b77cb3d6859d1751ad9b3d9d7d0f772dd670` |
| `0008_files.sql`                   | `d50244cd0abfd7d31a617f417844baf4dc6df072c0951ebffbbe5bdb14e7fc67` |
| `0009_sync.sql`                    | `3b449cf0b8b8f17d960b1b867e0106d7594a65d5b995ee51a4159ffb1369b003` |
| `0010_hardware.sql`                | `7940ff23274c65192148314eca9ae51575e917f556b7143baf7eca00cff1e764` |
| `0011_audit_and_security.sql`      | `336f6086cdf1e89f557646a21709cfdf95a650793102eb6546fbb536646820e7` |
| `0012_indexes_and_constraints.sql` | `e1782663f80032ba3f91b791d9f42cde458f2b79ed758f1f285317383687fa42` |
| `0013_views_and_procedures.sql`    | `c2b33a75ded0b163e26b12704c7373a9c9d849980b4f13e4513bb01dd4b9ca0a` |
| `0014_seed_reference_profiles.sql` | `d7f0076b962fe49702a3f6390b25fd4ead52567a5314c3be05cf3207aee9cf42` |

## 1. Independence check

| Question                                                           | Answer                                                                                                                                                                                                                                                 |
| ------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Did the reviewer author any of the six reviewed commits?           | No. This session began at `e70539c` with a clean tree and authored nothing before reviewing.                                                                                                                                                           |
| Did the reviewer re-execute the gates, or read reported results?   | Re-executed. Every number in §6 is one I observed in my own terminal.                                                                                                                                                                                  |
| Did the reviewer rely on the authors' test suites for the verdict? | No. Their suites were re-run as one input; the verdict rests on 44 probes I designed and wrote myself (§6.3–§6.5), including attacks their suites do not perform.                                                                                      |
| Did the reviewer accept the authors' assertion count?              | No. I counted with `grep -c "NOTICE:  PASS"` (**29**) and separately confirmed the naive `grep -c PASS` over-reports (**30**), reproducing hazard H2 exactly.                                                                                          |
| Files written by the reviewer                                      | Exactly one: this record. Probe scripts were written to the session scratchpad OUTSIDE the repository and are not part of the tree. `git status --short` is empty at the end of this review.                                                           |
| Probe data disposition                                             | Probes used throwaway terminal devices, sessions and Bookings under a reviewer prefix, or ran inside transactions I rolled back. The Hub DB was then reset and re-seeded, and the 29 assertions re-verified.                                           |
| Environment precondition                                           | **Not as stated.** Docker was DOWN when this review began; my first mandated run failed against the Docker API. I started Docker Desktop, waited for all 18 containers to report healthy, and re-ran from the beginning. Recorded as RV-008 for truth. |

## 2. Materials reviewed

- Task records `00_AI_HANDOFF/tasks/WS-09-T001.md` … `WS-09-T007.md` (all seven).
- `docs/data/kitluy-storehub-local-schema-reconciliation-v1.0.0.md` (WS-09-T001; §1 naming decision, §2 relation mapping, §4 conflicts R1–R4, §5 gaps G1–G8).
- `docs/data/kitluy-storehub-local-schema-amendment-001-v1.0.0.md` (§1 KLREQ-020..023, §2 sync-state mapping, §3 deviations D1–D11, §3a findings F1–F7 and KLREQ-024, §3b hazards H1/H2, §4 fences, §5 acceptance criteria).
- `docs/source/offline/kitluy-storehub-local-database-schema-v1.0.0.md` (§1 conventions, §6 relation catalogue, §7 procedures, §8 indexes) — read directly for the append-only relation list and the `edge_audit.security_event` column list.
- `docs/authority/kitluy-open-decisions-and-required-values-v1.0.0.md` (KLREQ-020..024 rows) and `kitluy-decision-and-reconciliation-register-v1.0.0.md` (KLREC-2026-07-26-002 disposition).
- `docs/authority/kitluy-implementation-status-and-evidence-register-v1.0.0.md`, `00_AI_HANDOFF/000_CURRENT_STATE.md`.
- `docs/source/security/kitluy-suite-rbac-permission-registry-v1.0.0.csv` (searched directly for plant-stage keys).
- All 15 Hub migrations, `hub/seed/dev-fixtures.sql`, `hub/tests/assertions.sql`, `scripts/hub/hub-db.mjs`, `scripts/hub/hub-validate.mjs`, and the whole of `services/kitluy-hub-agent/src/hub/**` (24 modules, 6,894 lines).
- The live `kitluy_hub_local` database: schemas, relations, columns, constraints, triggers and role grants queried directly with `psql`.

## 3. Scope and file ownership

`git diff --stat bcc7423^..e70539c` — 76 files, 21,231 insertions, 2 deletions.

| Ownership claim                                                     | Reviewer verification                                                                                                                                                                                                                                                   | Verdict |
| ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| No cloud migration modified (reconciliation acceptance criterion 4) | `git diff --name-only bcc7423^..e70539c \| grep '^supabase/'` returns nothing. Zero Supabase artifacts touched.                                                                                                                                                         | HOLDS   |
| Canonical engines untouched (engine authority)                      | No file under `verticals/` or `packages/` appears in the diff. `@kitluy-verticals/phase1-laundry` and `@kitluy/payments` are consumed, never edited.                                                                                                                    | HOLDS   |
| Hub owns a separate migration directory (gap G6)                    | `hub/migrations/**` with the canonical `NNNN_name.sql` convention; the cloud validators are untouched; `pnpm hub:db:validate` is a distinct Hub-aware validator.                                                                                                        | HOLDS   |
| Only the declared schemas created                                   | Live DB carries exactly 11 `edge_*` schemas: the ten §2 schemas plus the `edge_ops` control plane (D9). No `edge_finance`, `edge_commands`, `edge_events` or `edge_print` exists.                                                                                       | HOLDS   |
| The 2 deletions                                                     | One line in `kitluy-decision-and-reconciliation-register-v1.0.0.md` (KLREC-2026-07-26-002 status line, correctly rewritten to RESOLVED for the schema portion) and one line in `services/kitluy-hub-agent/package.json` (dependency block). Neither removes a contract. | HOLDS   |
| Neutral-Core boundary                                               | `edge_core` contains no Laundry vocabulary; the Booking lives in `edge_laundry` (recorded asymmetry R3, accepted with rationale).                                                                                                                                       | HOLDS   |
| No money floats                                                     | `hub:db:validate` `no-floating-point` check passes on all 15 files; assertion 6 confirms 23 `*_minor bigint`, 8 `currency_code char(3)`, 8 `currency_exponent smallint`, zero float/real/double.                                                                        | HOLDS   |
| Secrets                                                             | `pnpm secret:scan` — passed, 1,020 tracked files. Backups land in gitignored `hub/.backups/`.                                                                                                                                                                           | HOLDS   |

## 4. Acceptance criteria vs WS-09-T001..T007

| Task | Stated acceptance / status ceiling                                                                                                                | Reviewer verdict                                                                                                                                                                                                                                                                                                                                                                                                    |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T001 | Reconciliation record BEFORE any DDL; custody naming conflict resolved; unmatched relations listed with rationale; `docs:verify` passes           | **MET.** Commit order is correct (`bcc7423` precedes `7000958`). §1 closes the KLREC-2026-07-26-002 schema portion with a stated rationale; §2 lists both no-counterpart directions; G1–G8 registered before implementation. `docs:verify` 8/8.                                                                                                                                                                     |
| T002 | Booking/custody persistence driven exclusively by the canonical engines; custody append-only; duplicate scan no duplicate effect; T2/T3/T4 fences | **MET.** Probes P7 (engine refuses RECEIVED→READY, nothing written), P8c (duplicate Ready scan → one `scanned_count`), A1/A2/A3 (T2/T3/T4 separation, zero rows written) all held.                                                                                                                                                                                                                                  |
| T003 | Payment/finance persistence; integer minor units; `PAYMENT_PENDING` never becomes paid; duplicate confirmation → one effect; four-eyes            | **MET with a recorded architecture ruling.** No `edge_finance` schema exists (KLREQ-022 / G8) — the Hub records finance SOURCE events with a `posting_dedup_key` and `cloud_posting_status='unknown'`. P5 showed a pending payment leaves `paid_minor = 0`. P8a/P8b showed one payment, one cash movement, and `IDEMPOTENCY_KEY_REUSED_WITH_DIFFERENT_REQUEST` on a changed payload. A13/A14 held four-eyes closed. |
| T004 | Idempotency ledger, command journal, outbox, sync journal, audit, print, file cache; outbox in the SAME transaction; truthful sync state          | **MET.** P2 proved a mid-command failure leaves no event, no outbox row, no command result, no audit row; the burnt `hub_sequence` is journalled in its own transaction. P3 proved WS-09 cannot write or report any cloud acknowledgement.                                                                                                                                                                          |
| T005 | Deterministic checksum-controlled forward-only migrations; append-only enforcement; safety modes; dev-grade backup/restore                        | **MET.** Migrations run inside `begin; … commit;`; checksum drift is detected and both `status` and `apply` REFUSE; the local-only guard refuses a remote URL and a production `KITLUY_ENV`. P6 proved `HUB_READ_ONLY` blocks all six mutating operation kinds with 503 while reads still serve. P4a/P4b proved both crash directions. Backup/restore verified against my own content fingerprint.                  |
| T006 | Full matrix executed with exact counts; fail-closed authorization; KLREQ-015 fence; offline workflows                                             | **MET.** Reviewer-observed counts: hub-agent **147 passed / 2 skipped**; `hub:db:test` **29**; backup/restore **2 passed** opt-in. My independent 18-case authorization matrix reproduced every refusal AND proved zero business rows written in each case.                                                                                                                                                         |
| T007 | Independent verdict recorded; complete evidence package; promotion only on that evidence                                                          | **PARTIALLY MET.** This record supplies the verdict. The EVIDENCE PACKAGE is absent — see RV-001. Promotion cannot be executed until it exists.                                                                                                                                                                                                                                                                     |

Status ceilings are respected in the code itself: `src/hub/index.ts` states the IMPLEMENTED-IN-DEV ceiling, `lan-api.ts` still fails closed for every mutating verb, and no T1–T4 client integration is claimed anywhere.

## 5. Technical review checklist

| #   | Check                                                                     | Result                                                                                                                                                                                                           |
| --- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Migration from zero is deterministic and complete                         | PASS — `hub:db:reset` applied 15 migrations from a dropped database, journalling a sha256 for each; ten canonical schemas plus `edge_ops` created; assertion 29 tallies 53 relations, 163 indexes, 38 triggers.  |
| 2   | Fixture idempotency                                                       | PASS — second `hub:db:seed` produced `INSERT 0 0` for all 52 statements; zero rows changed. Verified twice (initial run and final restoration run).                                                              |
| 3   | Schema mapping vs the T001 record                                         | PASS — assertion 2 asserts all 51 canonical §6 relations; assertion 3 asserts the four G3/G4 additive extensions; the live schema list matches §2 exactly.                                                       |
| 4   | Engine-only transition authority (service layer)                          | PASS — see §6.4 P7. The pipeline never decides a transition; the engine throws before any write.                                                                                                                 |
| 5   | Engine-only transition authority (database layer)                         | **NOT ENFORCED** — `edge_laundry.booking.status` carries no CHECK and no transition trigger. See RV-004.                                                                                                         |
| 6   | Authorization fails closed on every dimension                             | PASS — 18/18 reviewer cases refused with the correct distinct code and zero business rows written.                                                                                                               |
| 7   | Idempotency: replay, conflict, parallel duplicate                         | PASS — P1 (genuine concurrency across two pools), P4b (response-loss replay), P8a/P8b.                                                                                                                           |
| 8   | Append-only and no-hard-delete                                            | PASS for custody, status history, payment, payment attempt, refund/void, cash, command result, local event, audit — refused even for a TRUE SUPERUSER. See RV-003 for `security_event`.                          |
| 9   | No role holds DELETE                                                      | PASS — `information_schema.role_table_grants` shows INSERT/SELECT/UPDATE only for `kitluy_hub_runtime`, SELECT for `kitluy_backup`/`kitluy_sync_worker`, no PUBLIC grants, DELETE nowhere.                       |
| 10  | Truthful sync state / no fabricated acknowledgement                       | PASS — four independent layers, all proven. See §6.4 P3.                                                                                                                                                         |
| 11  | Hub read-only mode                                                        | PASS — 503 `HUB_READ_ONLY` on all six mutating kinds; `read` executes; no synthesized result path exists (`HubSafetyModeError` is the only outcome).                                                             |
| 12  | Outbox atomicity                                                          | PASS — deferred constraint trigger plus P2.                                                                                                                                                                      |
| 13  | Restart / crash-before / crash-after recovery                             | PASS — real `pg_terminate_backend` in both directions.                                                                                                                                                           |
| 14  | Offline operation                                                         | PASS — full intake→READY→pickup workflow on LAN/DB alone; no outbound network client exists anywhere in `src/hub/**`.                                                                                            |
| 15  | Backup and restore                                                        | PASS — verified against a reviewer fingerprint stronger than the runner's. See RV-006.                                                                                                                           |
| 16  | KLREQ-015 discipline: no invented permission key, no invented owner value | PASS — 22 active commands, all 8 distinct keys canonical; 10 inactive commands, all carrying `[REQUIRED: …]`; 9 permission-less Edge routes still permission-less; 13 distinct `[REQUIRED: …]` markers retained. |
| 17  | Strict transition guard not relaxed; seed not modified                    | PASS — `hub/seed/dev-fixtures.sql:581` still contains the `intake_confirmed → ready` `status_event`; `booking-status.ts` implements the strict reading and refuses it. Verified by reading both artifacts.       |
| 18  | Lint / format / secrets / typecheck                                       | PASS — `lint` 0 errors (2 pre-existing warnings in files NOT owned by WS-09), `format:check` clean, `secret:scan` clean, `typecheck` PASS inside `verify`.                                                       |

## 6. Reviewer validation

### 6.1 Re-executed commands — ACTUAL results

All commands run with `export PATH="/c/Users/zaqws/AppData/Local/kitluy-toolchain/node-v22.23.0-win-x64:$PATH"` (node v22.23.0, pnpm 9.15.9) at `C:\dev\HET-KITLUY-PROJECT`, HEAD `e70539c`, clean tree.

| Command                                                        | Expected             | ACTUAL result observed by the reviewer                                                                                                                                               | Verdict |
| -------------------------------------------------------------- | -------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| `docker ps` (precondition)                                     | stack up             | **DOWN at review start** — Docker API unreachable. Started Docker Desktop, waited for 18/18 healthy containers, restarted the review.                                                | RV-008  |
| `pnpm hub:db:validate`                                         | pass                 | exit 0 — 15 Hub migration files, every static check PASS, "all ten §2 schemas are created by the migration set"                                                                      | PASS    |
| `pnpm hub:db:reset`                                            | pass                 | exit 0 — database dropped and recreated, **15 migrations applied from zero**, each journalled with a sha256                                                                          | PASS    |
| `pnpm hub:db:seed` (1st)                                       | pass                 | exit 0 — 52 INSERT statements, 124 rows; notice "4 local_event(s), 4 outbox row(s), 3 booking(s)"                                                                                    | PASS    |
| `pnpm hub:db:seed` (2nd)                                       | zero rows changed    | exit 0 — **every statement `INSERT 0 0`**; zero rows changed. Reproduced a second time in the final restoration run.                                                                 | PASS    |
| `pnpm hub:db:test` — `grep -c "NOTICE:  PASS"`                 | **29**               | **29**                                                                                                                                                                               | PASS    |
| `pnpm hub:db:test` — naive `grep -c PASS`                      | 30 (the wrong count) | **30** — reproduced exactly; the extra match is the runner's own line `assertions passed — 29 PASS notice(s).` Hazard H2 is accurate.                                                | PASS    |
| `pnpm hub:db:status`                                           | no drift             | exit 0 — 15 file(s); 15 applied, 0 pending, 0 checksum drift, 0 missing                                                                                                              | PASS    |
| `pnpm --filter @kitluy-services/kitluy-hub-agent test`         | 147 passed 2 skipped | **147 passed, 2 skipped** (13 files passed, 1 skipped) — matches exactly                                                                                                             | PASS    |
| `KITLUY_HUB_DESTRUCTIVE_TESTS=1 vitest run hub-backup-restore` | opt-in round trip    | exit 0 — **2 passed**; BEFORE and AFTER runner fingerprints identical (`a3de4339…f212ea`)                                                                                            | PASS    |
| Reviewer backup→wipe→restore with MY OWN fingerprint           | content preserved    | before `6a457e43…64ca` / 124 rows / 54 relations; after wipe `0d70e93f…5f3d` / 24 rows; after restore `6a457e43…64ca` / 124 rows — **MATCH**                                         | PASS    |
| `pnpm db:test`                                                 | 121                  | **121** `NOTICE:  PASS`, exit 0                                                                                                                                                      | PASS    |
| `pnpm test:rls`                                                | 95                   | **95** `NOTICE:  PASS`, exit 0                                                                                                                                                       | PASS    |
| `pnpm verify`                                                  | 11/11                | **11/11 PASS** (format, lint, typecheck, unit, contract, offline harness, build, OpenAPI, migration validation, secret scan, docs links)                                             | PASS    |
| `pnpm docs:verify`                                             | 8/8                  | **8/8 PASS**                                                                                                                                                                         | PASS    |
| `pnpm lint`                                                    | clean                | exit 0 — **0 errors, 2 warnings**; both are unused `eslint-disable` directives in `packages/payments-persistence` and `verticals/phase1-laundry-persistence`, neither owned by WS-09 | PASS    |
| `pnpm format:check`                                            | clean                | exit 0 — "All matched files use Prettier code style!"                                                                                                                                | PASS    |
| `pnpm secret:scan`                                             | clean                | exit 0 — 1,020 tracked files                                                                                                                                                         | PASS    |
| `git diff --check`                                             | clean                | exit 0, no output                                                                                                                                                                    | PASS    |
| `git status --short`                                           | clean                | empty, before and after this review                                                                                                                                                  | PASS    |

### 6.2 The 29 Hub assertions — what they actually cover

Counted and read by the reviewer, not summarised from a report. Coverage: schema inventory and forbidden-schema absence (1), 51 canonical relations (2), the four additive extensions (3), 7 enum types (4), scope columns on 43 relations (5), money representation (6), `numeric(18,4)` quantities (7), 6 append-only triggers (8) and their behaviour (9), no-hard-delete on 25 relations (10), no-DELETE role privileges (11), idempotency-key format and its 5-relation coverage (12–13), the outbox transaction invariant (14), payment immutability (15), command-result immutability (16), truthful sync state (17), storage capacity and double-occupancy (18), the Booking balance CHECK (19), 16 named indexes plus scope indexes (20), 14 routines and 4 views with support redaction (21), display-number allocation and Appendix B formatting (22), the acceptance algorithm's five outcomes (23), fixture personas including attackers (24), 23 SHA-256 columns (25), the 15-entry migration journal (26), print suppression keys (27), single-active invariants (28), and the 53/163/38 tally (29).

### 6.3 Reviewer authorization matrix — 18 cases, each proving refusal AND zero writes

Designed and written by the reviewer. Each case snapshots a 17-relation business census (`booking`, `booking_line`, `garment`, `custody_event`, `status_event`, `ready_scan_session`, `pickup_session`, `storage_assignment`, `payment`, `payment_attempt`, `refund_adjustment`, `cash_movement`, `local_event`, `outbox`, `command_result`, `audit_event`, `print_job`) before and after, and asserts the delta is ZERO on every relation.

| #    | Attack                                                   | ACTUAL refusal code                   | Business rows written | Security event  |
| ---- | -------------------------------------------------------- | ------------------------------------- | --------------------- | --------------- |
| A1   | T2 customer display attempts a staff mutation            | `EDGE_PROFILE_NOT_AUTHORIZED`         | 0                     | +1              |
| A2   | T3 attempts to complete a pickup (release authority)     | `EDGE_PROFILE_NOT_AUTHORIZED`         | 0                     | +1              |
| A3   | T4 attempts to record Ready intake                       | `EDGE_PROFILE_NOT_AUTHORIZED`         | 0                     | +1              |
| A4   | Terminal with NO cloud profile assignment                | `EDGE_PROFILE_NOT_AUTHORIZED`         | 0                     | +1              |
| A5   | Device with `lifecycle_status = 'revoked'`               | `EDGE_DEVICE_REVOKED`                 | 0                     | +1              |
| A5b  | ACTIVE device holding a REVOKED credential               | `EDGE_DEVICE_REVOKED`                 | 0                     | +1              |
| A6   | Expired actor session                                    | `EDGE_SESSION_EXPIRED`                | 0                     | +1              |
| A7   | `assignment_generation` mismatch (stale replacement Hub) | `EDGE_ASSIGNMENT_GENERATION_MISMATCH` | 0                     | +1              |
| A8   | Cross-Tenant device context                              | `EDGE_RESOURCE_SCOPE_DENIED`          | 0                     | +1              |
| A9   | Cross-Location target scope                              | `EDGE_RESOURCE_SCOPE_DENIED`          | 0                     | +1              |
| A10  | Actor whose cached projection grants no profile          | `EDGE_PERMISSION_DENIED`              | 0                     | +1              |
| A11  | Refund grant presented for a DIFFERENT Location          | `EDGE_PERMISSION_DENIED`              | 0                     | +1              |
| A12  | `environment = 'production'` (KL-INF-P1-037)             | `EDGE_ENVIRONMENT_DENIED`             | 0                     | +1              |
| A13  | Four-eyes command with NO approval evidence              | `EDGE_APPROVAL_REQUIRED`              | 0                     | +1              |
| A14  | SELF-approval (requester == approver)                    | `EDGE_SELF_APPROVAL_FORBIDDEN`        | 0                     | +1              |
| A15  | All 9 permission-less Edge routes, resolved              | `EDGE_PERMISSION_KEY_UNREGISTERED` ×9 | 0                     | n/a             |
| A15b | One INACTIVE route driven through the FULL pipeline      | `EDGE_PERMISSION_KEY_UNREGISTERED`    | 0                     | **+0** (RV-005) |
| A16  | Unknown command type                                     | `EDGE_COMMAND_UNKNOWN`                | 0                     | **+0** (RV-005) |

Every dimension carries its own distinct code — none is collapsed into another, and none can be used to bypass another (KLD-2026-07-26-002 Group 3 holds).

### 6.4 Reviewer behavioural probes — 9 probes

| Probe | What I forced                                                                                                                                                                                                                                                                                                                | ACTUAL result                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ----- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| P1    | **Genuinely PARALLEL duplicates** — the same `confirm_intake` fired from two INDEPENDENT connection pools via `Promise.allSettled`, so two real backends race                                                                                                                                                                | Outcomes `[accepted, duplicate]`; `status_event` +1 (not 2); exactly 1 `command_result`; exactly 1 `local_event` on the terminal key. **One business effect.**                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| P2    | Mid-command failure AFTER a real event + outbox row was recorded                                                                                                                                                                                                                                                             | `local_event`, `outbox`, `command_result`, `audit_event` and `status_event` deltas **all 0**. The burnt `hub_sequence` was journalled as `gap_reason='transaction_rollback'` in its own transaction, exactly as the offline contract §5 requires.                                                                                                                                                                                                                                                                                                                                                                                                                   |
| P3    | **Fabricated acknowledgement** — four separate attacks                                                                                                                                                                                                                                                                       | (a) `assertWs09DeliveryState` rejects `sending`/`acknowledged`/`retry_wait`/`blocked`/`dead_letter`, all `EDGE_COMMAND_INACTIVE`; (b) `cloud_acknowledged`/`cloud_rejected` are absent from `WS09_WRITABLE_SYNC_STATES`; (c) calling `completeCommand(…, 'cloud_acknowledged')` inside a real reservation → `EDGE_COMMAND_INACTIVE`; (d) raw SQL `update command_result set sync_state='cloud_acknowledged'` → `KLUY-EDGE-COMMAND-RESULT-IMMUTABLE`, **refused even for a true superuser**. Live DB: 0 outbox rows written by WS-09 are non-pending, 0 carry ack evidence, 0 command results are `cloud_*`, 0 cursors claim `acked > pushed`. **It is impossible.** |
| P4a   | **Crash BEFORE commit** — `pg_terminate_backend` on the command's own backend, from a second pool, after an event was recorded                                                                                                                                                                                               | Client saw `57P01`; `local_event`, `command_result` and `status_event` deltas **all 0**; `loadCommandResult` returns `undefined`. Nothing survived.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| P4b   | **Crash AFTER commit, response lost** — backend killed, fresh pool, command retried                                                                                                                                                                                                                                          | Retry returned `outcome='duplicate'` with the **SAME** aggregate id, the **SAME** `aggregate_version`, the **SAME** event ids; `status_event` count still 1; `completed_at` on the stored result unchanged. **The ORIGINAL result, not a new one.**                                                                                                                                                                                                                                                                                                                                                                                                                 |
| P5    | **Full offline workflow** (WAN irrelevant — no WAN client exists): draft → line → garment → confirm intake → pending KHQR payment → cash payment → production stages via the canonical engine → Ready session → scan → storage → QA → complete → pickup session → verify collector → scan → balance → complete → print queue | Booking reached `ready` then `picked_up`; 3 custody events; `print_job` = 1; 5 audit rows; outbox **+18, ALL `pending`**; the pending KHQR payment left `paid_minor = 0` (202 is genuinely non-terminal). `grep` over `src/hub/**` for `fetch(`/`http`/`axios`/`undici`/`WebSocket` returns **nothing** — there is no outbound network path to fail.                                                                                                                                                                                                                                                                                                                |
| P6    | **Hub read-only** — every operation kind through the safety gate, then a REAL command                                                                                                                                                                                                                                        | `mutation`, `sensitive_write`, `large_file_write`, `nonessential_capture`, `provider_dependent`, `certificate_sensitive` → all `HUB_READ_ONLY` / **503**; `read` **executed**; the real `confirmIntake` was refused with 503, `command_result` delta 0, Booking still `draft`; a read issued through the gate still returned data. No fabricated success path exists — `HubSafetyModeError` is the only outcome type.                                                                                                                                                                                                                                               |
| P7    | **Engine authority** — `completeReady` on a Booking in RECEIVED (exactly the seed's shortcut)                                                                                                                                                                                                                                | Refused by the CANONICAL `markReady`: "Illegal production transition RECEIVED → READY … (KBR-LND-003)". `local_event` and `status_event` deltas 0; Booking still `intake_confirmed`. A stale `expectedVersion` → `EDGE_AGGREGATE_VERSION_CONFLICT`, no write.                                                                                                                                                                                                                                                                                                                                                                                                       |
| P8    | Duplicate cash payment, same-key-different-payload, duplicate custody scan, posting intent                                                                                                                                                                                                                                   | Cash `[accepted, duplicate]` → 1 payment row, 1 cash movement, `paid_minor = 1000`. Same key + changed amount → `IDEMPOTENCY_KEY_REUSED_WITH_DIFFERENT_REQUEST`, `paid_minor` still 1000. Ready scan `[accepted, duplicate]` → `scanned_count = 1`. `postingDeduplicationKey` is deterministic and source-bound; the duplicate command added **no new** posting intent (2 distinct keys after the first command, still 2 after the duplicate).                                                                                                                                                                                                                      |
| P9    | Forbidden schemas in the LIVE database                                                                                                                                                                                                                                                                                       | 11 schemas present, none of `edge_finance`, `edge_commands`, `edge_events`, `edge_print`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |

### 6.5 Append-only attack surface — 32 attacks × 2 privilege levels

Real value-changing SQL (not self-assignments), each isolated in its own subtransaction so no attack observes another's effect; the whole probe rolled back. Run as the Hub service role `kitluy_hub_runtime` and as `supabase_admin`, a **TRUE SUPERUSER** (`rolsuper = t`).

| Target                             | as `kitluy_hub_runtime`                             | as SUPERUSER                                      | Refusing layer                           |
| ---------------------------------- | --------------------------------------------------- | ------------------------------------------------- | ---------------------------------------- |
| `custody_event` UPDATE / DELETE    | `KLUY-EDGE-APPEND-ONLY` / `42501 permission denied` | `KLUY-EDGE-APPEND-ONLY` / `KLUY-EDGE-APPEND-ONLY` | trigger + GRANT (two independent layers) |
| `status_event` UPDATE / DELETE     | `KLUY-EDGE-APPEND-ONLY` / `42501`                   | `KLUY-EDGE-APPEND-ONLY` / `KLUY-EDGE-APPEND-ONLY` | trigger + GRANT                          |
| `payment` amount inflation         | `KLUY-EDGE-PAYMENT-IMMUTABLE`                       | `KLUY-EDGE-PAYMENT-IMMUTABLE`                     | trigger                                  |
| `payment` DELETE (confirmed)       | `42501`                                             | `KLUY-EDGE-NO-HARD-DELETE`                        | trigger + GRANT                          |
| `payment_attempt` DELETE           | `42501`                                             | `KLUY-EDGE-NO-HARD-DELETE`                        | trigger + GRANT                          |
| `refund_adjustment` UPDATE/DELETE  | `KLUY-EDGE-APPEND-ONLY` / `42501`                   | `KLUY-EDGE-APPEND-ONLY` / `KLUY-EDGE-APPEND-ONLY` | trigger + GRANT                          |
| `cash_movement` UPDATE / DELETE    | `KLUY-EDGE-APPEND-ONLY` / `42501`                   | `KLUY-EDGE-APPEND-ONLY` / `KLUY-EDGE-APPEND-ONLY` | trigger + GRANT                          |
| `command_result` rewrite / DELETE  | `KLUY-EDGE-COMMAND-RESULT-IMMUTABLE` / `42501`      | `KLUY-EDGE-COMMAND-RESULT-IMMUTABLE` (both)       | trigger + GRANT                          |
| `local_event` payload / DELETE     | `KLUY-EDGE-APPEND-ONLY` / `42501`                   | `KLUY-EDGE-APPEND-ONLY` (both)                    | trigger + GRANT                          |
| `outbox` fake ack, no evidence     | `23514 outbox_ack_ck`                               | `23514 outbox_ack_ck`                             | CHECK constraint                         |
| `outbox` fake ack + invented id    | **SUCCEEDS**                                        | **SUCCEEDS**                                      | none at the DB layer — see RV-007 / §8   |
| `outbox` DELETE                    | `42501`                                             | `KLUY-EDGE-NO-HARD-DELETE`                        | trigger + GRANT                          |
| `audit_event` actor / details      | `KLUY-EDGE-APPEND-ONLY`                             | `KLUY-EDGE-APPEND-ONLY`                           | trigger                                  |
| `audit_event` DELETE               | `42501`                                             | `KLUY-EDGE-APPEND-ONLY`                           | trigger + GRANT                          |
| `security_event` severity/details  | **SUCCEEDS**                                        | **SUCCEEDS**                                      | none — see RV-003                        |
| `security_event` DELETE            | `42501`                                             | `KLUY-EDGE-NO-HARD-DELETE`                        | trigger + GRANT                          |
| `sync_cursor` acked > pushed       | `23514`                                             | `23514`                                           | CHECK constraint                         |
| `booking.status` forced to `ready` | **SUCCEEDS**                                        | **SUCCEEDS**                                      | none at the DB layer — see RV-004        |
| `booking` money balance break      | `23514`                                             | `23514`                                           | CHECK constraint                         |

Two independent layers protect the append-only ledgers: the row-level triggers (which refuse a superuser) and the absence of any DELETE grant (which refuses the service role earlier). Neither alone is relied upon.

### 6.6 Migration-control probes

| Probe                                                             | ACTUAL result                                                                                                                                                                                                             |
| ----------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Migration transactionality                                        | Each file is wrapped `begin; … <file> … insert into edge_ops.migration_journal … commit;` — a failure cannot leave a partially applied schema.                                                                            |
| Checksum drift (I corrupted a journal checksum, then reverted it) | `hub:db:status` reported `DRIFT 0005_laundry.sql` and `REFUSED — an applied file is never edited (schema contract §4)`; `hub:db:apply` exited 1 with "Add a NEW migration instead." Reverted; status returned to 0 drift. |
| Remote connection string                                          | `KITLUY_HUB_DB_URL=postgresql://…@db.prod.example.com…` → `REFUSED: … must point at a local development database (KL-INF-P1-037, OWNER-LOCKED)`, exit 1.                                                                  |
| Production environment                                            | `KITLUY_ENV=production` → `REFUSED: … only operates on local/development targets (KL-INF-P1-037)`, exit 1.                                                                                                                |

## 7. Findings

| ID     | Severity | Blocking                                               | Finding                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ------ | -------- | ------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| RV-001 | MEDIUM   | **No** for the merge; **YES** for the status promotion | **The WS-09 evidence package does not exist.** `docs/evidence/phase1/ws-09/` is absent (T001–T006 each require a subdirectory); the evidence register `docs/authority/kitluy-implementation-status-and-evidence-register-v1.0.0.md` contains **no WS-09 row**; no WS-09 handoff note exists in `00_AI_HANDOFF/<area>/`; `00_AI_HANDOFF/000_CURRENT_STATE.md` still records WS-09 as `SCAFFOLDED`. The executable evidence exists and I reproduced it, but T007's status gate requires the RECORDED package. Promotion to IMPLEMENTED-IN-DEV cannot be executed until it is written.                                                                                                                                           |
| RV-002 | LOW      | No                                                     | **Open items recorded in code but not promoted to the authority register.** `kitluy-open-decisions-and-required-values-v1.0.0.md` carries KLREQ-020..024 only. KLREQ-025 (no Hub permission-grant projection), KLREQ-026 (multi-event key namespace), KLREQ-027 (provider-callback ledger) and the three residual risks KLRISK-HUB-001/002/003 live only in `src/hub/open-items.ts` and amendment §3a/§3b. `open-items.ts` states KLRISK-HUB-001 "remains recorded here **and in the WS-09 evidence and residual-risk record**" — that record does not yet exist (see RV-001). Nothing is silently resolved; the records are simply not yet at authority level.                                                               |
| RV-003 | LOW      | No                                                     | **`edge_audit.security_event` is fully mutable by the Hub runtime role.** I downgraded `severity` on all 43 rows and erased `details_json` on all 43 rows as `kitluy_hub_runtime` and as a superuser; only DELETE is refused. This is CONTRACT-CONSISTENT — canonical §6.10 does not mark the relation immutable and it carries `acknowledged_by`/`acknowledged_at`/`cloud_synced_at`, so it must be updatable. But unlike `edge_payments.payment`, which freezes its money/identity/provenance columns by trigger, the evidence columns of a security event are unprotected. Recommend a column-scoped freeze permitting only the acknowledgement and `cloud_synced_at` columns.                                             |
| RV-004 | LOW      | No                                                     | **Engine transition authority is a SERVICE-layer guarantee, not a structural one.** `edge_laundry.booking.status` carries no CHECK and no transition trigger; as `kitluy_hub_runtime` I set 9 Bookings from `intake_confirmed` to `ready` with one SQL statement. This is a documented characteristic (KLREQ-024 notes the column has no CHECK and no published value list) and the access model confines DB credentials to the Hub agent (reconciliation §3: "POS terminals hold no database credentials"). The command layer itself is airtight (P7). The claim in evidence must therefore be phrased "the engines are the only transition-decision path **in the command layer**", never as a database-enforced invariant. |
| RV-005 | LOW      | No                                                     | **Two refusal codes produce no security event.** `EDGE_PERMISSION_KEY_UNREGISTERED` and `EDGE_COMMAND_UNKNOWN` are thrown by `requireActiveHubCommand` at the top of `executeHubCommand`, **outside** the try/catch that calls `recordDenialEvidence` — so an attempt against one of the nine INACTIVE routes leaves no audit trail, even though `EDGE_PERMISSION_KEY_UNREGISTERED` is listed in `EVIDENCE_CODES`. All 15 other denial dimensions produced exactly one `security_event` each in my matrix. Fail-closed behaviour is intact; only the evidence trail is missing, and probing for INACTIVE routes is exactly the reconnaissance worth recording.                                                                |
| RV-006 | LOW      | No                                                     | **The backup verification is weaker than its message implies.** `hub-db.mjs`'s `fingerprint()` hashes **row counts only**, then prints "restore verified — every edge_* relation has the row count recorded at backup time." A restore that preserved counts but corrupted content would pass. I computed an independent fingerprint (row count **plus** an order-independent md5 of every row of every `edge_*` relation, 54 relations) and it also matched exactly (`6a457e43…64ca` before and after, versus `0d70e93f…5f3d` when wiped), so the round trip is content-correct **today**. Recommend folding a content hash into the shipped fingerprint.                                                                    |
| RV-007 | INFO     | No                                                     | **The shipped dev seed contains non-pending outbox rows and an advanced ack cursor.** `hub/seed/dev-fixtures.sql` inserts one `acknowledged` outbox row (with a real `DEMO-CLOUD-ACK-0001` id, correctly labelled in the seed as such), one `retry_wait` row, and a `sync_cursor` with `last_acked_hub_sequence = 1`. These are deliberate fixtures depicting a previously-synced Hub, not runtime writes — my probes confirmed WS-09 wrote **zero** non-pending rows. But any evidence sentence of the form "no outbox row is anything but pending" is FALSE as stated and must be scoped to rows WS-09 produced.                                                                                                            |
| RV-008 | INFO     | No                                                     | **Stated environment precondition was not met.** The review brief stated "Docker up, Supabase stack up". Docker was down; my first `hub:db:reset`/`seed`/`test` sequence failed with `failed to connect to the docker API … dockerDesktopLinuxEngine`. I started Docker Desktop, waited for 18/18 healthy containers, and re-ran everything from `hub:db:validate`. All results in §6.1 are from the post-recovery runs.                                                                                                                                                                                                                                                                                                      |

**Explicitly checked and NOT found:** no weakened authorization, no weakened append-only protection, no fabricated cloud acknowledgement, no invented owner value, no fabricated permission key, no silently resolved open item, and no engine-bypassing transition path in the command layer. None of the blocking triggers named in the review brief fired.

## 8. Decision rationale

**Why APPROVED, not CHANGES-REQUESTED.** Every gate the brief mandated was re-executed by me and passed with the exact expected numbers, including the two counts most likely to be inflated: `hub:db:test` is **29** by the correct method and **30** by the naive one, precisely as hazard H2 records. The build's own claim of 29 is right, and no record anywhere in the repository claims 30 — the only occurrence of "30" in this context is the warning in `open-items.ts` explaining why the naive count is wrong.

**Why the fabrication fence holds.** The brief singled this out as the key WS-09 fence, so I attacked it from four directions rather than reading the tests. The service guard (`assertWs09DeliveryState`), the writable-state whitelist (`WS09_WRITABLE_SYNC_STATES`), the `complete_command` path inside a real reservation, and raw SQL against `command_result` all refuse. The `command_result` immutability trigger refuses a **true superuser**. The one place where a fabricated acknowledgement IS writable is `edge_sync.outbox` with an invented `cloud_ack_id` — but that is the surface WS-10 must own, the CHECK correctly forces ack id and timestamp to be co-present, and **no WS-09 code path reaches it**: `insertLocalEventWithOutbox` hard-codes `WS09_DELIVERY_STATE`, and nothing in `src/hub/**` issues an `UPDATE` against `edge_sync.outbox` at all. The fence is where it should be.

**Why the append-only protection is credible.** Two independent layers, verified separately: the row-level triggers (which stop a superuser) and the total absence of DELETE grants (which stop the service role earlier, at `42501`). I checked both because a green run on a machine where the runtime role cannot be assumed would prove only the triggers — the very evidence-quality caveat the authors themselves recorded as KLRISK-HUB-002. I ran as `kitluy_hub_runtime` explicitly, so the §3 GRANT surface WAS exercised in this review.

**Why the governance is honest.** KLREQ-024 is recorded OPEN in the authority register with the owner ruling framed as a question, and I verified both halves of the "what was explicitly NOT done" claim by inspecting the artifacts myself: `hub/seed/dev-fixtures.sql` line 581 still carries the `intake_confirmed → ready` `status_event`, and `booking-status.ts` still refuses that edge — the seed was not edited to make a test pass, and the guard was not relaxed. KLRISK-HUB-001 is recorded as RETAINED with the explicit sentence "A workaround is not a fix", and `hub-open-items.test.ts` asserts the open items and residual risks cannot be quietly dropped once a workaround exists. That is the correct discipline; RV-002 only asks that the authority register catch up with the code.

**Why KLREQ-015 discipline holds.** I re-derived it: 22 active commands declaring 8 distinct permission keys, every one canonical; 10 inactive commands, every one carrying a `[REQUIRED: …]` marker; the 9 permission-less Edge routes still permission-less; 13 distinct `[REQUIRED: …]` markers retained across `src/hub/**`, `hub/**` and `scripts/hub/**`, including the LOCATION_CODE that the command layer refuses to default (my own probes had to supply it explicitly, and a missing one produced `EDGE_REQUIRED_VALUE_MISSING`). No key was invented to make anything pass.

**On the two findings that "succeed".** RV-003 and RV-004 are places where the database does not enforce what the service layer does. Neither contradicts a contract: §6.10 does not declare `security_event` immutable and it carries acknowledgement columns; KLREQ-024 explicitly records that `booking.status` has no CHECK. Both matter for how the evidence is WORDED, not for whether WS-09 is correct. I raise them because an evidence record that says "the engines are the only transition path" without the words "in the command layer" would be a claim I could disprove in one SQL statement.

## 9. Merge conditions

| ID  | Condition                                                                                                                                                                                                                                                                                                                         | Gates the merge? | Gates IMPLEMENTED-IN-DEV?                                              |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------- |
| C1  | Write the WS-09 evidence package: `docs/evidence/phase1/ws-09/WS-09-T001..T006/`, WS-09 rows in the evidence register, a handoff note in `00_AI_HANDOFF/<area>/` with `000_INDEX.md` updated, and the WS-09 row in `000_CURRENT_STATE.md` advanced from `SCAFFOLDED` with this review linked as the independent verdict (RV-001). | No               | **YES**                                                                |
| C2  | Promote KLREQ-025, KLREQ-026, KLREQ-027 and KLRISK-HUB-001/002/003 into `docs/authority/kitluy-open-decisions-and-required-values-v1.0.0.md` (or a named residual-risk record), so the code comment in `open-items.ts` that refers to a "residual-risk record" becomes true (RV-002).                                             | No               | **YES** (KLRISK-HUB-001 must be visibly unresolved at authority level) |
| C3  | Word every evidence statement about engine authority as a **command-layer** guarantee, and every statement about outbox state as scoped to rows WS-09 wrote. Do not write "no outbox row is anything but pending" (RV-004, RV-007).                                                                                               | No               | **YES**                                                                |
| C4  | Backlog: column-scoped freeze trigger on `edge_audit.security_event` permitting only `acknowledged_by`, `acknowledged_at` and `cloud_synced_at` (RV-003).                                                                                                                                                                         | No               | No                                                                     |
| C5  | Backlog: move the `requireActiveHubCommand` call inside the pipeline's try/catch (or record the denial before rethrowing) so `EDGE_PERMISSION_KEY_UNREGISTERED` and `EDGE_COMMAND_UNKNOWN` also leave a `security_event` (RV-005).                                                                                                | No               | No                                                                     |
| C6  | Backlog: fold a content hash into `hub-db.mjs`'s backup fingerprint, or soften the "restore verified" message to say row counts only (RV-006).                                                                                                                                                                                    | No               | No                                                                     |
| C7  | Carry forward unchanged: WS-10 stays SCAFFOLDED; NOT INTEGRATION-VERIFIED, NOT PILOT-READY, NOT DEPLOYED, NOT PRODUCTION-READY; backup/restore is development-grade only (KLRISK-HUB-003); KHQR remains simulator-only; PG16 remains the production target while development runs 15.8 (G7).                                      | No               | **YES** (as labels)                                                    |

### On the plant-production-stage limitation — reviewer assessment

**The reported limitation is ACCURATELY described.** I verified each half independently:

1. `docs/source/security/kitluy-suite-rbac-permission-registry-v1.0.0.csv` contains **no** permission key for `washing`, `drying`, `pressing` or `qa_packaging` — the only "production" matches are unrelated environment keys (`onboarding.migration.execute`, `integrations.production_enable`, …).
2. `EDGE_ROUTES` (22 routes) contains **no** route for a plant production stage.
3. `HUB_COMMANDS` therefore exposes **no** command for them — the correct KLREQ-015 response: a capability with no approved key stays absent rather than inventing one.
4. Empirically, in probe P5 I could only advance a Booking through WASHING → DRYING → PRESSING → QA_PACKAGING by asking the canonical engine for each edge and writing the projection out of band — the same thing the authors' own `arrangeProductionStage` fixture does, and it is labelled as TEST ARRANGEMENT there. In probe P7, `completeReady` from RECEIVED was refused by `markReady` and wrote nothing.

**It is a correctly-fenced gap, not a blocking defect.** The alternative — minting a permission key or an Edge route for the plant stages — would violate CLAUDE.md hard rules 7 and 9 and the KLREQ-015 fence, and would be a far worse outcome than an honestly reported hole. The gap is recorded in three places (amendment §3a F7, `open-items.ts` KLREQ-024 `currentBehaviour`, and the `arrangeProductionStage` header) and the seed evidence that provoked it was deliberately preserved rather than "fixed".

**But it must not be understated.** It means a Booking created purely through Hub commands **cannot reach READY**, so no end-to-end T1→T4 Laundry lifecycle is achievable through the Hub command surface alone. WS-09's status ceiling already says exactly that ("No T1–T4 client integration is claimed"), so nothing is over-claimed. The condition is C3: any evidence sentence describing the offline workflow must state that the plant stages were driven by the engine out of band, never imply that a Hub command performed them. It should also be registered as its own numbered open decision alongside KLREQ-024, since it is a distinct question (register plant-terminal routes and keys) from the Booking-status ambiguity.

### Eligibility for IMPLEMENTED-IN-DEV

**WS-09 is ELIGIBLE for IMPLEMENTED-IN-DEV on the executable evidence, and I independently reproduced all of it.** Every gate passes, every fence holds under adversarial probing, and no blocking finding exists.

The promotion must nevertheless **not be executed today**, because WS-09-T007's own status gate requires "all required executable evidence AND independent approval", and the recorded evidence package (RV-001) does not exist: there is no `docs/evidence/phase1/ws-09/**`, no WS-09 row in the evidence register, and no handoff note. Once C1, C2, C3 and C7 are satisfied — all of them documentation work on top of software I have already verified — WS-09 may be promoted to **IMPLEMENTED-IN-DEV** with this review as the independent verdict. It may **not** be recorded as INTEGRATION-VERIFIED, PILOT-READY, DEPLOYED or PRODUCTION-READY, and WS-10 remains SCAFFOLDED.

## 10. Reviewer truth statement

I personally executed, on the local stack at `C:\dev\HET-KITLUY-PROJECT` against commit `e70539c` with a clean tree, every command in §6.1 and recorded its ACTUAL output: `hub:db:validate`, `hub:db:reset`, `hub:db:seed` (twice, then twice again in the final restoration), `hub:db:test`, `hub:db:status`, `hub:db:backup`, `hub:db:restore`, `pnpm --filter @kitluy-services/kitluy-hub-agent test` (**147 passed / 2 skipped**), the opt-in `KITLUY_HUB_DESTRUCTIVE_TESTS=1` backup/restore suite (**2 passed**), `db:test` (**121**), `test:rls` (**95**), `verify` (**11/11**), `docs:verify` (**8/8**), `lint`, `format:check`, `secret:scan`, `git diff --check` and `git status --short`. Every number in this record is one I observed, not one I was told.

I counted the Hub assertions myself with `grep -c "NOTICE:  PASS"` and got **29**. I separately ran the naive `grep -c PASS` and got **30**, confirming that the extra match is the runner's own summary line. I report **29**.

I wrote and executed my own probes: an 18-case authorization matrix in which every case snapshots a 17-relation business census before and after and asserts a zero delta; nine behavioural probes covering genuinely parallel duplicates fired from two independent connection pools, mid-command rollback, four separate fabricated-acknowledgement attacks, crash-before-commit and crash-after-commit using real `pg_terminate_backend` calls, a complete offline intake→READY→pickup→print workflow, the read-only gate applied to a real command, engine authority, and duplicate scan/payment/posting intent; a 32-attack append-only probe run at two privilege levels including a **true superuser**; a permission-key discipline probe; and four migration-control probes. All 44 held. Probe scripts were written to the session scratchpad outside the repository.

I verified the backup round trip with **my own** fingerprint — row count plus an order-independent md5 of every row of every one of the 54 `edge_*` relations — not with the runner's row-count-only fingerprint. Before `6a457e4308b6e4b94042fdc870f264ca` (124 rows), after the wipe `0d70e93f3fc07ef62153d7e9a1bb5f3d` (24 rows), after restore `6a457e4308b6e4b94042fdc870f264ca` (124 rows).

My probes created throwaway terminal devices, sessions and Bookings under a reviewer prefix; SQL attacks ran inside transactions I rolled back. I then reset and re-seeded the Hub database and re-ran the assertions, observing **29** again and a second seed changing **zero** rows. One journal checksum I corrupted to test drift detection was reverted and `hub:db:status` confirmed 0 drift. `git status --short` is empty and `git diff --check` is clean.

I did not author any reviewed commit. The only file I wrote in this repository is this record.

I found that the stated environment precondition was false — Docker was down when I started — and I say so rather than presenting the post-recovery runs as if the first attempt had worked.

I claim only what I ran. I did **NOT** verify: production behaviour; any deployed environment; Hub appliance hardware, ARM64 imaging, PostgreSQL 16 or encrypted volumes; disaster recovery beyond a development-grade `pg_dump`/`pg_restore` round trip on x86-64; live KHQR or any real payment provider; cloud transmission, acknowledgement or reconciliation (WS-10 is not built); T1–T4 client applications (none exist); or the correctness of the owner's business decisions themselves. I verified that the Store Hub local runtime does what its contracts say, refuses what they forbid, and reports nothing it does not know.
