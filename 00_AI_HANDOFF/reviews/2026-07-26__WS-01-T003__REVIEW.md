# KitLuy Independent Review Record

## 0. Review identity

| Field           | Value                                                                                                                                                                        |
| --------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Task ID         | WS-01-T003                                                                                                                                                                   |
| Task title      | Supabase local execution foundation — seeds, test harness, db-exec tooling, package scripts                                                                                  |
| Reviewer        | Independent Claude subagent (not the task author)                                                                                                                            |
| Review role     | independent review agent (seeds/tests/tooling)                                                                                                                               |
| Reviewed commit | UNCOMMITTED working tree on top of base `48a0774f473818879f6619eaffb1a8a6ac3d48d8`                                                                                           |
| Base commit     | `48a0774f473818879f6619eaffb1a8a6ac3d48d8`                                                                                                                                   |
| Branch/worktree | `main`, /Users/vongvichetpa/Documents/HET-KITLUY-PROJECT (shared working tree)                                                                                               |
| Review date     | 2026-07-26                                                                                                                                                                   |
| Decision        | **CHANGES_REQUESTED** — BLOCKING RV-301 (seed environment guard fails OPEN while claiming fail-closed); RV-302 (positive-case count below the cycle-instruction requirement) |

## 1. Independence check

- Reviewer was not the primary writer: PASS.
- Reviewer did not modify the task branch: PASS (read-only; only the three review records are written).
- Reviewed commit matches evidence commit: PASS with caveat — uncommitted working tree; re-review on change.

## 2. Materials reviewed

- `supabase/seed/dev-fixtures.sql`, `supabase/tests/assertions.sql`, `supabase/tests/rls-tests.sql`
- `scripts/database/db-exec.mjs`, `package.json` (new db:reset/apply/seed/types/test + supabase:status scripts), `scripts/testing/blocked.mjs` (rls message update), READMEs (`supabase/migrations|seed|tests|generated`)
- Contracts: `docs/source/data-contracts/kitluy-suite-supabase-seed-and-test-fixture-plan-v1.0.0.md` (classes, idempotency rules 1–7, security/RLS fixture list), `docs/source/qa/kitluy-rls-and-tenant-isolation-test-pack-v1.0.0.md` (§3 methods, §4 case ids, §5 validator rules, §6 harness pattern), `docs/security/kitluy-phase1-security-test-system-v1.0.0.md` (KLSEC id crosswalk), `docs/source/data-contracts/kitluy-suite-supabase-generated-types-policy-v1.0.0.md`, RLS spec §9. The Cycle-3 operator instruction (§10.1/§10.2/§13 lists) is NOT in the repository; counts below are checked against the review mandate (11 negative / 9 positive) and the in-file citations.

## 3. Reviewer validation (commands re-run by reviewer)

| Command/check                                                              | Result                                                                                                                        | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| -------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pnpm db:apply`                                                            | **exit 3**, single `BLOCKED-NOT-EXECUTED (BLK-002): supabase, docker; see 00_AI_HANDOFF/OPERATOR-INSTRUCTION-BLK-002.md` line | blocked-honest contract holds; never a fake pass                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `pnpm db:seed` / `pnpm db:test` / `pnpm db:types` / `pnpm supabase:status` | exit 3 each, one BLOCKED line each                                                                                            | same contract; psql correctly added to the required-tool set for seed/test                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `pnpm db:migrations:check`, `pnpm db:validate`, `pnpm db:schema:check`     | exit 0                                                                                                                        | static checks pass                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| db-exec code path audit                                                    | PASS                                                                                                                          | every branch either `requireTools` → exit 3, or propagates the child exit status (`result.status ?? 1`); no branch converts failure to success; launch errors exit 1 with message                                                                                                                                                                                                                                                                                                                                                                 |
| Local-only guard audit                                                     | PASS with notes                                                                                                               | `assertLocalTarget`: refuses `KITLUY_ENV` outside local/development (exit 1) and any `SUPABASE_DB_URL` not matching localhost/127.0.0.1; default URL is 127.0.0.1:54322; `db:reset` labels itself destructive-to-local-only; no production inference anywhere. Substring host match is spoofable (RV-306).                                                                                                                                                                                                                                        |
| Generated-types wiring                                                     | PASS                                                                                                                          | `db:types` writes `packages/kitluy-supabase-types/src/database.generated.ts` — exactly the governed owner package of the generated-types policy ("Recommended package", owner backend/platform data team); schema list `public + kitluy_core/auth/admin/audit` matches the authored groups; never-hand-edit stated in output and README                                                                                                                                                                                                           |
| Seed idempotency (static)                                                  | PASS                                                                                                                          | fixed UUIDs from one namespace + `ON CONFLICT (id                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | user_id) DO NOTHING` on every INSERT; identical re-runs are semantically no-ops (arbiter PK conflict short-circuits before secondary unique indexes are touched); three consecutive runs identical |
| Seed fictional-data sweep                                                  | PASS                                                                                                                          | all emails `@kitluy.example`; phones `+855-00-000-000x`; names marked "(fictional)"; Khmer strings synthetic; `payload_hash` is visibly non-hex synthetic; zero credentials/secrets                                                                                                                                                                                                                                                                                                                                                               |
| 13 personas/cases                                                          | PASS                                                                                                                          | (1) HET admin u01 + profile + platform role, (2) demo tenant A, (3) partner owner u02, (4) store manager u03, (5) cashier u04, (6) laundry store ds01, (7) physical location loc01, (8) online-only ds02 with no location, (9) suspended membership u05, (10) attacker tenant B + u06, (11) requester u07, (12) distinct approver u08, (13) support ticket + consent session; plus sibling loc02 and scoped staff u09/u10 for RLS-007/008; vertical registry seeded as reference data (LAUNDRY ACTIVE + 7 ROADMAP codes), never a hardcoded enum  |
| rls-tests id mapping                                                       | PASS                                                                                                                          | RLS-001..008, 011, 012, 017, 019, 021, 022 semantics match test-pack §4 rows one-to-one (SQL-method portions); KLSEC-003/018/019/020/022/024/026/036 all verified against the consolidated security test system crosswalk; harness = test-pack §6 pattern (transaction-scoped `set_config('request.jwt.claims',…)` + `SET LOCAL ROLE`, rollback per case, no destructive production probes)                                                                                                                                                       |
| Case counts (counted myself)                                               | PARTIAL                                                                                                                       | Negative: **15 labeled case blocks** (RLS-001,002,003,004,005,006,007,008,011+012,021,KLSEC-036×2,022,019,017) ≥ 11 required. Positive: **7 dedicated blocks** (P1–P7) < 9 required (RV-302); trailer line says "14 negative + 7 positive" (self-count also off by one vs 15 blocks — RV-305). Embedded positive controls exist inside RLS-007/008/022 (own-read/own-grant must succeed), raising effective positive assertions to 10, but they are not labeled as cycle positive cases.                                                          |
| assertions.sql coverage                                                    | PASS with note                                                                                                                | Covers: 5 schemas; 45 tables + partition with PKs; 10 critical FKs (incl. auth.users anchors and cross-schema order); RLS enabled+forced on every relation; 12 A/O tables with no UPDATE/DELETE/ALL policy AND the enforce_append_only trigger; membership/vertical/link/four-eyes/break-glass invariants; 44/0/0 policy inventory; execution_tokens deny-all + revoked SELECT; no PUBLIC grants; 9 helpers + 2 trigger functions; definer search_path lock (RLS-027 static). Missing vs plan group 0160: COMMENT ON metadata assertion (RV-304). |
| Honest execution labeling                                                  | PASS                                                                                                                          | seed/tests headers and READMEs all state BLOCKED (BLK-002), "No execution result is claimed until this file actually runs"; `test:rls` blocked message updated accurately                                                                                                                                                                                                                                                                                                                                                                         |

## 4. Findings

| Finding ID | Severity | Path/location                                         | Description                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Required remediation                                                                                                                                                                                                                                                                                                                                                      | Blocking?                                                                                                            |
| ---------- | -------- | ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| RV-301     | **HIGH** | `supabase/seed/dev-fixtures.sql` lines 21–29          | The environment guard defaults OPEN: `coalesce(current_setting('kitluy.environment', true), 'local')` treats an **unset** GUC as 'local' and proceeds. No migration or config in this repo ever sets `kitluy.environment`, so on ANY database — including a future production database reached by a manual `psql -f` that bypasses db-exec — the guard passes. The header claims "guard below fails closed"; that claim is false as authored. Practical risk is currently low (db-exec refuses non-local URLs; no production DB exists; BLK-002 blocks all execution), but a fail-closed claim that fails open is exactly what this repository's blocked-honest rules exist to prevent. | Invert the default: refuse when the GUC is unset unless an explicit local marker is present (e.g. require `kitluy.environment IN ('local','development','test')` to be **set**, and set it via `supabase/config.toml`/session in db-exec seed invocation), or correct the header to state the guard is advisory and the runner is the control. Fail-closed fix preferred. | **yes**                                                                                                              |
| RV-302     | HIGH     | `supabase/tests/rls-tests.sql` positive section       | Cycle instruction §10.2 (per review mandate) requires **9** positive cases; the file delivers 7 dedicated positive blocks (P1–P7). Three additional positive controls are embedded inside negative blocks (RLS-007 own-location read, RLS-008 own-store read, RLS-022 staging-grant-authorizes-staging) but are not labeled as cycle positive cases, so the required coverage is not demonstrable from the artifact. The cycle instruction document itself is not in the repository, so the reviewer could not verify the enumerated list — only the count.                                                                                                                             | Split/relabel the embedded controls into explicit positive case blocks (or add two new ones, e.g. store-scoped staff reads own store; suspended-tenant-status boundary positive) and/or commit the cycle-instruction case list so coverage is verifiable. Update the trailer count.                                                                                       | **yes**                                                                                                              |
| RV-303     | MEDIUM   | `dev-fixtures.sql` lines 34–46                        | Direct `INSERT INTO auth.users (id, email)` violates fixture-plan idempotency rule 7 ("Demo Auth users are created by a controlled script/API, not raw inserts into unsupported auth.* internals"). The file discloses this as a recorded deviation, restricts intent to local FK anchors, and inserts no credentials. Assessed acceptable for LOCAL dev only; GoTrue-visible rows may confuse auth tooling (missing aud/role/instance_id).                                                                                                                                                                                                                                             | Replace with a controlled script (supabase admin API / auth.admin) before any staging use; keep the deviation note until then.                                                                                                                                                                                                                                            | no                                                                                                                   |
| RV-304     | LOW      | `supabase/tests/assertions.sql`                       | No COMMENT ON metadata assertion, although plan group 0160 lists it and the migrations do comment every relation. Cycle §13 list could not be cross-checked verbatim (instruction not in repo).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Add a comment-coverage assertion (cheap: count relations in the 4 schemas lacking obj_description).                                                                                                                                                                                                                                                                       | no                                                                                                                   |
| RV-305     | NOTE     | `rls-tests.sql` trailer                               | "14 negative + 7 positive cases executed" — 15 negative blocks exist (KLSEC-036 spans two blocks); also the word "executed" in a file whose header says nothing has executed; the header disclaimer controls, but the trailer should say "defined".                                                                                                                                                                                                                                                                                                                                                                                                                                     | Correct the trailer string.                                                                                                                                                                                                                                                                                                                                               | no                                                                                                                   |
| RV-306     | LOW      | `scripts/database/db-exec.mjs` lines 66–75            | Local-URL check is a substring regex (`/localhost                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | 127\.0\.0\.1/`) — `postgresql://…@prod-localhost.example/db`would pass. Also`db:types`truncates`database.generated.ts` before the CLI runs; on failure an empty file remains (exit code stays honest).                                                                                                                                                                    | Parse the URL and compare hostname exactly; open the types output only after success (write via temp file + rename). | no  |
| RV-307     | LOW      | `dev-fixtures.sql` permissions/role_templates inserts | Fixture inserts real registry permission keys (`rbac.read`, `audit.read`, …) as (key, version 1) rows with fixture UUIDs. When governed group 0150 registry seeds later insert the same (permission_key, version) under governed UUIDs, `ON CONFLICT (id) DO NOTHING` will NOT absorb the collision on `permissions_key_version_key` and one of the two will fail on a shared local DB.                                                                                                                                                                                                                                                                                                 | Make fixture inserts conflict on (permission_key, version) — or consume the 0150 registry rows once they exist and drop the fixture duplicates.                                                                                                                                                                                                                           | no                                                                                                                   |
| RV-308     | NOTE     | `dev-fixtures.sql`                                    | File is not wrapped in a transaction; with ON_ERROR_STOP a mid-file failure leaves a partial (but rerun-completable) fixture set. Acceptable for disposable local DBs.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | Optionally wrap in begin/commit.                                                                                                                                                                                                                                                                                                                                          | no                                                                                                                   |

## 5. Decision rationale

The tooling is exemplary on the blocked-honest contract: every execution path was re-run by the reviewer and either performs the real local command or exits 3 with exactly one BLOCKED-NOT-EXECUTED line — never a fake pass — with local-only guards and the governed generated-types location wired correctly. Seeds are idempotent, fully fictional, and cover all 13 personas plus the scoped-staff and vertical-registry extras; the test harness follows the test-pack pattern with correct RLS/KLSEC id mapping, and its KLSEC-036 case would even catch the WS-04-T002 blocking defect on first execution. Two things force CHANGES_REQUESTED: the seed file's in-file environment guard fails open while claiming to fail closed (RV-301 — a one-line ideology violation this repo cannot ship), and the positive-case ledger falls short of the cycle-instruction count as labeled (RV-302 — trivially fixable by promoting the embedded positive controls to labeled cases). Both fixes are small and mechanical; everything else stands.

## 6. Merge conditions

- [ ] RV-301: seed guard made genuinely fail-closed (or claim corrected, fail-closed preferred);
- [ ] RV-302: 9+ labeled positive cases (or committed cycle list demonstrating coverage) and corrected trailer;
- [ ] RV-303 deviation note retained until the controlled auth-user script exists;
- [ ] re-review of the two amended files.

## 7. Reviewer truth statement

The reviewer did not treat documentation, a build, or a primary-agent summary as proof of deployment, migration application, pilot operation, or production correctness. No seed or test has been executed anywhere (BLK-002); all pass/fail statements above about seed/test behavior are static-analysis conclusions, and the only commands actually executed are the static harnesses and the blocked runners listed in §3 with their real exit codes.
