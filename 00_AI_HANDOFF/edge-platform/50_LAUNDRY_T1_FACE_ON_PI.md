# 50 — The designed Laundry T1 face on the Pi Terminal (T1-FACE-PORT-001)

**Date:** 2026-09-18 · **Area:** edge-platform / POS · **Status:** IMPLEMENTED · TESTED · RELEASED to the development Terminal · **HARDWARE VERIFIED (screen captured on the device) · CATALOG NOT DELIVERED (slice 2 open)**
**Authority:** owner instruction 2026-09-18 ("get the app from this project first, this is our designed and built laundry app") and owner choice **"T1 booking face first"** (KLD-2026-09-18-T1-FACE-PORT-001); POS feature disposition register (`00_AI_HANDOFF/migrations/pos-unification/10_POS_FEATURE_DISPOSITION_REGISTER.md`); KLD-2026-08-07-BOOKING-SEMANTICS-001; WS-12 task register (LOCKED); KLD-2026-09-17-TERMINAL-PIN-DEVICE-CREDENTIAL-001.
**Previous:** handoff 49 (§13 carries the same day's hardware run that this work followed).

## 1. What was asked, what was decided

The owner asked for the standalone `kitluy-laundry-pos-desk-app` (`repos/het-kitluy-standalone-repos/…`, branch `feat/full-app-wiring`, `8b2f107`, clean — unchanged since the disposition register's `d5d1a26` apart from docs) to be the application on the Pi Terminal. Inspection (read-only): Electron + Vite 6 + React 18.3.1 + Tailwind v4 (unused in practice), ~50 000 lines; T1 POS 17 700 lines (laundry-savor booking workspace, new-order steps, order queue/detail, handoff, shift close), storage, T2–T4; **Supabase directly** — 30 tables and 25 RPCs on the standalone schema — behind a **staff email + password** login and a per-operator PIN; `AUTH_BYPASS = true` with in-memory fixtures made it render fully offline.

Three locked facts ruled out a lift-and-shift: no email/password on a Pi (KLD-2026-09-17), never bypass the Store Hub (hard rule 6; the register REJECTED the donor's data layer), and "Not a lift-and-shift" (reconciliation §3.4) with KLDRV-CONF-001 already resolved to Service → Laundry Booking → Transaction. Put to the owner as three scopes; chosen: **T1 booking face first** — port the designed screens as the Pi's T1 face over the Store Hub ports that exist, show what is not built as not built, extend the Hub one operation at a time.

## 2. What exists now

`apps/kitluy-pos-desktop-app/src/vertical/laundry/face/` (~14 000 lines incl. 5 600 CSS, each ported screen carrying a `PROVENANCE` header naming the donor file):

| Layer             | Ported                                                                                                                                                                                                                                   | Replaced / removed                                                                                                                                                                                                                                                   |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shell             | `ScreenFrame`, `LaundrySavorShell`, `LaundryTopBar` (nav, clock, theme toggle), `ToastBar`, `RouteErrorBoundary`, theme tokens and the savor CSS, `NotoSansKhmer-Regular.ttf` bundled                                                    | exchange-rate pill (no published parity), Mode A/B chip (fixture projections), realtime notifications, staff avatar, "Back to terminals", "Log out"; the terminal block shows profile, Hub, configuration version and **Lock terminal** (PIN session close)          |
| Booking workspace | `LaundryBookingWorkspace`, `LaundryBookingPanel`, steps row, cart lines, subtotal bar, kg wheel/numpad/weight panel, garment checklist, category picker, customize modal, icons                                                          | `committedBooking` → `bookingDraft` (the Hub's draft answer); lanes re-keyed from the donor's Wash & Fold / Dry Clean / Wash & Press trio to the canonical pricing modes `wf` (per weight) / `pp` (per piece)                                                        |
| Items step        | the designed service cards and grids, **driven by the delivered catalog**                                                                                                                                                                | the donor's fixture catalog, `WF_KG_RATE_KHR = 4 000`, fixture categories and garments are **gone, not shipped**; with no catalog the step shows the truthful card "No service catalog on this terminal yet" and the cashier may continue                            |
| Customer step     | two-column rhythm, on-screen keyboard, phone numpad on the rail, row/preview chrome                                                                                                                                                      | rewritten over the Hub: search **by phone** (≥ 6 digits, debounced), create with **preferred language** (km/en), preview shows only Hub facts (masked phone, verified, sync state, origin); no tiers, spend, subscriptions, Telegram scans, recent list, name search |
| Pricing step      | shape kept                                                                                                                                                                                                                               | "Pricing is not available on this terminal yet" (WS-12-T004/T005) — a terminal never computes a Booking price                                                                                                                                                        |
| Review step       | review cards, post-commit success rhythm                                                                                                                                                                                                 | "Open Booking Draft" → WS-12-T002 draft on the Hub (customer snapshot, notes, walk-in), then save notes / cancel with a reason; lines shown but **not saved** (WS-12-T003), labelled so; a draft is a DRAFT with its sync state, never a Booking                     |
| Other views       | nav items kept                                                                                                                                                                                                                           | Dashboard, Order Queue, Handoff, Close Shift, Settings render `NotAvailable` naming the gating task (T003/T007, shift authority OWNER-DECISION)                                                                                                                      |
| Data              | `ports.ts`: `searchCustomersByPhone`, `createCustomer`, `createDraft`, `updateDraft`, `cancelDraft`, `readCatalog`, `lockTerminal` over the existing preload bridges                                                                     | react-query, `@supabase/supabase-js`, `useSession`, every fixture backend; `readCatalog` answers `not_delivered` today                                                                                                                                               |
| Terminal screen   | `src/terminal-launcher.tsx`: the donor's terminal launcher (brand, Store Hub pill, Counter/Storage pairs, medallion cards) with the PIN pad (`src/pin-screen.tsx` `PinPad`) as a centred modal                                           | the assignment is **shown, never chosen**: T1 is the one live card, T2–T4 are locked "assigned by provisioning" (user-selectable identity stays REJECTED; WS-12 §3); no staff name, no sign-out                                                                      |
| Shell integration | `App.tsx`: report → launcher; `ready`/`offline_ready` + PIN session → the face full-screen; no report → the fail-closed scaffold. Vite bakes the app version; `@face/*` alias; Pi path sets `data-kitluy-kiosk="touch"` → `cursor: none` | `src/intake-screen.tsx` retired                                                                                                                                                                                                                                      |

**Pi-specific findings folded in:** the image has Khmer OS and DejaVu only — every emoji rendered as a box, so the face draws its own SVG set (`iconForItemName`, `serviceIcon`, `ServiceItemIcon` treats an emoji glyph as absent; the test forbids pictographic glyphs in the first paint); UI fonts came from Google Fonts at runtime in the donor — the Khmer face ships in the bundle, Latin falls to the system stack (`[REQUIRED: owner-supplied Inter Tight files]`); the `@kitluy/web-ui` shell still injects a Google Fonts stylesheet that a kiosk cannot fetch (observation; that shell no longer renders on a Pi with a report).

## 3. Proof

- Tests: `test/laundry-face.test.tsx` (20 — SSR renders of the shell hand-over, the launcher while locked and through boot states, the face over test ports, Items and Review steps, kiosk mode; scans proving no fixture price, Supabase/RPC, react-query, staff login, payment stack, dev bypass, Tailwind directives or Google Fonts came over; provenance headers on every ported screen), `test/pi-terminal-boundaries.test.ts` extended to the face and launcher files. App suite **153 passed / 2 skipped**; `tsc` and `eslint` clean; secret scan clean.
- Releases, each built in a clean detached worktree at its commit (the main tree carries a parallel session's uncommitted work — see §5) and installed by the update agent on seat **Pi HEllo** (`KL-1CB3577C26A7`), no reflash: `0.1.0-face-202609181415` (`95325ad1…`, `git-78a6676`) → `0.1.0-face-202609181444` (`ff506bf4…`, `git-3f1e1e3`) → **`0.1.0-face-202609181452` (`d7eb6a69…`, `git-5809ead`, artifact `b15e1c89…`, assignment sequence 16)**.
- **On the device** (screens captured through a transient DevTools port, drop-in removed afterwards): the launcher (T1 live, T2–T4 locked, Store Hub linked · 172.16.13.203); after the owner's PIN, the face in dark theme on the Customer step with the rail numpad — the unlock worked, `pos-runtime.json` `ready · terminalUnlocked true`. The owner's "no data" on the first build was the Items step's honest empty state (no catalog delivered), not a fault.

## 4. Open, by owner value or later slice

| Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Kind                                                    |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- |
| **Slice 2 — real catalog on the Terminal.** The dev cloud holds a WS-05 Laundry catalog for the demo store (`SHIRT-WASH` per piece 2 500 KHR at the location, `WASH-KG` per kg 3 000 KHR, `dev-fixtures.sql`). Path: a `laundry_catalog` section projected by `hub-terminal-projection.mjs`, published into the signed configuration snapshot (`dev-configuration.ts` accepts one section today), delivered in `payloadJson`, parsed in the POS main after the digest check (`edge-machine.ts` — a file the parallel session is editing), exposed by IPC, `readCatalog` answers `delivered`. Then Booking lines (T003) so the cart is saved. | next slice                                              |
| Terminal identity on the launcher is `T1` by construction — the bootstrap report carries no profile code yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | follow-up (report field)                                |
| Inter Tight / Plus Jakarta Sans for the exact Latin look                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | `[REQUIRED]` owner asset                                |
| Pricing, payment, receipt, shifts, T2–T4, order queue, handoff on a Pi                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | WS-12-T003…T007                                         |
| True terminal selection on the device                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | would reverse a locked rule — owner conflict, not built |
| Defect H (mutual `After=` restart cycle) — handoff 49 §13                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | owner value                                             |

## 5. Coordination note

A parallel Claude session (TERMINAL-APPLICATION-ASSIGNMENT work: `packages/terminal-seat-contracts/`, Hub `0044`, cloud `0231`, `verticals/phase1-laundry/src/applications.ts`, edits in `apps/kitluy-pos-desktop-app/src/bootstrap/*`, `src/vertical/{contract,modules}.ts`, `package.json`, tests) was active in the same working tree. Nothing of it was touched, staged or built into a release: the three commits here stage only the face's files, and every payload was built from a detached worktree at the commit (`worktrees/kitluy-ecosystem/wt-t1-face-release`, kept for the next release; remove when the owner says so).

## 6. Git

`78a6676` feat(pos): port the designed Laundry T1 face · `3f1e1e3` feat(pos): Terminal launcher with the PIN as a centred modal; touch kiosk hides the pointer · `5809ead` fix(pos): no emoji on the Pi face. Docs commit follows.

## 7. First-boot device PIN, install progress, and the two hardware defects of the afternoon (2026-09-18, 15:30–17:30 +07:00)

**Owner decision** KLD-2026-09-18-FIRST-BOOT-PIN-001: the PIN is created at first boot on the device, registered with the Hub at pairing; the Hub stays the verifier. Built:

| Piece                                                                                                                                                                            | Where                                                                                                                       |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Sealed first-boot PIN (HKDF from the identity key → AES-256-GCM; root 0600; public posture `absent / sealed / registered` at `/var/lib/kitluy/terminal/device-pin.json`)         | `services/kitluy-device-firstboot-agent/src/device-pin.ts`                                                                  |
| Root broker verbs `pin.status`, `pin.setup` (four digits each, never echoed), `update.check` (restart the update agent so a just-paired board installs now, not in five minutes) | `src/device-config.ts`                                                                                                      |
| terminal-edge registers the sealed PIN on the Hub's first `setup_required`, then destroys the seal; never in a status file                                                       | `src/edge-session.ts`, `src/bin/terminal-edge.ts`                                                                           |
| Device Shell: `pin_setup` screen first (digits keypad, dots, create → confirm), `installing` screen with the update journal's phase, `update.check` after PAIRED                 | `apps/kitluy-device-shell` (`model/shell-state.ts`, `screens.tsx`, `App.tsx`, `electron/{main,preload,device-state-files}`) |
| POS: `registering` face while the Hub says `setup_required` and the device posture is `sealed`; the create face only for a reset                                                 | `apps/kitluy-pos-desktop-app/src/pin-screen.tsx`, `electron/pi-runtime.ts` (`pin.devicePin`)                                |

Tests: agent 30 (device-pin + broker) + terminal-edge 24 (incl. registration once, nothing sealed → no call); shell 150; POS 152/2 skipped. Packaged overlay refreshed (`device-pin` declared to the packager). **NOT yet on a board**: the flow needs a fresh card — the next Terminal image.

**Two defects found on the second board (`KL-54A3320E1201`, seat T1T2) and fixed:**

1. **Two-profile seat refused as `PROFILE_NOT_T1`** — the seat listed T2 first, the terminal paired into `profileCodes[0]` (T2), and the Hub picked one grant by row order. Fix `12232f1`: the Hub reads the grant SET at the current assignment version (T1 among them → T1; the receipt may name any granted profile; Defect G still 409); terminal-edge pairs into the lowest-numbered profile. Hub agent hot-deployed (`/var/lib/kitluy/hotfix-12232f1`, runtime drop-in).
2. **Defect H hit for real** — the update agent's install pass `restart`ed the POS while the Device Shell held the seat: cyclic transaction, POS never started, health gate failed, release rolled back and blocked. Fix `3e66672`: `start` when the POS is not running. Agent hot-deployed on the new board; a newer release (`03738b69…`, `0.1.0-face-202609181630`) installed and committed; the board reached `staff_authentication_required` with the Hub answering `setup_required`.

Also this afternoon: the development device purge on `kitluy-fresh` (only the Store Hub and the terminals remain; backup `backups/kitluy-fresh-devices-20260918-1507/`), the dev release service's **auto-assignment by business type** (`9dbe2ac`; `LAUNDRY → kitluy-terminal`, proven: `AUTO-ASSIGNED KL-54A3320E1201 … seq=17`), configuration **v5** on the Hub carrying both terminals' grants (the old terminal's v4 grants closed and re-issued on v5 — a two-terminal publish is still by hand).

**Still by hand (BLK-006):** provisioning a newly paired terminal on the Store Hub — the automation (cloud projection route + Hub poller) is the next build.

## 8. Terminal image with the first-boot PIN flow — IMAGE VERIFIED (2026-09-18, 17:32–18:05 +07:00)

Built from the clean detached worktree at `e1cb7f4` (branch `fix/t1-grant-pick`; content identical to `dev` `3715e42` — the other session's uncommitted work is not in it), profile `pi-terminal`, environment `development`, `KL-PI5-TERMINAL-DEV`, WITH packaging (a `--skip-packaging` attempt earlier the same afternoon produced an image with no Electron runtime — image-contents 113/2 — and was set aside as `build/preserved-20260918-3715e42-skip-packaging-UNUSABLE`; never build a flashable image with that flag).

| Artifact                                                    | SHA-256                                                            | Bytes         |
| ----------------------------------------------------------- | ------------------------------------------------------------------ | ------------- |
| `deploy-v2.7.0/kitluy-pos-terminal-wayland-arm64.img.zst`   | `41109b3d5307cbbba32e515e4975859e7f487fff19cc0f38525f27cfcd901865` | 998 879 217   |
| `deploy-v2.7.0/…img.sparse.zst`                             | `30d6cf43471cc11f6982daacc2d29f9be0f57d9c087b6b99e49f9ca4ba1f4bc9` | 999 499 281   |
| `deploy-v2.7.0/…-v2.7.0.tar.zst` (IDP archive)              | `ee0af91e48395628c7ff3620990abda29fcfc8fc6743b6614afe18a3f67f6d6f` | 1 501 386 592 |
| `image-…/kitluy-pos-terminal-wayland-arm64.img` (raw)       | `b20adc93be3f42d6af705407aef9e3434f881c8ffb05db481f8372228998b53f` | 8 900 333 568 |

Read-back of the FINAL image (not the build tree): manifest hashes = independent `sha256sum` for all four; every overlay entry committed at `e1cb7f4` hashed against the erofs copy — 112/112 (100 files + 12 links, 0 differ); persistent partition carries no key, PIN, receipt or release. Inside the image: `/usr/lib/kitluy/lib/firstboot-agent/device-pin.js` (seal schema `kitluy.device-pin.sealed.v1`, posture path), broker verbs `pin.setup`/`pin.status`/`update.check`, `edge-session.js` → `terminal-pin/setup`, `release-runtime.js` `is-active` (Defect H), `bin/terminal-edge.js` `orderProfilesForPairing`/`unsealDevicePin`/`markDevicePinRegistered`; Device Shell `dist/assets/index-CMAH7MiO.js` and `shell-state.js` with `pin_setup` and `installing`, `main.js` `pin.setup` + `update.check`, `preload.cjs` `setupDevicePin`; `/usr/lib/kitluy/electron/electron` 192 875 808 bytes, version 38.8.6. Suites on the built rootfs: rpi-image-gen 23/0 (1 skip — no `KITLUY_RIG_UPSTREAM` checkout), build-gates 67/0, environment-gating 20/0, systemd-runtime 245/0, image-contents **117/0/0**, `scan-image-secrets.sh` 17/0 PASS. Classification unchanged: DEVELOPMENT / UNSIGNED / NOT RELEASE-ELIGIBLE / NOT BOOT-TESTED — the boot test is the owner's flash that follows.

Before the flashed card boots: the cloud (`kitluy-fresh`) and the Hub still hold `KL-54A3320E1201` approved + paired, so an as-is boot would take the reflash-recovery path and skip admin approval. To rehearse the full chain the record is removed on the owner's "go" (purge script in plan → execute, then the Hub's terminal row), then: create PIN → registers → approve (Admin Portal) → pair (Partner Portal) → "Installing KitLuy" → terminal launcher → same PIN → the counter. Hub provisioning after pairing is still the manual stand-in (§7).
