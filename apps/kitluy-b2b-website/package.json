{
  "name": "@kitluy-apps/kitluy-b2b-website",
  "version": "0.1.0",
  "private": true,
  "description": "B2B Website — Public business acquisition, registration, authentication and portal-entry website. NOT an operational portal; the Admin Portal is never reachable through public routing (b2b spec v1.0.0).",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf .next"
  },
  "dependencies": {
    "@kitluy/feature-flags": "workspace:*",
    "@kitluy/localization": "workspace:*",
    "@kitluy/web-ui": "workspace:*",
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "typescript": "catalog:"
  }
}
