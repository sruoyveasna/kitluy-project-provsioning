import type { CSSProperties } from "react";

/** Scroll/shell inset so − / qty pills stay inside overflow clips. */
export const SERVICE_MENU_GRID_INSET: CSSProperties = {
  padding: "12px 8px 8px",
  boxSizing: "border-box",
};
