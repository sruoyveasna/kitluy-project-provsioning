# Edge Functions (Deno TypeScript)

Approved short-lived Edge Functions only (RB v4 §7.1). None exist at
bootstrap. Each function requires a contract entry in docs/api/ before
implementation. Service-role usage is server-side only.
