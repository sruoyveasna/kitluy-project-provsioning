{
  "name": "@kitluy-services/kitluy-notification-service",
  "version": "0.1.0",
  "private": true,
  "description": "Notification Service — channels, consent and delivery truth; providers are [REQUIRED] owner decisions",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kitluy/api-errors": "workspace:*",
    "@kitluy/observability": "workspace:*",
    "@kitluy/shared-config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:"
  }
}
