{
  "name": "@kitluy-services/kitluy-sync-service",
  "version": "0.1.0",
  "private": true,
  "description": "Cloud side of the edge sync protocol — dedupe, apply, acknowledge outbox events; per-data-class conflict policy, no generic last-write-wins (Hub spec §11)",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kitluy/api-errors": "workspace:*",
    "@kitluy/observability": "workspace:*",
    "@kitluy/shared-config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:"
  }
}
