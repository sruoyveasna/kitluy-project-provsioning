{
  "name": "@kitluy-services/kitluy-management-api",
  "version": "0.1.0",
  "private": true,
  "description": "Governed Management API \u2014 Admin, Chain and Partner back-office contracts and approved private integrations (RB v4 \u00a710.1)",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "test:contract": "vitest run test/contract.test.ts"
  },
  "dependencies": {
    "@kitluy/api-errors": "workspace:*",
    "@kitluy/observability": "workspace:*",
    "@kitluy/shared-config": "workspace:*",
    "pg": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "yaml": "^2.6.1"
  }
}
