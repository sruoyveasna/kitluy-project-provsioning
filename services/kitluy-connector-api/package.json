{
  "name": "@kitluy-services/kitluy-connector-api",
  "version": "0.1.0",
  "private": true,
  "description": "Governed Connector API — marketplaces, delivery, social and certified external systems; connectors never receive production-database credentials (RB v4 §10.1, §3.5)",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist",
    "test:contract": "vitest run test/contract.test.ts"
  },
  "dependencies": {
    "@kitluy/api-errors": "workspace:*",
    "@kitluy/observability": "workspace:*",
    "@kitluy/shared-config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:",
    "yaml": "^2.6.1"
  }
}
