{
  "name": "@kitluy-services/kitluy-file-service",
  "version": "0.1.0",
  "private": true,
  "description": "File Service — DigitalOcean Spaces bytes, Supabase metadata/ownership/audit; clients never receive long-lived object-storage credentials (RB v4 §7.4)",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@kitluy/api-errors": "workspace:*",
    "@kitluy/observability": "workspace:*",
    "@kitluy/shared-config": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "@types/node": "catalog:",
    "vitest": "catalog:"
  }
}
