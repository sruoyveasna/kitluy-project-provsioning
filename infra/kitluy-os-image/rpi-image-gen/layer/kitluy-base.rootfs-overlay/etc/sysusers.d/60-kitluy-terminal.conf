# The graphical bootstrap session runs as this unprivileged system user, not as
# root and not via a `%i` specifier that silently expands to nothing.
u kitluy-terminal - "KitLuy terminal kiosk runtime" /var/lib/kitluy/terminal
