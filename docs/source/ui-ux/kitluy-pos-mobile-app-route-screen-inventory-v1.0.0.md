# KitLuy POS Mobile App Route and Screen Inventory

| Field          | Value                                                        |
| -------------- | ------------------------------------------------------------ |
| Filename       | `kitluy-pos-mobile-app-route-screen-inventory-v1.0.0.md`     |
| Version        | `v1.0.0`                                                     |
| Date           | 2026-07-26                                                   |
| Owner          | HET / KitLuy Suite Project Owner                             |
| Status         | Canonical target route contract; not implementation evidence |
| Primary market | Cambodia                                                     |
| Languages      | Khmer (`km-KH`) and English (`en-KH`)                        |
| Currencies     | KHR and USD                                                  |
| Timezone       | `Asia/Phnom_Penh`                                            |

> **Evidence rule:** This artifact defines a build contract. It is not evidence that Figma files, components, routes, code, migrations, tests or deployments exist. Every missing owner, brand, provider or production value is marked `[REQUIRED: ...]`.

## Authority and scope

Authority order:

1. Current owner decisions and KitLuy Project Instructions.
2. Applied migrations, verified code/tests and production evidence.
3. Current approved KitLuy Rebuild, Business and product specifications.
4. This UI/UX build-pack artifact after approval.
5. Approved handoffs and evidence-based analyses.
6. Competitor rebuild documents as design references only.

The shared design system is neutral. Laundry terminology belongs only in Laundry-specific compositions and must not be hardcoded into Core components.

## Route authority

Routes preserve the current product specification. Where this inventory introduces a Screen ID, analytics namespace or Figma reference, that identifier is a UI traceability addition and does not change product behavior.

## Figma file registry

| Field                 | Value                                                                    |
| --------------------- | ------------------------------------------------------------------------ |
| Product Figma URL     | `[REQUIRED: KitLuy POS Mobile App Route and Screen Inventory Figma URL]` |
| Figma file key        | `[REQUIRED: file_key]`                                                   |
| Design-system library | `[REQUIRED: KitLuy Design System Figma URL]`                             |
| Screen page           | `[REQUIRED: page node-id]`                                               |
| Review status         | Not supplied; do not claim Designed/Approved                             |

## Mapping conventions

- `KLUI-POSM-NNN` is a stable UI-pack traceability ID, not a replacement for `KLMF-*` or product feature IDs.
- Permission names are contract requirements; exact registry keys must be reconciled with the canonical RBAC permission registry.
- Data-contract names are families until exact OpenAPI/JSON Schema operation IDs are published.
- Every route implements the canonical state model and must not present stale, partial, cached or unavailable data as authoritative.
- Analytics events never include raw PII, free text, payment tokens or secrets.

## Route and screen inventory

| Route                           | Screen ID     | Screen                             | Feature IDs     | Permission                     | Data contracts                                                                                                    | Mutations                                                            | Components                                                                                                 | States                                                                                                                                                     | Localization keys                                                                   | Analytics events                                                                                                 | QA cases                                                | Figma reference                        |
| ------------------------------- | ------------- | ---------------------------------- | --------------- | ------------------------------ | ----------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------- | -------------------------------------- |
| `/launch`                       | `KPM-SCR-001` | Launch                             | `KLUI-POSM-001` | `device.activate`              | Edge Operations / Store Hub read contract `pos_mobile.launch`; `[REQUIRED: schema/version]`                       | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.launch.*`; shared `state.*`, `action.*`, `a11y.*`                       | `ui.screen_viewed` (`screen=pos_mobile.launch`); `ui.primary_action` when used; no raw PII                       | `QA-POSM-001-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-001` / `[REQUIRED: node-id]` |
| `/settings/language`            | `KPM-SCR-002` | Settings / Language                | `KLUI-POSM-002` | `device.settings`              | Edge Operations / Store Hub read contract `pos_mobile.settings.language`; `[REQUIRED: schema/version]`            | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.settings.language.*`; shared `state.*`, `action.*`, `a11y.*`            | `ui.screen_viewed` (`screen=pos_mobile.settings_language`); `ui.primary_action` when used; no raw PII            | `QA-POSM-002-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-002` / `[REQUIRED: node-id]` |
| `/auth/sign-in`                 | `KPM-SCR-003` | Auth / Sign In                     | `KLUI-POSM-003` | `employee.session.start`       | Edge Operations / Store Hub read contract `pos_mobile.auth.sign-in`; `[REQUIRED: schema/version]`                 | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.auth.sign-in.*`; shared `state.*`, `action.*`, `a11y.*`                 | `ui.screen_viewed` (`screen=pos_mobile.auth_sign-in`); `ui.primary_action` when used; no raw PII                 | `QA-POSM-003-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-003` / `[REQUIRED: node-id]` |
| `/provisioning/code`            | `KPM-SCR-004` | Provisioning / Code                | `KLUI-POSM-004` | `device.provision`             | Edge Operations / Store Hub read contract `pos_mobile.provisioning.code`; `[REQUIRED: schema/version]`            | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.provisioning.code.*`; shared `state.*`, `action.*`, `a11y.*`            | `ui.screen_viewed` (`screen=pos_mobile.provisioning_code`); `ui.primary_action` when used; no raw PII            | `QA-POSM-004-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-004` / `[REQUIRED: node-id]` |
| `/provisioning/context`         | `KPM-SCR-005` | Provisioning / Context             | `KLUI-POSM-005` | `device.provision`             | Edge Operations / Store Hub read contract `pos_mobile.provisioning.context`; `[REQUIRED: schema/version]`         | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.provisioning.context.*`; shared `state.*`, `action.*`, `a11y.*`         | `ui.screen_viewed` (`screen=pos_mobile.provisioning_context`); `ui.primary_action` when used; no raw PII         | `QA-POSM-005-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-005` / `[REQUIRED: node-id]` |
| `/hub/discovery`                | `KPM-SCR-006` | Hub / Discovery                    | `KLUI-POSM-006` | `device.hub.connect`           | Edge Operations / Store Hub read contract `pos_mobile.hub.discovery`; `[REQUIRED: schema/version]`                | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.hub.discovery.*`; shared `state.*`, `action.*`, `a11y.*`                | `ui.screen_viewed` (`screen=pos_mobile.hub_discovery`); `ui.primary_action` when used; no raw PII                | `QA-POSM-006-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-006` / `[REQUIRED: node-id]` |
| `/device/tests`                 | `KPM-SCR-007` | Device / Tests                     | `KLUI-POSM-007` | `device.diagnostics`           | Edge Operations / Store Hub read contract `pos_mobile.device.tests`; `[REQUIRED: schema/version]`                 | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.device.tests.*`; shared `state.*`, `action.*`, `a11y.*`                 | `ui.screen_viewed` (`screen=pos_mobile.device_tests`); `ui.primary_action` when used; no raw PII                 | `QA-POSM-007-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-007` / `[REQUIRED: node-id]` |
| `/provisioning/complete`        | `KPM-SCR-008` | Provisioning / Complete            | `KLUI-POSM-008` | `device.provision`             | Edge Operations / Store Hub read contract `pos_mobile.provisioning.complete`; `[REQUIRED: schema/version]`        | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.provisioning.complete.*`; shared `state.*`, `action.*`, `a11y.*`        | `ui.screen_viewed` (`screen=pos_mobile.provisioning_complete`); `ui.primary_action` when used; no raw PII        | `QA-POSM-008-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-008` / `[REQUIRED: node-id]` |
| `/auth/pin`                     | `KPM-SCR-009` | Auth / Pin                         | `KLUI-POSM-009` | `employee.session.start`       | Edge Operations / Store Hub read contract `pos_mobile.auth.pin`; `[REQUIRED: schema/version]`                     | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.auth.pin.*`; shared `state.*`, `action.*`, `a11y.*`                     | `ui.screen_viewed` (`screen=pos_mobile.auth_pin`); `ui.primary_action` when used; no raw PII                     | `QA-POSM-009-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-009` / `[REQUIRED: node-id]` |
| `/intake`                       | `KPM-SCR-010` | Intake                             | `KLUI-POSM-010` | `laundry.mobile.intake`        | Edge Operations / Store Hub read contract `pos_mobile.intake`; `[REQUIRED: schema/version]`                       | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.intake.*`; shared `state.*`, `action.*`, `a11y.*`                       | `ui.screen_viewed` (`screen=pos_mobile.intake`); `ui.primary_action` when used; no raw PII                       | `QA-POSM-010-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-010` / `[REQUIRED: node-id]` |
| `/scan/camera`                  | `KPM-SCR-011` | Scan / Camera                      | `KLUI-POSM-011` | `laundry.mobile.scan`          | Edge Operations / Store Hub read contract `pos_mobile.scan.camera`; `[REQUIRED: schema/version]`                  | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.scan.camera.*`; shared `state.*`, `action.*`, `a11y.*`                  | `ui.screen_viewed` (`screen=pos_mobile.scan_camera`); `ui.primary_action` when used; no raw PII                  | `QA-POSM-011-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-011` / `[REQUIRED: node-id]` |
| `/scan/result`                  | `KPM-SCR-012` | Scan / Result                      | `KLUI-POSM-012` | `laundry.mobile.scan`          | Edge Operations / Store Hub read contract `pos_mobile.scan.result`; `[REQUIRED: schema/version]`                  | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.scan.result.*`; shared `state.*`, `action.*`, `a11y.*`                  | `ui.screen_viewed` (`screen=pos_mobile.scan_result`); `ui.primary_action` when used; no raw PII                  | `QA-POSM-012-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-012` / `[REQUIRED: node-id]` |
| `/bookings/search`              | `KPM-SCR-013` | Bookings / Search                  | `KLUI-POSM-013` | `laundry.mobile.bookings`      | Edge Operations / Store Hub read contract `pos_mobile.bookings.search`; `[REQUIRED: schema/version]`              | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.bookings.search.*`; shared `state.*`, `action.*`, `a11y.*`              | `ui.screen_viewed` (`screen=pos_mobile.bookings_search`); `ui.primary_action` when used; no raw PII              | `QA-POSM-013-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-013` / `[REQUIRED: node-id]` |
| `/bookings`                     | `KPM-SCR-014` | Bookings                           | `KLUI-POSM-014` | `laundry.mobile.bookings`      | Edge Operations / Store Hub read contract `pos_mobile.bookings`; `[REQUIRED: schema/version]`                     | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.bookings.*`; shared `state.*`, `action.*`, `a11y.*`                     | `ui.screen_viewed` (`screen=pos_mobile.bookings`); `ui.primary_action` when used; no raw PII                     | `QA-POSM-014-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-014` / `[REQUIRED: node-id]` |
| `/bookings/:bookingId`          | `KPM-SCR-015` | Bookings / Booking detail          | `KLUI-POSM-015` | `laundry.mobile.bookings`      | Edge Operations / Store Hub read contract `pos_mobile.bookings.detail`; `[REQUIRED: schema/version]`              | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.bookings.detail.*`; shared `state.*`, `action.*`, `a11y.*`              | `ui.screen_viewed` (`screen=pos_mobile.bookings_detail`); `ui.primary_action` when used; no raw PII              | `QA-POSM-015-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-015` / `[REQUIRED: node-id]` |
| `/intake/customer`              | `KPM-SCR-016` | Intake / Customer                  | `KLUI-POSM-016` | `laundry.mobile.intake`        | Edge Operations / Store Hub read contract `pos_mobile.intake.customer`; `[REQUIRED: schema/version]`              | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.intake.customer.*`; shared `state.*`, `action.*`, `a11y.*`              | `ui.screen_viewed` (`screen=pos_mobile.intake_customer`); `ui.primary_action` when used; no raw PII              | `QA-POSM-016-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-016` / `[REQUIRED: node-id]` |
| `/intake/services-garments`     | `KPM-SCR-017` | Intake / Services Garments         | `KLUI-POSM-017` | `laundry.mobile.intake`        | Edge Operations / Store Hub read contract `pos_mobile.intake.services-garments`; `[REQUIRED: schema/version]`     | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.intake.services-garments.*`; shared `state.*`, `action.*`, `a11y.*`     | `ui.screen_viewed` (`screen=pos_mobile.intake_services-garments`); `ui.primary_action` when used; no raw PII     | `QA-POSM-017-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-017` / `[REQUIRED: node-id]` |
| `/intake/evidence`              | `KPM-SCR-018` | Intake / Evidence                  | `KLUI-POSM-018` | `laundry.mobile.intake`        | Edge Operations / Store Hub read contract `pos_mobile.intake.evidence`; `[REQUIRED: schema/version]`              | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.intake.evidence.*`; shared `state.*`, `action.*`, `a11y.*`              | `ui.screen_viewed` (`screen=pos_mobile.intake_evidence`); `ui.primary_action` when used; no raw PII              | `QA-POSM-018-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-018` / `[REQUIRED: node-id]` |
| `/intake/review`                | `KPM-SCR-019` | Intake / Review                    | `KLUI-POSM-019` | `laundry.mobile.intake`        | Edge Operations / Store Hub read contract `pos_mobile.intake.review`; `[REQUIRED: schema/version]`                | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.intake.review.*`; shared `state.*`, `action.*`, `a11y.*`                | `ui.screen_viewed` (`screen=pos_mobile.intake_review`); `ui.primary_action` when used; no raw PII                | `QA-POSM-019-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-019` / `[REQUIRED: node-id]` |
| `/intake/t1-handoff`            | `KPM-SCR-020` | Intake / T1 Handoff                | `KLUI-POSM-020` | `laundry.mobile.intake`        | Edge Operations / Store Hub read contract `pos_mobile.intake.t1-handoff`; `[REQUIRED: schema/version]`            | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.intake.t1-handoff.*`; shared `state.*`, `action.*`, `a11y.*`            | `ui.screen_viewed` (`screen=pos_mobile.intake_t1-handoff`); `ui.primary_action` when used; no raw PII            | `QA-POSM-020-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-020` / `[REQUIRED: node-id]` |
| `/production/status`            | `KPM-SCR-021` | Production / Status                | `KLUI-POSM-021` | `laundry.mobile.production`    | Edge Operations / Store Hub read contract `pos_mobile.production.status`; `[REQUIRED: schema/version]`            | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.production.status.*`; shared `state.*`, `action.*`, `a11y.*`            | `ui.screen_viewed` (`screen=pos_mobile.production_status`); `ui.primary_action` when used; no raw PII            | `QA-POSM-021-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-021` / `[REQUIRED: node-id]` |
| `/issues/report`                | `KPM-SCR-022` | Issues / Report                    | `KLUI-POSM-022` | `laundry.mobile.issues`        | Edge Operations / Store Hub read contract `pos_mobile.issues.report`; `[REQUIRED: schema/version]`                | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.issues.report.*`; shared `state.*`, `action.*`, `a11y.*`                | `ui.screen_viewed` (`screen=pos_mobile.issues_report`); `ui.primary_action` when used; no raw PII                | `QA-POSM-022-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-022` / `[REQUIRED: node-id]` |
| `/issues/evidence`              | `KPM-SCR-023` | Issues / Evidence                  | `KLUI-POSM-023` | `laundry.mobile.issues`        | Edge Operations / Store Hub read contract `pos_mobile.issues.evidence`; `[REQUIRED: schema/version]`              | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.issues.evidence.*`; shared `state.*`, `action.*`, `a11y.*`              | `ui.screen_viewed` (`screen=pos_mobile.issues_evidence`); `ui.primary_action` when used; no raw PII              | `QA-POSM-023-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-023` / `[REQUIRED: node-id]` |
| `/pickup/search`                | `KPM-SCR-024` | Pickup / Search                    | `KLUI-POSM-024` | `laundry.mobile.pickup_helper` | Edge Operations / Store Hub read contract `pos_mobile.pickup.search`; `[REQUIRED: schema/version]`                | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.pickup.search.*`; shared `state.*`, `action.*`, `a11y.*`                | `ui.screen_viewed` (`screen=pos_mobile.pickup_search`); `ui.primary_action` when used; no raw PII                | `QA-POSM-024-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-024` / `[REQUIRED: node-id]` |
| `/pickup/prepare`               | `KPM-SCR-025` | Pickup / Prepare                   | `KLUI-POSM-025` | `laundry.mobile.pickup_helper` | Edge Operations / Store Hub read contract `pos_mobile.pickup.prepare`; `[REQUIRED: schema/version]`               | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.pickup.prepare.*`; shared `state.*`, `action.*`, `a11y.*`               | `ui.screen_viewed` (`screen=pos_mobile.pickup_prepare`); `ui.primary_action` when used; no raw PII               | `QA-POSM-025-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-025` / `[REQUIRED: node-id]` |
| `/pickup/customer-verification` | `KPM-SCR-026` | Pickup / Customer Verification     | `KLUI-POSM-026` | `laundry.mobile.pickup_helper` | Edge Operations / Store Hub read contract `pos_mobile.pickup.customer-verification`; `[REQUIRED: schema/version]` | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.pickup.customer-verification.*`; shared `state.*`, `action.*`, `a11y.*` | `ui.screen_viewed` (`screen=pos_mobile.pickup_customer-verification`); `ui.primary_action` when used; no raw PII | `QA-POSM-026-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-026` / `[REQUIRED: node-id]` |
| `/pickup/retrieval`             | `KPM-SCR-027` | Pickup / Retrieval                 | `KLUI-POSM-027` | `laundry.mobile.pickup_helper` | Edge Operations / Store Hub read contract `pos_mobile.pickup.retrieval`; `[REQUIRED: schema/version]`             | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.pickup.retrieval.*`; shared `state.*`, `action.*`, `a11y.*`             | `ui.screen_viewed` (`screen=pos_mobile.pickup_retrieval`); `ui.primary_action` when used; no raw PII             | `QA-POSM-027-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-027` / `[REQUIRED: node-id]` |
| `/pickup/t4-handoff`            | `KPM-SCR-028` | Pickup / T4 Handoff                | `KLUI-POSM-028` | `laundry.mobile.pickup_helper` | Edge Operations / Store Hub read contract `pos_mobile.pickup.t4-handoff`; `[REQUIRED: schema/version]`            | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.pickup.t4-handoff.*`; shared `state.*`, `action.*`, `a11y.*`            | `ui.screen_viewed` (`screen=pos_mobile.pickup_t4-handoff`); `ui.primary_action` when used; no raw PII            | `QA-POSM-028-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-028` / `[REQUIRED: node-id]` |
| `/shift/status`                 | `KPM-SCR-029` | Shift / Status                     | `KLUI-POSM-029` | `laundry.mobile.status`        | Edge Operations / Store Hub read contract `pos_mobile.shift.status`; `[REQUIRED: schema/version]`                 | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.shift.status.*`; shared `state.*`, `action.*`, `a11y.*`                 | `ui.screen_viewed` (`screen=pos_mobile.shift_status`); `ui.primary_action` when used; no raw PII                 | `QA-POSM-029-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-029` / `[REQUIRED: node-id]` |
| `/pending`                      | `KPM-SCR-030` | Pending                            | `KLUI-POSM-030` | `laundry.mobile.pending`       | Edge Operations / Store Hub read contract `pos_mobile.pending`; `[REQUIRED: schema/version]`                      | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.pending.*`; shared `state.*`, `action.*`, `a11y.*`                      | `ui.screen_viewed` (`screen=pos_mobile.pending`); `ui.primary_action` when used; no raw PII                      | `QA-POSM-030-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-030` / `[REQUIRED: node-id]` |
| `/pending/conflict/:id`         | `KPM-SCR-031` | Pending / Conflict / Record detail | `KLUI-POSM-031` | `laundry.mobile.pending`       | Edge Operations / Store Hub read contract `pos_mobile.pending.conflict.detail`; `[REQUIRED: schema/version]`      | Store Hub LAN mutation or local draft; explicit handoff limits apply | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.pending.conflict.detail.*`; shared `state.*`, `action.*`, `a11y.*`      | `ui.screen_viewed` (`screen=pos_mobile.pending_conflict_detail`); `ui.primary_action` when used; no raw PII      | `QA-POSM-031-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-031` / `[REQUIRED: node-id]` |
| `/device/diagnostics`           | `KPM-SCR-032` | Device / Diagnostics               | `KLUI-POSM-032` | `device.diagnostics`           | Edge Operations / Store Hub read contract `pos_mobile.device.diagnostics`; `[REQUIRED: schema/version]`           | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.device.diagnostics.*`; shared `state.*`, `action.*`, `a11y.*`           | `ui.screen_viewed` (`screen=pos_mobile.device_diagnostics`); `ui.primary_action` when used; no raw PII           | `QA-POSM-032-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-032` / `[REQUIRED: node-id]` |
| `/device/update-required`       | `KPM-SCR-033` | Device / Update Required           | `KLUI-POSM-033` | `device.diagnostics`           | Edge Operations / Store Hub read contract `pos_mobile.device.update-required`; `[REQUIRED: schema/version]`       | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.device.update-required.*`; shared `state.*`, `action.*`, `a11y.*`       | `ui.screen_viewed` (`screen=pos_mobile.device_update-required`); `ui.primary_action` when used; no raw PII       | `QA-POSM-033-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-033` / `[REQUIRED: node-id]` |
| `/device/revoked`               | `KPM-SCR-034` | Device / Revoked                   | `KLUI-POSM-034` | `device.diagnostics`           | Edge Operations / Store Hub read contract `pos_mobile.device.revoked`; `[REQUIRED: schema/version]`               | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.device.revoked.*`; shared `state.*`, `action.*`, `a11y.*`               | `ui.screen_viewed` (`screen=pos_mobile.device_revoked`); `ui.primary_action` when used; no raw PII               | `QA-POSM-034-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-034` / `[REQUIRED: node-id]` |
| `/support`                      | `KPM-SCR-035` | Support                            | `KLUI-POSM-035` | `laundry.mobile.support`       | Edge Operations / Store Hub read contract `pos_mobile.support`; `[REQUIRED: schema/version]`                      | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.support.*`; shared `state.*`, `action.*`, `a11y.*`                      | `ui.screen_viewed` (`screen=pos_mobile.support`); `ui.primary_action` when used; no raw PII                      | `QA-POSM-035-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-035` / `[REQUIRED: node-id]` |
| `/about`                        | `KPM-SCR-036` | About                              | `KLUI-POSM-036` | `device.about`                 | Edge Operations / Store Hub read contract `pos_mobile.about`; `[REQUIRED: schema/version]`                        | Local/session/device command only                                    | MobileAppShell; StoreContextHeader; HubStatusBanner; BottomNavigation; ProtectedActionSheet; StateBoundary | loading, empty, error, stale, partial, offline, unauthorized, unavailable, hub-unreachable, wan-unavailable, pending-sync, conflict, revoked, incompatible | `pos_mobile.about.*`; shared `state.*`, `action.*`, `a11y.*`                        | `ui.screen_viewed` (`screen=pos_mobile.about`); `ui.primary_action` when used; no raw PII                        | `QA-POSM-036-AUTH`, `-STATE`, `-A11Y`, `-L10N`, `-RESP` | `FIG-POSM-036` / `[REQUIRED: node-id]` |

## Required common QA bundles

| Suffix   | Minimum verification                                                                                        |
| -------- | ----------------------------------------------------------------------------------------------------------- |
| `-AUTH`  | Authentication, permission, resource scope, context mismatch, deep-link and negative-access tests           |
| `-STATE` | Loading, valid empty, error, stale, partial, offline, unavailable and route-specific conflict/pending tests |
| `-A11Y`  | Keyboard/touch, focus, labels, announcements, contrast, text size and reduced motion                        |
| `-L10N`  | Khmer/English completeness, text expansion, terminology, formatting and missing-key behavior                |
| `-RESP`  | Supported breakpoints or certified device resolutions, orientation and safe-area behavior                   |

## Completion evidence

A route can be marked `IMPLEMENTED` only with repository path, component exports, contract tests, permission/RLS tests, visual/accessibility tests, Figma/code reconciliation, deployment evidence and the applicable phase/pilot gate.
