# KitLuy Implementation Status and Evidence Register

**Filename:** `kitluy-implementation-status-and-evidence-register-v1.0.0.md`  
**Version:** v1.0.0  
**Date:** 2026-07-26  
**Owner:** HET / KitLuy Suite Project Owner  
**Status:** CANONICAL IMPLEMENTATION-EVIDENCE CONTROL

## 1. Governing rule

The Master Feature Registry is a planning and normalization baseline, not implementation evidence. A bible, specification, backlog, mockup, source classification, generated code sample or agent statement is not sufficient to claim implementation.

Every status **above `SPECIFIED`** must link to verifiable evidence.

## 2. Status model

| Status               | Meaning                                                            | Minimum evidence                                                                | Evidence link required? |
| -------------------- | ------------------------------------------------------------------ | ------------------------------------------------------------------------------- | ----------------------- |
| PROPOSED             | Idea or recommendation; not approved.                              | Proposal source and owner review target.                                        | No                      |
| OWNER-LOCKED         | Owner-approved direction; not yet a complete build contract.       | Versioned owner decision.                                                       | No implementation claim |
| SPECIFIED            | Buildable target documented with scope and acceptance obligations. | Current approved specification and traceability.                                | Baseline                |
| CONTRACT-APPROVED    | Schema/API/event/state/permission/offline contracts approved.      | Approved contract artifacts, reviewers and version IDs.                         | Yes                     |
| SCAFFOLDED           | Repository structure or nonfunctional skeleton exists.             | Repository path, commit, build result and scope statement.                      | Yes                     |
| IMPLEMENTED-IN-DEV   | Capability works in development.                                   | Commit, applied dev migrations, automated tests and dev deployment evidence.    | Yes                     |
| INTEGRATION-VERIFIED | Cross-product and failure-path verification passed.                | Integration/security/offline/payment/reconciliation/performance evidence.       | Yes                     |
| PILOT-READY          | Operational prerequisites and go-live package are complete.        | Monitoring, DR, support, training, rollback and sign-off evidence.              | Yes                     |
| PILOT-PROVEN         | Approved pilot demonstrates target outcomes and stability.         | Pilot cohort, dates, metrics, incidents, acceptance and owner sign-off.         | Yes                     |
| PRODUCTION           | Running for approved production scope.                             | Production release, migrations, health, monitoring, support and owner approval. | Yes                     |
| DEPRECATED           | No longer approved for new use; removal/migration is controlled.   | Deprecation decision, consumers, migration and removal date.                    | Yes                     |

## 3. Evidence classes

| Evidence code | Evidence type                    | Required metadata                                                                |
| ------------- | -------------------------------- | -------------------------------------------------------------------------------- |
| E-REPO        | Repository implementation        | Repository, path, commit SHA, branch/tag, reviewer                               |
| E-MIG         | Migration                        | File/version, checksum, environment, applied timestamp, validation result        |
| E-CONTRACT    | Schema/API/event contract        | Artifact/version, approvers, compatibility and test references                   |
| E-TEST        | Automated or manual test         | Test ID, build/commit, environment, date, result, retained output                |
| E-DEPLOY      | Deployment/release               | Environment, release ID, artifact digest/signature, deployment date, rollback ID |
| E-OBS         | Monitoring/telemetry             | Dashboard/query, time window, thresholds and interpretation                      |
| E-SEC         | Security evidence                | Threat review, RLS/auth tests, scan/assessment, findings and closure             |
| E-DR          | Backup/restore/recovery evidence | Backup ID, restore target, drill date, achieved RPO/RTO                          |
| E-HW          | Hardware/device evidence         | Model/BOM, serial/certificate, image/release, certification test                 |
| E-PILOT       | Pilot evidence                   | Cohort, dates, scope, metrics, incidents, acceptance and owner sign-off          |
| E-PROD        | Production evidence              | Tenant/Store scope, release, health, support, monitoring and approval            |

## 4. Current baseline from the supplied source package

No complete repository checkout, applied migration history, executable end-to-end test package, deployment record, signed release inventory, pilot evidence or production telemetry was supplied with this control-pack task. Therefore this register asserts **no product capability above `SPECIFIED`**.

| Register ID | Capability/artifact                  | Product/domain            | Current status | Evidence/source                                       | Gap/constraint                                                                | Accountable owner    |
| ----------- | ------------------------------------ | ------------------------- | -------------- | ----------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------- |
| IMP-001     | Suite-wide product direction         | Suite                     | OWNER-LOCKED   | Current Project Instructions and owner decisions      | Direction only; no implementation claim                                       | Owner                |
| IMP-002     | Source-of-truth control pack         | Documentation             | SPECIFIED      | This v1.0.0 control pack files                        | Created in this package; requires owner adoption into repository              | Project Owner        |
| IMP-003     | Master technical authority v4        | Documentation             | PROPOSED       | Expected file not present                             | Must be created and approved                                                  | Project Owner        |
| IMP-004     | Master business authority v2         | Documentation             | PROPOSED       | Expected file not present                             | Must be created and approved                                                  | Project Owner        |
| IMP-010     | Admin PWA Portal Phase 1             | kitluy-admin-pwa-portal   | SPECIFIED      | kitluy-admin-pwa-portal-phase1-spec-v3.1.0.md         | No complete repository/migration/test/deployment evidence supplied            | Product Owner        |
| IMP-011     | Chain Portal Phase 1                 | kitluy-chain-pwa-portal   | SPECIFIED      | kitluy-chain-portal-phase1-spec-v3.0.0.md             | No complete implementation evidence supplied                                  | Product Owner        |
| IMP-012     | Partner PWA Portal Phase 1           | kitluy-partner-pwa-portal | SPECIFIED      | kitluy-partner-portal-phase1-spec-v2.0.0.md           | No complete implementation evidence supplied                                  | Product Owner        |
| IMP-013     | Partner App Phase 1                  | kitluy-partner-app        | SPECIFIED      | kitluy-partner-app-phase1-spec-v2.0.0.md              | No complete implementation evidence supplied                                  | Product Owner        |
| IMP-014     | POS Desktop App Phase 1              | kitluy-pos-desktop-app    | SPECIFIED      | kitluy-pos-desktop-app-phase1-spec-v4.0.0.md          | No complete implementation evidence supplied                                  | Product Owner        |
| IMP-015     | POS Mobile App Phase 1               | kitluy-pos-mobile-app     | SPECIFIED      | kitluy-pos-mobile-app-phase1-spec-v2.2.0.md           | No complete implementation evidence supplied                                  | Product Owner        |
| IMP-016     | Storefront Phase 1                   | kitluy-storefront         | SPECIFIED      | kitluy-storefront-phase1-spec-v1.1.0.md               | Owner-approved target; no complete implementation evidence supplied           | Product Owner        |
| IMP-017     | Store Hub Phase 1                    | kitluy-hub-agent          | SPECIFIED      | kitluy-storehub-phase1-spec-v1.0.0.md                 | Owner-approved target; no image/cert/hardware/pilot evidence supplied         | Edge Product Owner   |
| IMP-018     | B2B Website Phase 1                  | kitluy-b2b-website        | SPECIFIED      | kitluy-b2b-website-phase1-spec-v1.0.0.md              | No complete implementation evidence supplied                                  | Product Owner        |
| IMP-019     | Ecosystem Infrastructure Phase 1     | Infrastructure            | SPECIFIED      | kitluy-ecosystem-infrastructure-phase1-spec-v1.0.0.md | No applied IaC/deployment/monitoring/restore evidence supplied                | Platform Owner       |
| IMP-020     | Master Feature Registry v0.2         | Planning registry         | PROPOSED       | kitluy-master-feature-registry-v0.2.*                 | Planning/normalization baseline; individual owner locks remain direction only | Product Owner        |
| IMP-021     | Twelve registry capability decisions | Cross-domain              | OWNER-LOCKED   | kitluy-owner-decision-lock-12-capabilities-v1.0.md    | No decision is implementation evidence                                        | Project Owner        |
| IMP-030     | Applied Supabase migration baseline  | Database                  | PROPOSED       | No authoritative applied-migration inventory supplied | Do not claim schema readiness                                                 | Backend Owner        |
| IMP-031     | Signed edge release pipeline         | Release                   | OWNER-LOCKED   | Project Instruction Writing.txt and current specs     | Direction specified; no release artifacts or verification supplied            | Platform/Fleet Owner |
| IMP-032     | Phase 1 pilot                        | Operations                | PROPOSED       | No approved pilot evidence package supplied           | Pilot-ready/proven/production claims prohibited                               | Project Owner        |

## 5. Status-transition gates

### SPECIFIED -> CONTRACT-APPROVED

- authoritative entity and state model;
- schema/API/event contracts;
- permissions and RLS design;
- offline and reconciliation behavior;
- migration and compatibility plan;
- acceptance criteria and documentation review;
- named approvers.

### CONTRACT-APPROVED -> SCAFFOLDED

- repository path exists;
- build/lint/typecheck passes for skeleton scope;
- package ownership and CI are registered;
- no false end-user capability claim.

### SCAFFOLDED -> IMPLEMENTED-IN-DEV

- code and required migrations are applied in development;
- unit/component/contract tests pass;
- feature flag and audit controls work;
- dev deployment is reproducible.

### IMPLEMENTED-IN-DEV -> INTEGRATION-VERIFIED

- cross-product contracts pass;
- Store Hub/offline/reconnect behavior passes where applicable;
- tenant/RLS/permission isolation passes;
- payment, finance and inventory reconciliation passes;
- failure, retry, idempotency, performance and recovery paths pass.

### INTEGRATION-VERIFIED -> PILOT-READY

- monitoring and alerts active;
- backup/restore and rollback tested;
- support, training, hardware and runbooks complete;
- open blockers and `[REQUIRED]` production values closed;
- go-live approval recorded.

### PILOT-READY -> PILOT-PROVEN

- approved cohort operated for the agreed period;
- success metrics and failure thresholds assessed;
- incidents and corrections documented;
- owner accepts pilot result.

### PILOT-PROVEN -> PRODUCTION

- production release and migrations approved;
- production monitoring, support and recovery active;
- commercial/legal requirements complete;
- production scope and rollback documented.

## 6. Evidence record template

```markdown
### EVID-<product>-<number>

- Capability / feature ID:
- Claimed status:
- Product and vertical:
- Environment:
- Repository / path:
- Commit / release / artifact digest:
- Applied migrations:
- Tests and retained outputs:
- Deployment record:
- Monitoring / security / DR evidence:
- Pilot or production scope:
- Verified by:
- Verification date:
- Limitations:
- Related decision/reconciliation IDs:
```

## 7. Downgrade and deprecation rule

When evidence expires, is invalidated, or no longer matches the active release, downgrade the status. Do not preserve a higher status for convenience. `DEPRECATED` requires a migration/removal plan and must not hide active production dependencies.

---

## Repository addendum — KL-DOCS-001 (not part of the owner original)

Owner original (immutable):
`docs/source/canonical/kitluy-implementation-status-and-evidence-register-v1.0.0.md`.
The owner 11-status model (PROPOSED → OWNER-LOCKED → SPECIFIED →
CONTRACT-APPROVED → SCAFFOLDED → IMPLEMENTED-IN-DEV → INTEGRATION-VERIFIED →
PILOT-READY → PILOT-PROVEN → PRODUCTION → DEPRECATED) is adopted as canonical.

**Baseline correction:** the owner register's §4 baseline ("no complete
repository checkout was supplied") predates this repository. The evidence rows
below re-register the actual repository evidence under the owner model.

**Status mapping (KLREC-2026-07-26-008):** bootstrap statuses BUILT/TESTED map
to **SCAFFOLDED** with linked evidence — they exceed bare scaffolding (real
unit-tested behavior) but do not meet the IMPLEMENTED-IN-DEV gates (no applied
dev migrations, no reproducible dev deployment). No status was advanced beyond
its evidence. Evidence classes: E-REPO (repository path at commit),
E-TEST (executed test run 2026-07-26, `pnpm verify` all 11 gates passing).

### Machine-checkable evidence table

Rows above SPECIFIED carry linked evidence (validated by `pnpm docs:registry-check`).

<!-- registry-check:start -->

| Item | Status | Evidence |
| --- | --- | --- |
| Shared package @kitluy/money (integer minor-unit money, KHR/USD, allocation) | SCAFFOLDED | E-REPO+E-TEST `packages/money/test/money.test.ts` (8 tests, pass 2026-07-26) |
| Shared package @kitluy/localization (km/en, +855 phones, business date) | SCAFFOLDED | E-REPO+E-TEST `packages/localization/test/localization.test.ts` |
| Shared package @kitluy/api-errors (envelope + POS §14.3 codes) | SCAFFOLDED | E-REPO+E-TEST `packages/api-errors/test/api-errors.test.ts` |
| Shared package @kitluy/rbac (explicit grants, deny-by-default) | SCAFFOLDED | E-REPO+E-TEST `packages/rbac/test/rbac.test.ts` |
| Shared package @kitluy/approvals (A0-A4, four-eyes self-approval rejection) | SCAFFOLDED | E-REPO+E-TEST `packages/approvals/test/approvals.test.ts` |
| Shared package @kitluy/audit (append-only log) | SCAFFOLDED | E-REPO+E-TEST `packages/audit/test/audit.test.ts` |
| Shared package @kitluy/sync-protocol (outbox envelope, conflict policies, idempotency keys) | SCAFFOLDED | E-REPO+E-TEST `packages/sync-protocol/test/sync-protocol.test.ts` |
| Shared package @kitluy/feature-flags (Phase 1 active; 2-8 + future clients OFF) | SCAFFOLDED | E-REPO+E-TEST `packages/feature-flags/test/phase-gates.test.ts` |
| Shared package @kitluy/shared-config (fail-closed env parsing, local-target guard) | SCAFFOLDED | E-REPO+E-TEST `packages/shared-config/test/shared-config.test.ts` |
| Shared package @kitluy/observability (JSON logger, secret redaction) | SCAFFOLDED | E-REPO+E-TEST `packages/observability/test/observability.test.ts` |
| Shared packages shared-types, resource-scope, event-contracts, web-ui | SCAFFOLDED | E-REPO `packages/*/src` (exercised via dependent tests and app smoke tests) |
| Remaining 26 shared packages (boundary placeholders) | SCAFFOLDED | E-REPO `packages/*/README.md` (boundary only, no behavior) |
| Laundry vertical: T1-T4 profile capability matrix | SCAFFOLDED | E-REPO+E-TEST `verticals/phase1-laundry/test/laundry.test.ts` |
| Laundry vertical: T2 display state machine (matches terminal-profile contract §6.4) | SCAFFOLDED | E-REPO+E-TEST `verticals/phase1-laundry/test/laundry.test.ts` |
| Laundry vertical: custody event registry + per-piece/per-weight pricing lines | SCAFFOLDED | E-REPO+E-TEST `verticals/phase1-laundry/test/laundry.test.ts` |
| Store Hub agent: transactional outbox + offline/reconnect harness | SCAFFOLDED | E-REPO+E-TEST `services/kitluy-hub-agent/test/offline-reconnect.test.ts` (pnpm test:offline) |
| Store Hub agent: LAN API kernel (agreed routes; mutations blocked per KLREC-2026-07-26-001) | SCAFFOLDED | E-REPO+E-TEST `services/kitluy-hub-agent/test/lan-api.test.ts` |
| 18 service kernels (health/ready/version, config validation, graceful shutdown) | SCAFFOLDED | E-REPO+E-TEST `services/*/test/http.test.ts` |
| 4 governed API OpenAPI governance skeletons + contract tests | SCAFFOLDED | E-REPO+E-TEST `services/kitluy-*-api/test/contract.test.ts` (pnpm test:contract) |
| 8 Phase 1 application shells (fail-closed, km/en, error boundaries) | SCAFFOLDED | E-REPO+E-TEST `apps/*/test/` smoke tests + pnpm build (68 tasks, 2026-07-26) |
| **WS-05 catalog, pricing and configuration persistence (0040/0045/0050)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST reset-from-zero 11 migrations + 73-case suite + live probes (overlap guard, snapshot immutability, scope escape); evidence `docs/evidence/phase1/ws-05/WS-05-T001/EXECUTION-EVIDENCE.md`; review 2026-07-27__WS-05-06-EXECUTION APPROVED |
| **WS-06 customer identity and consent persistence (0060/0065)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST same chain (E.164/merge/consent probes; composite tenant FK fix probe-verified); same evidence + review |
| **WS-07 Laundry Booking aggregate and garment custody persistence (0075/0080/0095)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-MIG reset-from-zero 16 migrations (30 new relations); E-TEST 121 assertion/RLS PASS (26 assertions + 95 RLS incl. 38 Cycle-6 cases), engine regression 65/65 unchanged, DB-backed integration 11/11 (`verticals/phase1-laundry-persistence`); E-SEC 30 relations RLS forced, 30 SELECT-only policies, 0 anon/0 write, append-only + no-delete triggers probed live; evidence `docs/evidence/phase1/ws-07/WS-07-T002,T003,T004/EXECUTION-EVIDENCE.md`; review `00_AI_HANDOFF/reviews/2026-07-27__WS-07-08-EXECUTION__REVIEW.md` **APPROVED-WITH-CONDITIONS** (47/47 adversarial probes held; carried non-blocking RV-002/RV-003; owner acceptance of the financial/custody surface remains OPEN). NOT INTEGRATION-VERIFIED — no Edge/Hub routes (BLK-003), no deployment |
| **WS-08 payments, deposits, refunds, reconciliation and finance posting persistence (0085/0090/0095)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-MIG same chain; E-TEST engine regression 40/40 (26 canonical vectors + 14 invariants) unchanged, DB-backed integration 12/12 (`packages/payments-persistence`), balanced-journal/idempotency/four-eyes probes live; E-SEC journals RPC-write-only (direct INSERT/UPDATE/DELETE revoked incl. service_role), AMD-I1..I7 enforced; evidence `docs/evidence/phase1/ws-08/WS-08-T002,T003,T004,T005/EXECUTION-EVIDENCE.md`; same review **APPROVED-WITH-CONDITIONS**. KHQR is the `DEV_KHQR_SIM` simulator only — live provider blocked (PAY-OD-001/BLK-006); chart of accounts fictional `DEV-*` (FIN-OD-001 open). NOT INTEGRATION-VERIFIED |
| **KL-DEC-001 contract alignment (Edge routes/scopes, terminal profiles, RBAC, resource scopes, event contracts, API errors)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | Owner decision KLD-2026-07-26-002 OWNER-APPROVED (all five groups) + completed ballot; E-REPO commits 463b50a/a97a380/83c86aa/0508cbb/191edc6/350aace; E-MIG 0100 terminal_profile_identifiers sha256 3068d287…2fff1f (reset-from-zero 17 migrations, double seed idempotent); E-TEST verify 11/11, typecheck 87/87, test 58/58, test:contract 20/20, db:test 121, test:rls 95, targeted suites 18/59/15/6/45/76/11/9; E-SEC Hub fail-closed across 297 verb×path probes, all retired identifiers rejected, forged grants denied, scopeBreadth hardened from fail-open; evidence `docs/evidence/phase1/kl-dec-001/KL-DEC-001-ALIGNMENT-EVIDENCE.md`; review `00_AI_HANDOFF/reviews/2026-07-27__KL-DEC-001-ALIGNMENT__REVIEW.md` **APPROVED-WITH-CONDITIONS** (no blocking findings). Edge mutation business workflows remain NOT INTEGRATION-VERIFIED; carried conditions RV-001..RV-005 and owner items KLREQ-015..019 |
| **WS-10 synchronization and configuration publication (outbox delivery, signed transmission, idempotent cloud ingestion, cloud-to-Hub delivery, snapshot/grant publication)** | **IMPLEMENTED-IN-DEV** (2026-07-28) | E-MIG 27 Hub migrations applied from zero (0015-0026 added this cycle; 0001 NEVER edited — the A01 enum alignment is an ADDITIVE `ALTER TYPE ... RENAME VALUE` that preserves member OIDs), journal 27 applied / 0 pending / 0 drift / 0 missing; cloud group 0110 creates `kitluy_sync` (already in DD v1.0.0) with RLS ENABLE+FORCE, SELECT-only client policies, zero anon policies. E-TEST Hub assertions **35** (counted as `NOTICE:  PASS`; 29 at WS-09 close + sections 29a-29e); cloud db:test **124** (121 + section 27); test:rls **94 distinct cases / 95 `NOTICE:  PASS`** — CORRECTED 2026-07-28 by WS-11-T001, which measured the runtime figure directly at this commit: RV-005's "the 95 is a miscount" was itself wrong, both numbers are right under different metrics, and the gap is case `KLSEC-036` emitting two notices; verify **11/11**; hub-agent **274 passed / 2 skipped** (opt-in destructive backup-restore), @kitluy/sync-protocol 21, kitluy-sync-service 18. E-SEC amendment KLD-2026-07-28-001-A01 enforced clause by clause: `reconciliation_required` is a SEPARATE enum so it cannot become a delivery state; ONE shared external projection held equal between SQL and TypeScript over the whole cross product; the conflict dimension is governed-only and a bare UPDATE by the sync worker is refused; `rejected` requires a code from a DATABASE-side durable set so a transport failure cannot become a cloud verdict; no acknowledgement without the cloud's own identity and a matching lease; a dead letter always raises the conflict dimension and writes an operator record. DEFECTS FOUND AND FIXED THIS CYCLE: (a) every Hub procedure was EXECUTE-able by PUBLIC, a PostgreSQL creation default a later GRANT does not revoke — closed by 0020 across the WS-10 AND WS-09 procedures. (b) **INDEPENDENT REVIEW RV-001 (blocking):** the amendment §5 gate was a custom GUC any role could set with `set_config()`, so the rule was not enforced at all — the reviewer cleared a raised reconciliation as `kitluy_sync_worker` with a forged authority and zero audit rows. Closed by 0024, which moves the gate to the executing identity of a NOLOGIN, memberless owner role; re-probed, the forge now fails for the sync worker and for the database owner. (c) **INDEPENDENT REVIEW RV-002 (blocking):** the grant resolver compared only the exact scope tuple, so a narrow allow beat a broad deny; closed by 0024, which walks the whole scope chain. The re-verification pass then found two more fail-open defects in the same resolver — RV-013 (a platform-scoped grant from ANOTHER TENANT resolved to `allow` here) and RV-014 (a DENY with a NULL scope_id was silently unmatchable and so failed OPEN) — both classed non-blocking because the resolver has no production caller, and both fixed anyway in 0025. All four findings were reproduced before being fixed. SEPARATELY, a re-read of the amendment against the running database found that the IMPLEMENTATION HAD DIVERGED FROM THE OWNER RULING in three places (C34/C35/C36): the §3 external-status table was wrong on three of six rows, `dead_letter + none` — a state §2 names explicitly — was unreachable, and the §6 invalid transitions were not refused. All three are closed by migration 0026 and asserted by section 29e. The wrong projection had been written into the assertions as well, which is why the gates stayed green while the code contradicted the ruling. Review `00_AI_HANDOFF/reviews/2026-07-28__WS-10-SYNC__REVIEW.md` **APPROVED-WITH-CONDITIONS**. Evidence `docs/evidence/phase1/ws-10/WS-10-EXECUTION-EVIDENCE.md`. MATERIAL LIMITATIONS: **WS-10 ships no wired production path** (review RV-004) — ingestBatch, prepareSignedBatch, applyBatchResponse and resolveGrant have no production callers and the transport/ingestion ports have no implementations, so this is the mechanisms and their guarantees, not a running delivery worker; **the §5 role separation is structural only**, since all kitluy_* roles are NOLOGIN and the agent connects as the database owner; the §5 immutable audit is a CALLER convention rather than a database guarantee (KLRISK-HUB-007) and the database owner can still forge the §5 identity (KLRISK-HUB-008, inside the KLRISK-HUB-003 trust boundary); **no cloud<->Hub integration run exists** — the two halves are tested separately with the transport injected, so NOT INTEGRATION-VERIFIED; **no production batch signer exists** (`createProductionBatchSigner` refuses; key custody `[REQUIRED: ...]` on BLK-005); KLREQ-024 and KLREQ-028 remain open so **no complete T1->T4 lifecycle is claimed**; KLREQ-025's verbatim 21-field grant enumeration is not in the register, so the projection implements the ruling's semantics and records the remainder as `[REQUIRED: ...]` rather than inventing fields; NEW open items KLREQ-029 (operator reconciliation-clearance key) and KLREQ-030 (dead-letter abandonment key) both fail closed; additive extensions G9/G10/G11 owe an amendment to the canonical Hub schema document |
| **WS-11-T003 Step 4 — governed credential revocation: emergency path, post-approval, lapse, and end-to-end containment (groups 0148-0154, Phases A-E)** | **IMPLEMENTED-IN-DEV WITH RECORDED ENVIRONMENT CONDITION** (2026-07-30) | **A COMPONENT promotion with an explicit composition ceiling. WS-11 overall stays SCAFFOLDED / IN PROGRESS. T004–T008 not started.** Phase E (three independent reviewers) COMPLETED 2026-07-30 — all three `APPROVED-WITH-CONDITIONS`, no CRITICAL code-blocking findings. Reviews: `00_AI_HANDOFF/reviews/2026-07-30__WS-11-T003-STEP4-PHASE-E__{DB-RBAC-REAUTH,GATEWAY-WORKER-COMPOSITION,CONCURRENCY-EVIDENCE}__REVIEW.md`. Gate handoff: `00_AI_HANDOFF/shared/2026-07-30__SHARED__WS-11-T003-STEP4__PHASE-E-PROMOTION-GATE__AI-HANDOFF.md`. **RECORDED ENVIRONMENT CONDITION (RV-GW-001): `RevocationGateway` / `loadRevocations` are library-available-but-uncalled** — zero apps/services/hub production callers; do NOT describe Step 4 as production-wired revocation composition. Also NOT claimed: production TypeScript lapse worker (0154 proves DB reachability only — RV-GW-002); offline Store Hub snapshot population of `revokedCertificateSerials` (RV-GW-003); device-level revoke beyond `retired` (KLREQ-034 OPEN). E-MIG 0148–0154 local only (KL-INF-P1-037). E-TEST clean `db:reset`+`db:seed`+`db:test` **PASS** including `ws11-emergency-post-approval-never-reverses`, `ws11-phase-d-containment`, `ws11-rc022-spendability-census` (satisfies R1 C1 / R3 RV-CE-003). Concurrency: 9 true Lock races + 2 SKIP LOCKED concurrent; scenarios 11–12 sequential/single-backend (handoff packaging corrected RV-CE-001/002). Containment join ONLINE proven (`pg-revocation-lookup.ts`). Toolchain: Node v24 residual — re-run on `.nvmrc` 22 before aggregate verify is authoritative. |
| **WS-11-T003 Step 4 component — renewal and credential-lifecycle worker runtime** | **IMPLEMENTED-IN-DEV component** (2026-07-29) | **A COMPONENT promotion ONLY. WS-11-T003 Step 4 is NOT complete and WS-11 stays SCAFFOLDED / IN PROGRESS. Production scheduling/deployment — NOT IMPLEMENTED. Provider-key destruction — BLOCKED ON KLREQ-031. Cross-process provider durability — NOT PROVEN.** E-REPO `@kitluy/job-contracts` advances SCAFFOLDED → IMPLEMENTED-IN-DEV as the NEUTRAL runtime (`durable-job.ts`, `retry-policy.ts`, `job-runtime.ts` — nothing names a device, a credential or a key), consumed by `packages/device-identity/src/credential-lifecycle-jobs.ts`, which is an ADAPTER holding no reconciliation or lifecycle logic of its own. E-MIG cloud group 0135 adds `kitluy_ops.durable_jobs` + append-only `durable_job_attempts` with NOLOGIN `kitluy_job_governor`/`kitluy_worker_service`, `FOR UPDATE SKIP LOCKED` claiming, deterministic deduplication by unique constraint, bounded backoff owned by the database, and governed operator controls. SHARED INFRASTRUCTURE REUSED, NOT DUPLICATED: the gap was PROVEN by probing the cloud database (only `net.http_request_queue`, owned by the pg_net extension, matched (job\|queue\|lease\|dead_letter\|worker\|schedul); no lease/attempt/backoff column existed in any `kitluy_*` schema), the Hub lease model (`hub/migrations/0016`) was examined and rejected on two facts — it is in the SEPARATE `kitluy_hub_local` database, and its ordered stop-not-skip stream semantics would import a head-of-line block for independent per-device work — and placement in `kitluy_ops` was decided by `db-validate.mjs`, whose `CONTROL_PLANE_SCHEMAS` allowlist contains exactly that schema. E-TEST after `db:reset`+`db:seed`: `pnpm db:test` **184 PASS** (new sections 40a/40b), `pnpm test:rls` **104 PASS**, `@kitluy/device-identity` **589 passed / 0 skipped (25 files)** including **12 LIVE job tests that EXECUTED** under `SET LOCAL ROLE kitluy_worker_service`, of which 3 use TWO REAL PostgreSQL CONNECTIONS to prove true concurrency; **four consecutive full runs at 589/589** after a test-isolation defect was fixed. `pnpm verify` **11/12**, the single failure the pre-existing `format:check` on the excluded `R&D_HSA_AI_Agent_MVP.md`. E-SEC at-least-once execution is STATED, not claimed away — exactly-once BUSINESS EFFECT comes from deduplication, lease ownership, idempotent operations and governed compare-and-swap; the worker holds NO direct table authority here or over credential state, cannot release/cancel/revive its own jobs, and cannot retire a credential without separately assuming `kitluy_issuance_service` (job authority and credential authority are distinct grants and neither role is a member of the other); a stale lease token can neither complete nor fail a job; attempt history is monotonic and job identity and payload are immutable against a direct write by the table owner; a dead letter cannot requeue itself; jobs cannot be deleted; the audit stores a lease-token FINGERPRINT, never the token. CLOCK SEPARATION ENFORCED AND TESTED: scheduler time sets `next_attempt_at`/leases/backoff while credential validity, overlap expiry and destruction eligibility remain trusted-time decisions; an active overlap DEFERS to the persisted `overlap_ends_at` to the millisecond, and reaching the due time grants no business verdict. `KEY_DESTRUCTION_NOT_AUTHORIZED` is classified `terminal_success` and COMPLETES the job (retrying an absent owner decision can only reach the same answer and would dead-letter a correct job); the cleanup handler contains no destroy call of any kind; live evidence `eligible: true, authorized: false`, provider destroy calls **0**, database destroyed transitions **0**. FOUR DEFECTS FOUND BY EXECUTING THIS GROUP, all in code first written this session and corrected before it shipped (so no new KLRISK entry): `running -> leased` was missing so a worker that died AFTER starting was unrecoverable; `record_job_attempt_v1` reached `extensions.digest()` its definer-owner cannot use, which the migration's own assertions had missed because they inspected grants without CALLING the function; ownership transfer needed CREATE on `kitluy_ops`; and the worker role was not assumable at all. MATERIAL LIMITATIONS: **no scheduler, cron, pg_cron, CronJob or queue infrastructure was deployed or written** — the runtime is callable only and cadence is a later infrastructure decision; provider durability across a real process restart is NOT proven (in-memory development provider), and an unresolvable provider key reference goes to `manual_review` with typed evidence rather than silently regenerating; the renewal-reconcile job kind has no live end-to-end scenario of its own (its handler is unit-tested and the state machine it calls has the 21-case live crash matrix from Prompt 3A); `durable_jobs` pins `environment = 'development'` by CHECK; **still no governed credential revocation (KLRISK-DEVICE-007) — a scheduled executor is not revocation**; no independent hostile review. KLRISK-DEVICE-003 remains OPEN; KLREQ-031 remains OPEN and is deliberately not closed. Toolchain deviation: Node v24.15.0, not the ACTIVE-BASELINE 22.23.0 |
| **WS-11-T003 Step 4 component — credential overlap expiry and superseded-key lifecycle** | **IMPLEMENTED-IN-DEV component** (2026-07-29) | **A COMPONENT promotion ONLY. WS-11-T003 Step 4 is NOT complete and WS-11 stays SCAFFOLDED / IN PROGRESS.** E-REPO `packages/device-identity/src/credential-lifecycle.ts` — `advanceDeviceCredentialLifecycle` reads authoritative state, retires the previous credential once the §5 overlap ends, and EVALUATES BUT NEVER PERFORMS provider-key destruction. E-MIG cloud group 0134 adds `retire_overlapped_credential_v1` (governed, idempotent, HALF-OPEN boundary, selects the credential from the HEAD so the current credential cannot be retired, refuses on untrusted time, never overwrites `revoked`), the append-only `device_credential_lifecycle_events` audit, and `key_destruction_policy` shipped with destruction DISABLED, both retention periods NULL and the missing owner decision NAMED. E-TEST after `db:reset`+`db:seed`: `pnpm db:test` **182 PASS**, `pnpm test:rls` **104 PASS**, `@kitluy/device-identity` **543 passed / 0 skipped (23 files)** including **8 LIVE lifecycle tests that EXECUTED** under `SET LOCAL ROLE kitluy_issuance_service`; `pnpm verify` **11/12** with the single failure the pre-existing `format:check` on the excluded `R&D_HSA_AI_Agent_MVP.md`. E-SEC the boundary is stated identically in `overlapIsActive`, in `evaluateCertificateValidity` and in SQL — `trusted_now < overlap_ends_at` usable, `>=` expired — and asserted one millisecond before, exactly at, and after, in both the unit suite and SQL section 39b. After the boundary the previous credential is REFUSED (`CERT_STALE_CERTIFICATE_GENERATION` under same-key, fingerprint mismatch under rotation) while the current credential keeps verifying; the head `version` is deliberately NOT bumped so an in-flight renewal reservation survives; retirement destroys NO provider key (same-key runs end `keys active:1`, rotation `superseded:1, active:2`, ZERO destroyed throughout). The KLRISK-DEVICE-008 overlap grant is hardened with a REQUIRED `previousCredentialState`, so a spent grant cannot keep a retired credential alive. ONE DEFECT FOUND BY EXECUTION: **KLRISK-DEVICE-010** — nothing could retire a credential (proven under BOTH `postgres` and `kitluy_issuance_service`: `permission denied for table device_credentials`, and no governed function performed the transition), so a previous credential stayed `issued` for ever once its overlap lapsed; corrected additively by 0134 with migration-local hostile assertions and permanent SQL section 39. **Provider-key destruction — SPECIFIED / BLOCKED ON OWNER POLICY**, `[REQUIRED: device_key_destruction_owner_decision]`: no retention period was invented, `destruction_enabled` cannot be set true without naming a decision AND supplying both retention periods (two CHECKs, not conventions), a key still backing the CURRENT credential is never destruction-eligible, and a fully unblocked key returns `KEY_DESTRUCTION_NOT_AUTHORIZED` (`eligible: true, authorized: false`) and destroys nothing. MATERIAL LIMITATIONS: no scheduled worker or cron — the lifecycle advances only when called; `destruction_pending`/`destruction_failed` states deliberately NOT added while no policy can reach that path; true parallel executors not exercised live (one pg connection per transaction); **still no governed credential revocation (KLRISK-DEVICE-007) — overlap RETIREMENT is not revocation and key DESTRUCTION is not revocation**; no independent review. KLRISK-DEVICE-003 remains OPEN. Toolchain deviation: Node v24.15.0, not the ACTIVE-BASELINE 22.23.0 |
| **WS-11-T003 Step 4 component — renewal reconciliation and interrupted-operation recovery** | **IMPLEMENTED-IN-DEV component** (2026-07-29) | **A COMPONENT promotion ONLY. WS-11-T003 Step 4 is NOT complete and WS-11 stays SCAFFOLDED / IN PROGRESS.** E-REPO `packages/device-identity/src/renewal-reconciliation.ts` — one exhaustive decision table (19 identified rules, most-specific first) that compares PostgreSQL against the key provider and performs exactly ONE action, failing closed on any pair no rule describes. E-MIG cloud group 0133 adds the append-only `device_renewal_reconciliations` audit with a governed writer, RLS ENABLE+FORCE, a total-order sequence and a CHECK refusing PEM key material — the durable-evidence gap, since no issuance table could carry the observed PROVIDER state. E-TEST after `db:reset`+`db:seed`: `pnpm db:test` **180 PASS**, `pnpm test:rls` **104 PASS**, `@kitluy/device-identity` **494 passed / 0 skipped** including a **21-case LIVE crash matrix** (6 same-key boundaries, 9 rotation boundaries, plus hostile cases) run under `SET LOCAL ROLE kitluy_issuance_service`, each reconciled twice with the second run proven a no-op. E-SEC exactly-once BUSINESS EFFECT rather than exactly-once signing (stated, not claimed away); an existing signature is reused and a conflicting one requires human review; a database-active key the provider does not have is refused as `INCONSISTENT_STATE` and recorded, never repaired by regenerating; the executor records only through the governed function and holds no table authority. TWO DEFECTS FOUND BY EXECUTION: **KLRISK-DEVICE-008** — the §5 three-day overlap was unreachable because the verifier admitted one key fingerprint and one generation floor, so the previous credential stopped verifying the moment the head advanced (corrected additively with an OPTIONAL `permittedOverlap`, absent by default); **KLRISK-DEVICE-009** — rotation passed the timestamped PoP challenge hash as the request-identity hash, so every retry was refused as a changed payload (corrected; no migration needed). MATERIAL LIMITATIONS: provider durability across a real process restart is NOT proven (in-memory development provider); true parallel reconcilers are not exercised live (one pg connection per transaction); no scheduled worker, no overlap-expiry advancement, no key destruction, **no governed credential revocation (KLRISK-DEVICE-007)**, no independent review. KLRISK-DEVICE-003 remains OPEN. Toolchain deviation: Node v24.15.0, not the ACTIVE-BASELINE 22.23.0 |
| **WS-11-T003 Step 4 components — device credential renewal (same-key preflight + reservation, same-key prepare/sign/finalize, optional rotate_key orchestration and provider activation)** | **IMPLEMENTED-IN-DEV components** (2026-07-29) | **These are COMPONENT promotions ONLY. WS-11-T003 Step 4 is NOT complete and WS-11 overall stays SCAFFOLDED / IN PROGRESS.** E-REPO `packages/device-identity/src/same-key-renewal-preflight.ts`, `same-key-renewal-issuance.ts`, `rotate-key-renewal-issuance.ts`, `replacement-key-provider.ts`. E-MIG cloud groups 0131 (issuance-service schema USAGE — the named executor held EXECUTE on every governed function but no schema USAGE, so it could never call one; KLRISK-DEVICE-004) and 0132 (renewal finalization corrections — the overlap window compared DEVICE trusted time against SERVER time so 69ms of skew refused every renewal, KLRISK-DEVICE-005; and a `reuse_current_key` reservation could never reach `completed`, KLRISK-DEVICE-006). Both migrations carry migration-local hostile assertions and permanent SQL assertions (sections 36, 37). E-TEST after `db:reset`+`db:seed`: `pnpm db:test` **178 PASS**, `pnpm test:rls` **104 PASS**, `@kitluy/device-identity` **418 passed / 0 skipped** including **41 LIVE PostgreSQL tests that EXECUTED** under `SET LOCAL ROLE kitluy_issuance_service` rather than under inherited `postgres` privileges. E-SEC the incumbent is DERIVED from the authoritative credential head and never accepted from a caller; there is no `signatureValid` input anywhere on these boundaries; a one-byte mutation of a PERSISTED signature is refused although the row still reads `issued`; the canonical TBS is built by PostgreSQL and signed unchanged, with nine single-field mutations each refused by real cross-layer conformance; the CA intermediate signs credentials while the device key only proves possession; private keys never leave the provider and no export surface exists; rotation stays DISABLED in the shipped policy and cannot be enabled without naming an owner decision (CHECK, not convention); a rotated key waits in `credential_issued_pending_activation` until the PROVIDER activates it, because a credential row proves issuance and not that the private half is loaded. MATERIAL LIMITATIONS: no crash reconciliation, no scheduled retry, no overlap-expiry lifecycle, no automatic key destruction, **no governed credential-revocation operation at all (KLRISK-DEVICE-007 — device containment is NOT revocation and has not been tested as one)**, and no independent hostile review of these components. KLRISK-DEVICE-003 (OPTION B: this package is the only cryptographic verifier and is inside the trusted computing base) remains OPEN. Toolchain deviation: produced on Node v24.15.0, NOT the ACTIVE-BASELINE 22.23.0, so aggregate `pnpm verify` is not baseline-authoritative and every gate was run individually |
| **WS-11-T006-P04 — A/B installation, health check and rollback** | **IMPLEMENTED-IN-DEV** (2026-08-06; physical slot-adapter gap RECORDED) | E-MIG Hub 0039 (matrix-enforced lifecycle, one-rollback pin, auto-journal). E-CODE release-agent.ts (owner gate 20 s/5 min/3 probes, offline health probe, durable resume). E-TEST agent suite 5/5, hub:db:test 43 (section 36). Terminals verify independently. Record: the P04 SHARED handoff |
| **WS-11-T006-P03 — Signed configuration and release authority** | **IMPLEMENTED-IN-DEV** (2026-08-06) | E-MIG cloud 0180 + Hub 0038. E-TEST cloud db:test exit 0 (241 PASS, section 57 + WS11-N23), hub:db:test 42 (section 35), release-manifest unit 5/5, release-cache integration 3/3, sync-configuration regression green. Pilot/Stable BLK-005 refusal proven with the owner sentinel. Record: the P03 SHARED handoff |
| **WS-11-T007 — Device Security, Offline, Recovery and Concurrency Verification** | **IN PROGRESS** (2026-08-06; KLD-2026-08-06-WS11-T007-001 LOCKED) | E-DOC security test system §19 (execution decision, eleven locked rules); T007 debt census D1–D7 (00_AI_HANDOFF/shared/2026-08-06__SHARED__WS-11-T007__DEBT-CENSUS.md). Stage 0 DONE: migration-0038 immutability reconciliation (b031cb8, byte-for-byte restore + pinned scanner exception). Stages 2–6 (debts, security/isolation matrix, offline/restart matrix, race/fault matrix, final run) pending |
| **WS-11-T006 — Store Hub Replacement, Recovery and Signed Release Lifecycle** | **COMPLETE — IMPLEMENTED-IN-DEV** (2026-08-06 S02; physical slot-adapter gap RECORDED) | P01–P04 all IMPLEMENTED-IN-DEV (rows below, executed evidence). S02 closeout run: cloud reset 0000→0180 + db:test 242 PASS lines zero failures (WS11-N22/N23a/b); Hub reset 0000→0039 + hub:db:test 43 PASS (tally 72); device-identity 872/872; hub-agent 356 passed with only the pre-recorded race-A debt failing; destructive backup round trip 2/2; secret:scan 1396 clean (0038 guard probe amended to the 0027 house style, refusal proof unchanged); pnpm verify 10/12 (Format check = pre-existing repo-wide CRLF condition, 876 files, T006 intersection EMPTY; Unit tests = exactly the pre-recorded race-A pairing debt, every other package green). Pilot/Stable promotion stays BLK-005 fail-closed (refusal proven); cloud→Hub release assignment and the revoked-release feed ride BLK-006. Record: 00_AI_HANDOFF/shared/2026-08-06__SHARED__WS-11-T006__FINAL-CLOSEOUT__AI-HANDOFF.md |
| **WS-11-T006-P02 — Hub backup, restore and reconciliation** | **IMPLEMENTED-IN-DEV** (2026-08-06) | E-TOOL scripts/hub/backup-manifest.mjs + hub-db.mjs (KLBK1 encryption, manifest v1, verify-before-valid, fail-closed restore with ownership replay and restored_quarantine). E-TEST manifest unit 4/4; destructive round trip 2/2; CLI drill + corrupt/scope injections executed; hub:db:test 41. Fixes forward the pre-existing governor-ownership restore defect (recorded). Record: the P02 SHARED handoff |
| **WS-11-T006-P01 — Hub replacement authority and cutover** | **IMPLEMENTED-IN-DEV** (2026-08-06) | E-MIG cloud 0179 + Hub 0037. E-TEST cloud db:test exit 0 (238 PASS, section 56 + WS11-N22), hub:db:test 41 (section 34, tally 68), hub-agent registration 42/42. Full record: 00_AI_HANDOFF/shared/2026-08-06__SHARED__WS-11-T006-P01__HUB-REPLACEMENT-AUTHORITY-AND-CUTOVER__AI-HANDOFF.md |
| **WS-11-T006 owner decision (replacement/recovery/release values)** | **RECORDED — LOCKED** (2026-08-06) | E-DOC KLD-2026-08-06-WS11-T006-001 (docs/decisions/kitluy-storehub-replacement-recovery-and-release-owner-decision-v1.0.0.md). Values only — no capability is thereby implemented. |
| **WS-11-T005 — Device Fleet Health, Support Access and Incident Containment** | **COMPLETE — IMPLEMENTED-IN-DEV** (2026-08-06, P02 closeout; the PARTIAL condition of the same date is superseded) | E-MIG cloud group **0177** (fleet_health_policy with governed dev thresholds and pilot/production refused unsigned; device_health_reports append-only + device_health_projections advanced only forward on the Hub report sequence; kh1-idempotent ingest_device_health_report_v1 refusing foreign-Hub/non-Hub/stale-generation and flagging clock anomalies without projecting them; classify_fleet_freshness_v1 failing closed without a policy; the DD fleet_health_read read model with four SEPARATE truth labels; support_access_sessions/events under policy SS3 with ticket-always, consent for C2+, verified four-eyes for C3+, clamp, immediate revocation, clock-fail-closed expiry + worker sweeper; device_containment_states/events with apply/clear/approve-incumbent doors COMPOSING the 0120/0122 lifecycle, four-eyes for suspension/quarantine/escalation/recovery, dispositions restricted to the seven runbook classes, wrong-Store/stale-generation/replay refused, duplicates idempotent; kitluy_fleet_governor/service/gateway with the 0173 NOINHERIT hinge) + Hub group **0035** (terminal_health_status local authority; containment_directive append-only log whose HIGHEST sequence gates pairing_session and terminal_session inserts locally with the WAN down; support_ro kept to views). E-TEST canonical order 2026-08-06: db:reset 0000-0177 from zero, db:seed, **db:test exit 0 (235 PASS incl. sections 55a-d)**, **test:rls exit 0 (133 PASS incl. WS11-N21a/b)**, hub reset 0000-0035 + seed, **hub:db:test 39 PASS** (37 baseline + terminal-health-authority + containment-enforcement), hub-agent hub-database 28/28, hub-agent typecheck clean, secret:scan 1364 clean. E-DOC runbook kitluy-device-investigation-and-containment-runbook-v1.0.0.md (seven owner classifications). **KLRISK-DEVICE-002: RESOLVED-IN-DEV** (independent verification stays with T007/T008; signed disposition BLK-005-blocked). **NOT delivered, and the reason the status is PARTIAL: the Hub-agent health REPORTER** — the heartbeat derivation loop, the /edge/v1 terminal heartbeat surface and the outbox event kind that would feed the (built, proven) cloud ingestion door; plus Partner/Admin PWA screens (application shells remain fail-closed pending WS-16..19) and the containment-directive cloud PRODUCER (BLK-006, same posture as the credential-projection producer). Successor package DELIVERED same day (P02): heartbeat route, reporter runtime, outbox event device_fleet.health_projection_reported v1, cloud consumer; cloud 0178 owner values (freshness 120 s, per-class session caps), Hub 0036 sequencing/report state + the 0035 pairing-governor grant fix; RBAC amendment 001 (109 keys), audit amendment 001. Closeout: cloud db:test exit 0 (369 PASS), hub:db:test 40, terminal-health 8/8, ingestion 2/2, hub-agent 343/2/2 (recorded flake + recorded race-A mapping finding), secret:scan 1368 clean. Record: 2026-08-06__SHARED__WS-11-T005-P02__...__AI-HANDOFF.md |
| **WS-11 remaining task register (T005–T008 titles)** | **RESOLVED** (2026-08-06) | E-DOC KLD-2026-08-06-WS11-REMAINING-TASKS-001 (docs/decisions/kitluy-ws-11-t005-t008-task-register-owner-decision-v1.0.0.md) + canonical task cards 00_AI_HANDOFF/tasks/WS-11-T005.md..WS-11-T008.md. T005 Device Fleet Health, Support Access and Incident Containment; T006 Store Hub Replacement, Recovery and Signed Release Lifecycle; T007 Device Security, Offline, Recovery and Concurrency Verification; T008 Independent WS-11 Review, Evidence Reconciliation and Closeout. Titles only — no task is thereby started or completed. |
| **WS-11-T004 — overall** | **IMPLEMENTED-IN-DEV** (2026-08-05) | Terminal provisioning, activation, LAN transport and pairing, complete in development. Cloud migrations 0162–0176 (provisioning codes, evaluator, lockout, revocation, expiry, replacement, recovery, PoP, atomic redemption, composition identity + NOINHERIT gateway, activation completion, challenge reconciliation, pairing-receipt fleet ingestion); Hub migrations 0031–0034 (pairing authority, owner-locked 300 s clamp, credential projection, §8 scope index). Packages: `@kitluy/device-identity` (provisioning PoP, activation ack, pairing, edge discovery, credential package) and the NEW `@kitluy/terminal-local-store` (encrypted terminal-local relational store). Services: device-registry provisioning and activation compositions, cloud bootstrap routes, pairing-receipt ingestion; Hub agent LAN mTLS transport on 7443, signed discovery, activation and pairing routes, credential projection consumer, receipt replication. Closeout evidence, one serialized run: cloud `db:test` **229**, `test:rls` **131**, `hub:db:test` **37**, device-identity **867/867**, device-registry **363/363**, hub-agent **344 passed** (1 pre-existing WS-10-T006 flake, recorded), terminal-local **14/14**; role-privilege and residue censuses clean (no inherited effective-privilege defect; 20 receipts = 20 replication events; zero fabricated acknowledgements; zero temporary objects); `secret:scan` **1356 clean**; `clock:check` PASS; **`pnpm verify` 11/12** with only the pre-existing repository-wide format condition failing. Rebuild Test performed and documented; it surfaced two schema-contract gaps in already-shipped work (Hub 0032 unregistered, Hub 0033 missing its §8 scope index and census entry), both fixed forward. **Pilot and production remain BLOCKED BY BLK-005** and are not required for IMPLEMENTED-IN-DEV. **BLK-006** still gates the authenticated Hub→cloud activation bridge, the cloud producer of the credential-projection delivery, and the signed Hub→cloud batch transport. Record: `00_AI_HANDOFF/shared/2026-08-05__SHARED__WS-11-T004__FINAL-CLOSEOUT__AI-HANDOFF.md` and the T004 capability census. |
| **WS-11-T003 (all steps) — overall** | **COMPLETED-IN-DEV** (2026-08-01) | Formal closure package: Step 4 = IMPLEMENTED-IN-DEV WITH RECORDED ENVIRONMENT CONDITION; Steps 1-3 previously closed. All reviewer findings accounted (R1 APPROVED; R2 AWC with every condition fixed and re-verified; R3 APPROVED with every note closed). KLRISK-DEVICE-012 registered (0137 abandoned-key destruction unreachable at confirm; superseded-key destruction proven). Closure SHAs: implementation evidence end ff15a80, reviewer-note closure b3989bf, formal closure = this record commit. WS-11 stays IN PROGRESS; Cycle 10 IN PROGRESS; WS-11-T004..T008 NOT STARTED; pilot and production readiness NOT PROMOTED. Environment conditions explicit: CRLF format, classified-document verification, Hub destructive restore tooling, sanctioned test clock inert, KLRISK-DEVICE-003 OPEN. Next permitted identifier WS-11-T004, title NOT RESOLVED FROM CURRENT REGISTER (separate discovery package required). Record: 00_AI_HANDOFF/shared/2026-08-01__SHARED__WS-11-T003-STEP4__RUNTIME-ENFORCEMENT-AND-REVIEW-REMEDIATION__AI-HANDOFF.md (FORMAL CLOSURE section). |
| **WS-11-T003 Step 4 — Device Credential Revocation, Emergency Governance, Offline Enforcement and Lifecycle Containment** | **IMPLEMENTED-IN-DEV WITH RECORDED ENVIRONMENT CONDITION** (2026-08-01) | **This promotion is Step 4 ONLY. WS-11 overall stays SCAFFOLDED / IN PROGRESS; Cycle 10, pilot and production readiness are NOT promoted.** E-MIG cloud 0159 (environment-wide revocation read closed) + 0160 (job-governor membership leak repaired; narrow governor-owned readers and harness-only test scaffold), hub 0029 (signed revokedDeviceRecordIds enforced) + 0030 (governed snapshot staging; sync worker cannot fabricate an active snapshot); 0159/0160/0029 were corrected BEFORE FIRST APPLICATION after canonical from-zero replay exposed three committed-but-unreplayed defects. E-TEST from a from-zero reset under Node 22.23.0/pnpm 9.15.9: db:test **196 PASS**, test:rls **104**, hub:db:test **36**, device-identity **778 passed / 2 skipped (baseline)**, hub-agent **293 passed / 2 skipped (baseline)**, registry service **219/219** including the **30-stage production lifecycle 30/30 (executed=30/30)** with pass markers only after assertions, the post-approval REFUSE branch end-to-end 14/14, the three concurrency scenarios, and the Section-13 spendability census 14/14 with every zero-item at zero (two documented bounded counts; one expired leftover grant recorded per R2-RV-001). Aggregate pnpm verify **12/13**; the single failure is the pre-existing repository-wide CRLF format condition (808 files), with docs:verify classification condition recorded alongside. E-SEC live Hub gate enforces the held signed snapshot offline before authorization with no fallback; production lapse worker claims and executes with durable lease semantics; unsafe cross-Tenant snapshot path unreachable (KLUY-REVOCATION-READ-UNSCOPED reproduced); KLRISK-DEVICE-011 CLOSED with census at zero. INDEPENDENT REVIEWS: R1 Hub/snapshot APPROVED, R2 jobs/permissions/post-approval APPROVED-WITH-CONDITIONS (both conditions fixed and re-verified), R3 lifecycle/evidence APPROVED — records in 00_AI_HANDOFF/reviews/2026-08-01__WS-11-T003-STEP4-R*.md. KLRISK-DEVICE-003 remains OPEN. New finding recorded: 0137 abandoned-key destruction basis unreachable at confirm (abandon_chk constraint), future additive fix. Follow-up (b3989bf): all reviewer notes closed (refusal-reason assertions, exact residue enumeration, partition-reconciled census); one orphan lapse job of the superseded kind retired to manual_review through the governed queue; re-verified 219/219 + verify 12/13. Full record: 00_AI_HANDOFF/shared/2026-08-01__SHARED__WS-11-T003-STEP4__RUNTIME-ENFORCEMENT-AND-REVIEW-REMEDIATION__AI-HANDOFF.md. |
| **WS-11-T003 Step 2 — trusted time** | **IMPLEMENTED-IN-DEV** (2026-07-28) | **This promotion is Step 2 ONLY. WS-11 overall stays SCAFFOLDED / IN PROGRESS.** E-MIG cloud groups 0123 (trusted-time model, signed trust policy) and 0124 (four-eyes gated emergency correction). E-TEST db:test **155**; @kitluy/device-identity **168**; verify **13/13** including the new `clock:check` gate; docs:verify **8/8**; hub gates re-run separately and unchanged at 27/35. E-SEC trusted time is the MAXIMUM of validated sources; a source more than the ruled 300s behind the floor is rollback whatever its provenance; a jump beyond the SIGNED forward threshold is refused; the floor is monotonic against direct UPDATE by the database owner. Bootstrap tokens are CHALLENGE-BOUND (challengeId, assignmentGeneration, activationAttemptId inside the signed preimage) so an old correctly-signed token cannot bootstrap a wrong floor, and the nonce is consumed only AFTER the floor and audit commit. `trusted_time_max_forward_jump_seconds`, `trusted_time_token_max_lifetime_seconds` and `trusted_time_token_allowed_skew_seconds` are NULLABLE signed-policy values that FAIL CLOSED; pilot and production carry no policy at all. Emergency correction is gated on the EXISTING kitluy_auth four-eyes aggregate — risk class A3/A4 with an UNDECLARED class treated as insufficient, action, device and environment scope, quorum, non-self-approval and single use — with correction, floor advancement, approval consumption and audit in ONE transaction. Four governed consumers (certificate validity, renewal eligibility, revocation-snapshot validity, configuration-snapshot validity) all decide against trusted time; `pnpm clock:check --require-complete` reports COMPLETE 4/4 and was PROVEN non-vacuous by three attacks including a correctly-named stub that ignored trusted time. INDEPENDENT REVIEW **APPROVED-WITH-CONDITIONS** — `00_AI_HANDOFF/reviews/2026-07-28__WS-11-T003-STEP2-TRUSTED-TIME__REVIEW.md`. **RV-TT-001 (HIGH, blocking) was found BY ATTACK and fixed:** `trustedInstant()` keyed off `!isRestricted()`, so an evaluation with status `uninitialized` carrying a time was accepted as trusted by every consumer while the SQL layer already refused it — the C34/C35/C36 cross-layer divergence shape. Now only `trusted` yields an instant; regression and conformance tests added. Conditions C1-C4 carried. MATERIAL LIMITATIONS: **no real cryptography** — every `signatureValid` is a caller-supplied INPUT and `signature_verified` is false on every policy row, correct while no signer exists (step 6); no production caller; no concurrent-session SQL race test (T007); no RTC, TPM or secure element exists to test against; pilot activation, production activation, production signer and TPM/secure-element certification all remain BLOCKED |
| **WS-11 device provisioning and fleet management — T002 claim, scope and assignment** | **SCAFFOLDED / IN PROGRESS** (2026-07-28, T002 of 8 complete) | **BLK-005 is OPEN, so this row cannot advance and deliberately does not.** E-MIG cloud group 0121 adds `device_claims`, `device_assignments`, `device_terminal_assignments`, `device_assignment_projections`, `device_claim_events`, the `awaiting_trust` lifecycle state, and the claim/scope/assignment procedures. E-TEST cloud db:test **149** (140 + 9: assertions 29a-29e and RLS WS11-N5..N7/P2); test:rls **104**; @kitluy/device-identity **29 passed**; verify **11/11**; docs:verify **8/8**; Hub gates re-run SEPARATELY and unchanged at 27/35. E-SEC **the owner's two pre-T002 residual controls are closed structurally, not recorded and deferred.** (a) **KLRISK-DEVICE-001** — refusal evidence was caller-enforced; closed by `attempt_activate_device_v1`, which writes the refusal, commits, and RETURNS a typed outcome instead of raising, with the raising form REVOKED from `public` and `service_role`. There is no path that produces a refusal without producing its evidence, asserted via `has_function_privilege` (29e). (b) Duplicate hardware evidence now blocks activation, claim creation AND claim redemption, and enrollment quarantines BOTH identities — which unit is the clone is not knowable from the evidence. THE ACTIVATION BOUNDARY IS A STATE MACHINE: `enrolled -> active` was REMOVED from the transition matrix, so `active` is reachable only through `awaiting_trust`, which means only through an accepted claim and a bound assignment. The TypeScript matrix was changed in the same commit and has a test asserting `enrolled -> active` is false — the WS-10 C34/C35/C36 lesson applied. All seventeen owner-required adversarial cases execute and refuse with their own codes; case 17 (activation with BLK-005 open) refuses a device that is otherwise completely ready, with no certificate, no state drift, no assignment widening, no offline projection and no live terminal. Reuse and recovery are distinguished: the SAME device re-presenting the SAME token recovers the SAME assignment, a DIFFERENT device is refused. DEFECTS FOUND AND FIXED (C38/C39/C40): claim expiry was evaluated against transaction-start `now()` rather than `clock_timestamp()`, so a long transaction could redeem an expired claim; assignment generation could be REUSED after revocation because it was computed from the device's current value (0) rather than the highest ever issued, which would have let a stale Hub pass as current; and a re-created view silently lost its service grant, caught by an RLS control case rather than by the migration succeeding. Evidence `docs/evidence/phase1/ws-11/WS-11-T002-EXECUTION-EVIDENCE.md`. MATERIAL LIMITATIONS: no activation is possible at all; `device_assignment_projections` is necessarily EMPTY and its emptiness is asserted; **no production caller** — every 0121 function is reachable only from the test harness; concurrency is proven by a partial unique index exercised sequentially, so a true concurrent-session test is owed to T007; claim tokens are generated outside the database and no such generator exists; the both-identities-held rule creates an enrollment-station denial-of-service whose runbook mitigation is OWED; **no independent review yet** (T008) |
| **WS-11 device provisioning and fleet management — T001 enrollment and identity** | **SCAFFOLDED / IN PROGRESS** (2026-07-28, T001 of 8 complete) | **BLK-005 is OPEN, so this row cannot advance and deliberately does not.** T001 built what needs no cryptographic decision. E-MIG cloud group 0120 creates `kitluy_devices` (schema name already in DD v1.0.0) with 11 relations, RLS ENABLE+FORCE, zero anon policies, zero client write paths, and PUBLIC EXECUTE revoked on every function — applying the WS-10 migration-0020 lesson rather than repeating the finding. E-TEST cloud db:test **140** (124 + 16: assertions section 28a-28k and RLS WS11-N1..N4/P1); test:rls **100 `NOTICE:  PASS` / 99 distinct cases**; @kitluy/device-identity **24 passed**; verify **11/11**; docs:verify **8/8**; Hub gates re-run SEPARATELY after the cloud reset per KLRISK-HUB-006 and unchanged at 27 migrations / 35 assertions. E-SEC the owner's identity rule is enforced structurally: `devices.id` is an opaque server-generated uuid and assertion 28a computes the FORBIDDEN `sha256(mac‖board)` construction and proves no device carries it, while the MAC is still recorded as a binding signal. A changed signal quarantines the SAME device_record_id, creates no new identity and leaves the sealed manifest byte-identical (28c). A duplicate non-storage signal gives the second unit its OWN identity and raises a CRITICAL incident — detection by index, deliberately NOT a UNIQUE constraint, because a clone rejected by a constraint leaves no evidence (28j). Replacement records the owner's required order as facts, and `private_key_carried_over` is pinned false by CHECK while re-enrollment REFUSES the prior public-key fingerprint, so a copied key is refused from both sides (28f). THE BLK-005 GATE: `pki_trust_configuration` is created EMPTY and never seeded; `activate_device_v1`/`issue_device_certificate_v1` call `assert_pki_configuration_approved()` FIRST; 6/6 refusals across development/pilot/production, every message carrying an explicit `[REQUIRED: ...]` marker and `BLK-005`, with no state drift and zero certificate rows (28b). Five ways of faking the gate open are refused, including a placeholder or `[REQUIRED: ...]` approval reference and one signing key shared across purposes (28i); a client can neither read nor insert the gate table (WS11-N3) nor execute the governance procedures (WS11-N4). DEFECT FOUND AND FIXED: the refusal record inside `activate_device_v1` was rolled back by the very raise it documented — the same shape as WS-10's `verifySnapshot` — so refusal evidence moved to `record_activation_refusal_v1`, called by the caller in a NEW transaction. Evidence `docs/evidence/phase1/ws-11/WS-11-T001-EXECUTION-EVIDENCE.md`; ballot `docs/decisions/kitluy-blk-005-pki-and-device-trust-owner-ballot-v1.0.0.md`. MATERIAL LIMITATIONS: **no CA, no certificate issuance, no key generation, no revocation distribution, no signer, no production activation** — all BLK-005; `issue_device_certificate_v1` records a STATUS and issues nothing even once BLK-005 is ruled; no Hub claim, activation flow, assignment model, terminal assignment, provisioning session or fleet telemetry (T002+); **no production caller** — every 0120 function is reachable only from the test harness, the same limitation WS-10 carries; `assignment_generation` is a column, not a lifecycle, and nothing increments it; the `activation_blocked` incident depends on the caller reporting its own failure, which the database cannot compel; NEW gap **G12 clock bootstrap** (certificate validity is a time window and nothing establishes trusted time on an RTC-less Pi) recorded as ballot item 11 rather than guessed; DD relation-dictionary deviations D1-D5 owe an amendment before WS-11 closes; **no independent review yet** (T008) |
| **WS-09 Store Hub local runtime and persistence (edge_* schema, command layer, journals, safety modes)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-MIG 15 Hub migrations applied from zero into the SEPARATE `kitluy_hub_local` database, checksum-registered (editing an applied file is refused — proven); 54 `edge_*` base tables. E-TEST hub-agent 147 passed / 2 skipped (destructive backup-restore opt-in, run separately 2/2); Hub assertions **29** (counted as `NOTICE:  PASS`); double seed idempotent; engine suites reused unmodified (65/40/9/4); cloud regressions unchanged db:test 121, test:rls 95, verify 11/11. E-SEC 19-case authorization matrix + independent 18-case re-derivation each asserting zero write delta; 32-attack append-only probe incl. superuser; fabricated-acknowledgement rejection proven four ways; real crash-before/after-commit via pg_terminate_backend; Hub read-only blocks mutations while reads serve. Evidence `docs/evidence/phase1/ws-09/WS-09-EXECUTION-EVIDENCE.md`; review `00_AI_HANDOFF/reviews/2026-07-27__WS-09-STOREHUB__REVIEW.md` **APPROVED-WITH-CONDITIONS** (44 independent probes, all held, no blocking trigger). MATERIAL LIMITATION: plant production stages have no approved route/RBAC key (KLREQ-028), so no end-to-end T1→T4 lifecycle exists through the Hub command surface alone. Engine authority is a COMMAND-LAYER guarantee, not a DB constraint (KLRISK-HUB-003). NOT INTEGRATION-VERIFIED |
| **WS-01 local Supabase development foundation** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST reset-from-zero + healthy stack (-x logflare,vector) + deterministic types; evidence `docs/evidence/phase1/ws-01/WS-01-T004/EXECUTION-EVIDENCE.md`; review 2026-07-27__WS-01-04-EXECUTION APPROVED |
| **WS-02 identity/tenancy persistence (0010)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST migrations applied + seeds idempotent + assertions + RLS cases executed (same evidence + review) |
| **WS-03 Digital Store/Location persistence (0020)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST same evidence chain (primary-vertical invariant asserted live) |
| **WS-04 authorization/audit/approvals + RLS (0030/0035)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST 9/9 assertions + 23/23 RLS (14 neg/9 pos) + live four-eyes/append-only probes denied + actual-DB policy inventory (44 SELECT/0 write/0 anon) |
| Migrations 0010/0020/0030/0035 (45 tables, helpers, policies) + seeds + RLS/assertion test files + local-exec tooling | superseded row — see IMPLEMENTED-IN-DEV entries above (2026-07-27) | E-REPO `supabase/migrations/20260726190010..190035_*.sql`, `supabase/seed/`, `supabase/tests/`; static gates PASS 2026-07-26; 3 reviews (blocking RV-201/202/301/302 fixed) |
| Finance-subledger DD amendment 001 (6 kitluy_finance relations) | CONTRACT-APPROVED | E-REPO `docs/data/kitluy-suite-supabase-data-dictionary-amendment-001-finance-subledger-v1.0.0.md` + review 2026-07-26__FIN-DD-001 (APPROVED) |
| Governing Phase 1 security test system (114 KLSEC; 190/190 aliases) | CONTRACT-APPROVED | E-REPO `docs/security/kitluy-phase1-security-test-system-v1.0.0.md` + review 2026-07-26__SEC-CONS-001 (APPROVED, counts reproduced) |
| Canonical Supabase schema specification (235/235 dictionary parity, 18-invariant matrix) | CONTRACT-APPROVED | E-REPO `docs/data/kitluy-suite-supabase-schema-v1.0.0.md` + review 2026-07-26__WS-02-T001 (APPROVED-WITH-CONDITIONS, applied) |
| Canonical RLS/authorization specification (24 schemas, RLS-001..030 plans) | CONTRACT-APPROVED | E-REPO `docs/data/kitluy-suite-supabase-rls-and-authorization-v1.0.0.md` + review 2026-07-26__WS-04-T001 |
| Canonical migration plan (0000-0160) + 0000 controls migration + db harness | Plan CONTRACT-APPROVED; migration SQL SCAFFOLDED | E-REPO `docs/data/kitluy-suite-supabase-migration-plan-v1.0.0.md`, `supabase/migrations/20260726180000_*.sql`; static harness PASS 2026-07-26; DB execution BLOCKED (BLK-002) |
| Contract vocabulary + Edge API decision package (5-group ballot) | CONTRACT-APPROVED document; ALL GROUPS OWNER-APPROVAL-REQUIRED | E-REPO `docs/decisions/kitluy-contract-vocabulary-and-edge-api-owner-decision-v1.0.0.md` + review 2026-07-26__KL-DEC-001 |
| Laundry Booking lifecycle + production state machines (KBR-LND/TXN encoded) | superseded row — engine now DB-bound, see **WS-07** IMPLEMENTED-IN-DEV above (2026-07-27) | E-REPO+E-TEST `verticals/phase1-laundry/test/` (65/65, unchanged and re-run 2026-07-27; independent review PASS-WITH-CONDITIONS, RV-001 applied) |
| Payments engine passing all 26 canonical vectors | superseded row — engine now DB-bound, see **WS-08** IMPLEMENTED-IN-DEV above (2026-07-27) | E-REPO+E-TEST `packages/payments/test/vectors.test.ts` (40/40, unchanged and re-run 2026-07-27; four-eyes review, blocking RV-001 fixed + regression) |
| Data dictionary Amendment-002 (10 kitluy_core customer-identity/consent relations, KLREQ-012) | PROPOSED — technically authored, independently reviewed; **owner approval NOT granted** | E-REPO `docs/data/kitluy-suite-supabase-data-dictionary-amendment-002-customer-identity-and-consent-v1.0.0.md`; mirrors the implemented+tested 0060/0065 schema; open owner values (CUS-OD-001..004, retention) remain `[REQUIRED]` |
| Documentation corpus governance tooling (inventory/hash/classify/coverage checks) | SCAFFOLDED | E-REPO+E-TEST `scripts/docs/` (pnpm docs:verify, pass 2026-07-26) |

<!-- registry-check:end -->

### Documentation-status changes from KL-DOCS-001 (no evidence required at these levels)

| Item | Old | New | Source |
| --- | --- | --- | --- |
| **WS-05 catalog, pricing and configuration persistence (0040/0045/0050)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST reset-from-zero 11 migrations + 73-case suite + live probes (overlap guard, snapshot immutability, scope escape); evidence `docs/evidence/phase1/ws-05/WS-05-T001/EXECUTION-EVIDENCE.md`; review 2026-07-27__WS-05-06-EXECUTION APPROVED |
| **WS-06 customer identity and consent persistence (0060/0065)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST same chain (E.164/merge/consent probes; composite tenant FK fix probe-verified); same evidence + review |
| **WS-01 local Supabase development foundation** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST reset-from-zero + healthy stack (-x logflare,vector) + deterministic types; evidence `docs/evidence/phase1/ws-01/WS-01-T004/EXECUTION-EVIDENCE.md`; review 2026-07-27__WS-01-04-EXECUTION APPROVED |
| **WS-02 identity/tenancy persistence (0010)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST migrations applied + seeds idempotent + assertions + RLS cases executed (same evidence + review) |
| **WS-03 Digital Store/Location persistence (0020)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST same evidence chain (primary-vertical invariant asserted live) |
| **WS-04 authorization/audit/approvals + RLS (0030/0035)** | **IMPLEMENTED-IN-DEV** (2026-07-27) | E-TEST 9/9 assertions + 23/23 RLS (14 neg/9 pos) + live four-eyes/append-only probes denied + actual-DB policy inventory (44 SELECT/0 write/0 anon) |
| Migrations 0010/0020/0030/0035 (45 tables, helpers, policies) + seeds + RLS/assertion test files + local-exec tooling | superseded row — see IMPLEMENTED-IN-DEV entries above (2026-07-27) | E-REPO `supabase/migrations/20260726190010..190035_*.sql`, `supabase/seed/`, `supabase/tests/`; static gates PASS 2026-07-26; 3 reviews (blocking RV-201/202/301/302 fixed) |
| Finance-subledger DD amendment 001 (6 kitluy_finance relations) | CONTRACT-APPROVED | E-REPO `docs/data/kitluy-suite-supabase-data-dictionary-amendment-001-finance-subledger-v1.0.0.md` + review 2026-07-26__FIN-DD-001 (APPROVED) |
| Governing Phase 1 security test system (114 KLSEC; 190/190 aliases) | CONTRACT-APPROVED | E-REPO `docs/security/kitluy-phase1-security-test-system-v1.0.0.md` + review 2026-07-26__SEC-CONS-001 (APPROVED, counts reproduced) |
| Canonical Supabase schema specification (235/235 dictionary parity, 18-invariant matrix) | CONTRACT-APPROVED | E-REPO `docs/data/kitluy-suite-supabase-schema-v1.0.0.md` + review 2026-07-26__WS-02-T001 (APPROVED-WITH-CONDITIONS, applied) |
| Canonical RLS/authorization specification (24 schemas, RLS-001..030 plans) | CONTRACT-APPROVED | E-REPO `docs/data/kitluy-suite-supabase-rls-and-authorization-v1.0.0.md` + review 2026-07-26__WS-04-T001 |
| Canonical migration plan (0000-0160) + 0000 controls migration + db harness | Plan CONTRACT-APPROVED; migration SQL SCAFFOLDED | E-REPO `docs/data/kitluy-suite-supabase-migration-plan-v1.0.0.md`, `supabase/migrations/20260726180000_*.sql`; static harness PASS 2026-07-26; DB execution BLOCKED (BLK-002) |
| Contract vocabulary + Edge API decision package (5-group ballot) | CONTRACT-APPROVED document; ALL GROUPS OWNER-APPROVAL-REQUIRED | E-REPO `docs/decisions/kitluy-contract-vocabulary-and-edge-api-owner-decision-v1.0.0.md` + review 2026-07-26__KL-DEC-001 |
| Laundry Booking lifecycle + production state machines | REQUIRED VALUE | SPECIFIED | `docs/source/business-rules/kitluy-laundry-state-machines-v1.0.0.md`, `kitluy-transaction-and-booking-lifecycle-v1.0.0.md` |
| Governed API specifications (4) + registries | PLANNED | SPECIFIED | `docs/source/api-contracts/` |
| Event/job/webhook registries + outbox pattern + compatibility policy | PLANNED | SPECIFIED | `docs/source/api-contracts/` |
| Canonical business rules (10 documents) | PLANNED | SPECIFIED | `docs/source/business-rules/` |
| Security pack (RBAC registry 107 keys, audit registry, threat model, policies) | PLANNED | SPECIFIED | `docs/source/security/` |
| Store Hub/offline pack (11 documents) | PLANNED | SPECIFIED (LAN API + terminal-profile contract NEED RECONCILIATION) | `docs/source/offline/` |
| Shared-service specifications (9) | PLANNED | SPECIFIED | `docs/source/shared-services/` |
| UI/UX build pack (21 documents incl. provisional design tokens) | PLANNED | SPECIFIED | `docs/source/ui-ux/` |
| Engineering standards pack (13 standards + pack manifest) | — | SPECIFIED (toolchain adoption pending KLREQ-009) | `docs/source/engineering/` |
| QA/testing pack: master test plan, matrices, 524-case registry, payment vectors | — | SPECIFIED (all cases SPECIFIED_NOT_EXECUTED; zero execution evidence) | `docs/source/qa/` |
| Infrastructure/operations pack (14 runbooks/plans) | — | SPECIFIED (SLOs/domains/providers remain [REQUIRED]) | `docs/source/infrastructure/` |
| AI Swarm Operating System pack | — | SPECIFIED (adoption pending KLREQ-010) | `docs/source/owner-instructions/kitluy-ai-swarm-operating-system-v1.0.0/` |
| Supabase implementation pack | REQUIRED VALUE | SPECIFIED-PARTIAL (10 of 13; schema/RLS/migration-plan missing) | `docs/source/data-contracts/` |
